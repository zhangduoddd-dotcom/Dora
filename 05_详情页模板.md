# 详情页模板

## 详情页页面模板规范

详情页用于承载单个对象、事件、任务、资产、模型、风险、报告或分析结果的完整信息。详情页的核心目标是帮助用户完成“看清对象状态、理解关键原因、定位关联信息、执行下一步操作”。

详情页分为两大类：

```text
抽屉式详情
  ↓
适合从列表页或监控页快速查看，不打断主流程

独立页面详情
  ↓
适合信息量较大、层级较深、需要持续阅读或综合分析的场景
```

### 详情页通用信息结构

详情页应按以下顺序组织信息：

```text
标题区 / 对象名称 / 状态标签 / 操作区
  ↓
基础信息 / 摘要信息 / 关键指标
  ↓
核心分析区 / 对比区 / 图表区 / 时间线区
  ↓
详细字段 / 描述表格 / 关联数据
  ↓
操作建议 / 处置动作 / 导出 / 返回
```

复杂详情页可采用：

```text
顶部对象摘要
  ↓
标签页 / 锚点导航
  ↓
分组内容模块
  ↓
图表 + 表格 + 说明文本组合
  ↓
底部关联记录 / 空状态 / 操作建议
```

### 抽屉式详情规范

抽屉式详情适用于从列表、告警、事件、监控卡片中快速打开详情。背景页面应保留上下文，并通过遮罩弱化。

#### 抽屉布局

- 抽屉从页面右侧滑出。
- 宽度可根据内容复杂度设置为 `560px / 880px / 80%`。
- 信息量较大的分析型详情可使用接近全屏的大抽屉。
- 背景页面应添加半透明遮罩，保持用户对来源页面的上下文感知。
- 抽屉顶部固定显示标题、状态标签和关闭按钮。
- 抽屉内容区纵向滚动，顶部标题区不随滚动消失。
- 抽屉底部如有确认、处置、导出操作，可使用固定底部操作栏。

#### 抽屉标题区

标题区应包含：
- 对象名称
- 风险等级或状态标签
- 关闭按钮
- 可选操作按钮，如导出详情、处置、查看原始数据

标题示例：
- `10.17.3.30的异常SSL检测分析详情`
- `这是标题`
- `模型评估详情`
- `风险事件详情`

状态标签示例：
- 严重
- 高危
- 风险
- 安全
- 拒识
- AI 评估

#### 抽屉内容区

抽屉内容应按模块分段，每段使用小标题和左侧主色竖线加强识别。

推荐模块：
- 综合决策评估
- 基本信息
- 应用层模型评估
- 传输层模型评估
- 流信息评估
- 对比分析
- 处置建议
- 关联记录

### 分析评估抽屉详情模板

适用于异常流量、SSL 检测、通信检测、模型评分、安全策略分析等详情。

#### 页面结构

```text
抽屉标题区：对象名称 + 严重程度标签 + 关闭按钮
  ↓
综合决策评估：仪表盘评分 + 关键特征标签 + 流量指标卡
  ↓
基本信息：描述表格
  ↓
应用层模型评估：热力图 + 排名条形图
  ↓
传输层模型评估：雷达图 + 指标表
  ↓
流信息评估：描述表格 + 指标统计卡片
```

#### 综合决策评估区

- 左侧放置半圆仪表盘或评分仪表，突出模型评估分值。
- 中间展示关键特征标签，如“双向流 / 有证书 / 握手异常”。
- 右侧展示关键流量指标卡，如通信次数、上行包数、下行包数、上行负载、下行负载、包长度等。
- 指标卡建议横向排列，单个卡片只展示一个核心数值。
- 评分数值应大号展示，并包含分值单位，如 `75 分`。

#### 基本信息区

基础信息建议采用描述表格，字段可包括：
- 客户端 IP
- 客户端端口
- 服务端 IP
- 服务端端口
- 协议类型
- 网络流量
- 异常特征
- 会话时间
- 研判标签

描述表格规则：
- 每行 2–4 组字段。
- 字段名使用浅灰底。
- 字段值保持左对齐。
- 标签或可编辑字段可使用描边标签或输入态样式。
- 上行 / 下行数据可配合方向箭头表达。

#### 应用层模型评估区

可使用以下图表组合：
- 热力图：展示特征区间、模型评分、风险分布。
- 横向条形图：展示 Top 证书、Top 特征、Top 风险项。
- Tooltip：用于解释某个格子的评分或类别。

热力图规范：
- 使用主色渐变表示强弱。
- 每个格子显示数值。
- 图例位于图表上方。
- 鼠标悬停时显示短 tooltip。
- 不使用强烈彩虹色或复杂 3D 效果。

横向条形图规范：
- 排名数量建议 5–8 项。
- 条形使用主色或主色浅色系。
- 右侧展示数值。
- 排名从高到低排列。

#### 传输层模型评估区

可使用雷达图 + 指标表组合。

雷达图适合展示：
- 数据包个数
- 载荷最大值
- 子流载荷均值
- 数据包 window size
- 子流个数
- 上行流 / 下行流对比

规则：
- 雷达图系列不超过 2–3 条。
- 坐标维度建议 5–7 个。
- 右侧表格展示精确指标值。
- 图表与表格保持左右平衡布局。

#### 流信息评估区

流信息区适合使用“描述表格 + 统计卡片”组合。

描述表格可包括：
- 支持版本
- 通信版本
- 套件数量
- 选择套件
- 源 IP
- 目的 IP
- 威胁描述
- 证书信息
- 证书有效期

统计卡片可包括：
- 载荷信息均值
- 包长信息均值
- 流时长
- 发包速率
- 收包速率
- 最小值 / 最大值

统计卡片规则：
- 图标置于左侧。
- 核心数值放大显示。
- 最小值和最大值作为辅助指标。
- 颜色可根据指标类型使用蓝、绿等低饱和色。

### 对比分析抽屉详情模板

适用于 AI 模型评估、算法对比、检测结果对比、评分结果对比。

#### 页面结构

```text
抽屉标题区：标题 + 关闭按钮
  ↓
基础信息区：时间、任务、数据集、模型名称、数量等
  ↓
操作区：导出详情
  ↓
对比分析表：维度列 + 多模型对比列
```

#### 基础信息区

- 基础信息区可使用浅蓝渐变背景，弱化装饰并突出信息分组。
- 信息字段建议 2–3 列排列。
- 重要字段可使用可复制、可点击或选中态样式。
- 导出详情按钮位于右上方。

基础字段示例：
- 时间
- 时间范围
- 数据集名称
- 模型名称
- 任务数量
- 评估对象

#### 对比分析表

对比表应采用横向模型对比结构：

```text
维度名称 / 指标名称
  ↓
模型 1 / 模型 2 / 模型 3 / 模型 4
```

每个模型列可包含：
- 模型图标
- 模型名称
- AI 评估标签
- 核心得分
- 时间
- 各项比例
- 维度指标

特殊状态：
- 最高分使用蓝色或绿色高亮。
- 最低分使用红色浅底高亮。
- 安全、拒识、风险等标签放在左侧维度区域。
- 同一行不同模型的数据应严格横向对齐。

#### 视觉规则

- 对比表列宽应一致。
- 分割线使用浅灰细线。
- 最高 / 最低标识只标注最关键位置，不要重复过多。
- 对比页不应使用过多图表，重点是表格对齐与可比较性。

### 独立详情页规范

独立详情页适合承载完整业务对象的信息，通常由面包屑、对象摘要、分组模块、图表、表格和操作建议组成。

#### 独立详情页结构

```text
顶部导航 / 侧边导航
  ↓
面包屑 / 页面标题
  ↓
对象摘要区
  ↓
标签页 / 锚点导航 / 分组导航
  ↓
详情内容区
  ↓
关联数据 / 表格 / 日志 / 空状态
```

#### 使用场景

- 风险事件详情
- 资产详情
- 模型详情
- 下载行为详情
- 安全评估详情
- 报告型详情
- 运行状态详情
- 消息推送详情

### 时间轴型详情页模板

适用于风险行为、事件过程、攻击链、操作记录、异常轨迹等。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
面包屑
  ↓
标题区：对象名称 + 状态标签 + 操作按钮
  ↓
摘要信息区
  ↓
风险行为时间轴
  ↓
时间节点卡片
  ↓
展开详情表格
  ↓
嵌套表格 / 关联记录
```

#### 标题与摘要区

标题区应包括：
- 对象名称
- 风险标签
- 次要按钮
- 主按钮
- 操作按钮组

摘要区可包括：
- 事件名称
- 时间
- 对象信息
- 用户信息
- 部门信息
- 风险描述

#### 时间轴规则

- 时间轴左侧使用时间点标记。
- 每个节点使用卡片承载行为摘要。
- 节点卡片中可包含标签、字段组、描述信息。
- 节点详情可支持展开 / 收起。
- 展开后展示描述表格或关联表格。
- 多个节点之间保持统一间距。
- 当前重点节点可使用主色或风险色强调。

#### 展开详情规则

- 默认展示摘要信息。
- 点击“展开详情”后展示表格式字段。
- 复杂数据可使用嵌套表格。
- 展开状态应有箭头方向变化。
- 收起后保留节点摘要与时间信息。

### 图文概览型详情页模板

适用于风险对象、文件对象、告警对象、资产对象的图文摘要型详情。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题 / 面包屑
  ↓
对象摘要卡片：插图 / 状态 / 标题 / 标签 / 关键字段 / 操作按钮
  ↓
标签页
  ↓
指标卡片 + 排名图表
  ↓
筛选区 + 表格
  ↓
分页
```

#### 对象摘要卡片

摘要卡片可包含：
- 左侧对象插图或图标
- 风险等级文字，如“高危”
- 主标题
- 标签组
- 次要信息卡片
- 描述字段
- 右上角主次操作按钮

规则：
- 插图不应过大，避免压缩信息区。
- 风险等级应与状态颜色一致。
- 标签数量建议 2–4 个。
- 主操作位于右上角。
- 摘要信息最多分为 2–3 列。

#### 标签页内容区

标签页用于组织不同维度信息，如：
- 概览
- 关联数据
- 处置记录
- 影响范围
- 日志记录

标签页规则：
- 激活标签使用主色高亮。
- 标签数量建议 3–6 个。
- 标签页下方模块应根据当前标签内容变化。
- 不同标签页的头部摘要保持一致。

### 资产画像型详情页模板

适用于资产、设备、主机、节点、服务、系统状态详情。

#### 页面结构

```text
返回 / 面包屑 / 标题
  ↓
顶部标签页或对象切换栏
  ↓
横向指标摘要
  ↓
资产基础卡片
  ↓
能力 / 状态 / 配置模块
  ↓
描述表格
  ↓
关联消息表格 / 空状态
  ↓
右侧锚点导航
```

#### 指标摘要区

- 横向展示多个对象或指标。
- 每项包含图标、标题和数值。
- 当前选中项使用浅绿色背景。
- 指标数量较多时可横向滚动或均分展示。
- 数值应大于标题，形成视觉重点。

#### 资产基础卡片

资产基础卡片可包含：
- 资产图标
- 资产名称
- 更新时间
- 用户名 / 用户组
- 操作图标
- 平台或系统标签
- 详情入口

#### 状态与配置模块

适合展示：
- 资源占比
- CPU / 内存 / 存储状态
- 硬件信息
- 软件信息
- 系统功能图标
- 模块启用状态

规则：
- 左侧可放进度条指标。
- 中间可放图标宫格。
- 右侧可放锚点导航。
- 锚点导航用于快速跳转基本信息、硬件信息、消息推送等模块。

#### 空状态

当关联消息或记录为空时，应显示：
- 轻量空状态图标
- “暂无数据”文案
- 空状态居中显示
- 不使用过强插图或大面积空白装饰

### 报告型长详情页模板

适用于安全评估报告、风险分析报告、运营分析报告、巡检详情、综合分析详情。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题 + 操作按钮
  ↓
标签路径 / 顶部页签 / 下拉菜单
  ↓
信息提示 / 风险提示条
  ↓
指标摘要行
  ↓
分组模块 1
  ↓
分组模块 2
  ↓
Top 排名表格
  ↓
日志表格
  ↓
安全建议
```

#### 长页面分组规则

- 长详情页必须使用清晰的分组标题。
- 每个分组之间保持足够垂直间距。
- 重要提示使用浅黄色提示条。
- 风险提示使用红色图标或红色文字，但不宜整块大红。
- 分组标题可使用主色竖线增强识别。
- 重复型模块应保持相同结构，方便阅读。

#### 报告型模块

可包含：
- 标题说明
- 提示条
- 指标卡
- Top10 表格
- 日志表格
- 风险统计
- 安全加固建议
- 应急响应建议

#### Top 表格规则

- 排名建议展示 5–10 条。
- 第一列为序号。
- 中间列为对象名称或 IP。
- 最右列为数量或风险值。
- 小型 Top 表格可不展示分页。
- 长报告中多个 Top 表格应保持列结构一致。

### 详情页信息分组规范

详情页内容应按业务意义进行分组，不应简单堆叠字段。

推荐分组：
- 基础信息
- 风险信息
- 评分信息
- 模型评估
- 流量信息
- 证书信息
- 资产信息
- 时间线
- 对比分析
- 关联记录
- 操作建议
- 日志记录

分组标题规则：
- 标题简短，建议 4–10 字。
- 可使用主色竖线。
- 分组之间留白明显。
- 同一页面分组标题样式保持一致。

### 详情页图表使用规范

详情页图表服务于解释单个对象，不应像 Dashboard 一样堆叠大量概览图。

推荐图表：
- 仪表盘：用于评分。
- 热力图：用于特征分布。
- 雷达图：用于多维能力或特征对比。
- 条形图：用于 Top 排名。
- 折线图：用于趋势。
- 进度条：用于资源占比。
- 时间线：用于事件过程。

图表数量规则：
- 抽屉式详情中建议 2–4 个图表。
- 独立详情页中建议 3–6 个图表。
- 长报告详情可使用多个小型表格和指标卡替代复杂图表。
- 单个模块内图表类型不宜超过 2 种。

### 详情页表格规范

详情页中的表格分为描述表格、数据表格和对比表格。

#### 描述表格

用于展示对象属性：
- 字段名 + 字段值成对出现。
- 字段名可使用浅灰背景。
- 字段值保持左对齐。
- 每行建议 2–4 组字段。
- 空值使用“-”。

#### 数据表格

用于展示关联记录：
- 结构可参考列表页表格。
- 行数不宜过多，必要时跳转完整列表。
- 可展示 Top5 / Top10。
- 可保留简化分页。

#### 对比表格

用于多模型、多对象、多版本比较：
- 第一列为维度或指标名。
- 后续列为对比对象。
- 高亮最高值、最低值或异常值。
- 各列对齐严格一致。
- 单元格内避免堆叠过多标签。

### 详情页操作规范

详情页操作分为页面级操作、模块级操作和行级操作。

#### 页面级操作

常见操作：
- 返回
- 关闭
- 导出详情
- 处置
- 编辑
- 刷新
- 复制
- 查看原始数据

规则：
- 抽屉详情使用关闭按钮。
- 独立详情页使用返回或面包屑。
- 主操作放在标题区右侧。
- 次操作放在主操作左侧或收纳到更多菜单。
- 导出类操作使用次级按钮。

#### 模块级操作

常见操作：
- 展开详情
- 收起详情
- 查看更多
- 跳转列表
- 查看 Top 明细

规则：
- 模块操作应贴近模块标题。
- 展开/收起状态需有明确图标变化。
- 不要让模块操作与页面级操作混淆。

### 详情页状态与风险表达

详情页应统一使用状态标签表达对象风险。

#### 风险状态

- 严重 / 高危：红色
- 风险：红色或橙红色
- 拒识 / 异常：橙色
- 安全 / 正常：绿色
- 未知 / 无数据：灰色

#### 评分表达

- 评分应使用大号数字。
- 分值需带单位，如“75 分”。
- 最高分可使用蓝色或绿色标记。
- 最低分可使用红色浅底标记。
- 评分图表应配合文字说明，不单独依赖颜色。

### 详情页文案与假数据规范

- 对象标题：8–24 字。
- 分组标题：4–10 字。
- 标签文案：2–6 字。
- 按钮文案：2–6 字。
- 描述字段名：2–8 字。
- 时间格式：`2023-08-10 14:53:26`。
- IP 格式：`192.168.02.1`。
- 评分格式：`75 分`。
- 百分比格式：`24.23%`。
- 数量格式：`73,847`。
- 风险标签示例：`严重 / 高危 / 风险 / 安全 / 拒识`。
- 模型名称示例：`模型1 / 模型2 / 360safetyEval-test-s2裁判模型`。
- 业务描述文本不宜超过 3 行，长文本应折叠或放入详情区域。

### 详情页 AI 生图约束

- 必须明确详情页类型：抽屉式详情或独立页面详情。
- 抽屉式详情必须保留背景遮罩和关闭按钮。
- 独立详情页必须保留顶部导航、侧边导航、面包屑或返回入口。
- 详情页必须有明确对象标题和状态标签。
- 复杂内容必须分组，不可把所有字段平铺。
- 图表必须服务于单个对象解释，不要做成 Dashboard。
- 描述表格、对比表格、时间线、图表之间应形成清晰层级。
- 长详情页应通过标签页、锚点导航或分组标题降低阅读压力。
- 不要让主操作按钮超过 2 个。
- 不要在详情页中使用过多彩色标签。
- 不要把抽屉详情做成全屏列表页。
- 不要生成无意义的大段 lorem ipsum，说明文字应简短可信。

### 详情页 Prompt 模板

#### 分析评估抽屉详情 Prompt

```text
生成一张企业级安全产品分析评估抽屉详情高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景为被半透明遮罩弱化的列表页，右侧打开大抽屉。抽屉顶部包含对象名称、严重标签和关闭按钮。内容区依次包含综合决策评估、基本信息、应用层模型评估、传输层模型评估和流信息评估。综合评估区左侧为评分仪表盘，显示 75 分，中间展示关键特征标签，右侧为通信次数、上行包数、下行包数等指标卡。基本信息使用描述表格。应用层评估包含绿色热力图和横向排名条形图。传输层评估包含雷达图和指标表。流信息评估包含证书信息表格和统计指标卡片。整体为白色抽屉、浅灰背景、科技绿主色，信息清晰、分组明确。
```

#### 对比分析抽屉详情 Prompt

```text
生成一张企业级 AI 模型对比分析抽屉详情高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面背景为灰色遮罩，中央或右侧显示大尺寸详情抽屉。顶部为标题和关闭按钮。上方基础信息区使用浅蓝渐变背景，展示评估时间、数据集、模型名称、任务数量，并在右上角放置导出详情按钮。下方为对比分析表，左侧是指标维度，右侧横向排列模型1、模型2、模型3、模型4，每列包含模型图标、模型名称、得分、时间和多项指标。最高分用蓝色高亮，最低分用红色浅底标记。整体风格简洁、企业级、对齐严格。
```

#### 时间轴型独立详情页 Prompt

```text
生成一张企业级风险事件时间轴详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧导航、面包屑、标题区和右上角主次操作按钮。标题区展示事件名称和风险标签。主体内容为风险行为时间轴，每个时间节点包含时间、摘要卡片、标签、字段信息和展开详情入口。展开后显示描述表格和关联数据表格。页面使用白色卡片、浅灰背景、科技绿分组竖线和低饱和风险色标签，信息层级清晰。
```

#### 图文概览独立详情页 Prompt

```text
生成一张企业级图文概览详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧导航和内容区。顶部摘要卡片左侧展示风险对象插图和高危状态，右侧展示主标题、标签、次要信息、关键字段和主次操作按钮。摘要卡片下方是标签页，标签页内容包含指标卡片、Top5 横向条形图、筛选区、数据表格和分页。整体为浅灰背景、白色卡片、科技绿主色，插图轻量，表格和图表比例协调。
```

#### 资产画像独立详情页 Prompt

```text
生成一张企业级资产画像详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧导航、返回入口、标签页对象切换栏和内容卡片。顶部横向展示多个资产指标卡，当前选中项使用浅绿色背景。主体包含资产基础信息卡、资源占比进度条、图标宫格、描述表格、关联消息表格和右侧锚点导航。底部消息表格为空时展示居中的暂无数据空状态。整体简洁、留白适中、适合设备和资产详情场景。
```

#### 报告型长详情页 Prompt

```text
生成一张企业级安全分析报告型详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧导航、标题区、顶部标签路径、浅蓝科技背景区域、多个纵向分组模块和长页面滚动内容。每个分组包含标题、说明文字、浅黄色提示条、指标卡片、Top10 表格和风险建议。页面展示安全风险检测、历史检测、实时防护日志、漏洞情报、应急响应、安全建议等模块。整体采用白色卡片、浅灰背景、科技绿主色、低饱和警示色，长页面结构清晰，适合报告阅读。
```

### 详情页 Negative Prompt

```text
不要把详情页做成纯列表页，不要把抽屉式详情做成无背景上下文的孤立页面，不要缺少标题和关闭按钮，不要把所有字段堆在一屏里，不要无分组、不加标题，不要让图表与表格混乱重叠，不要使用过多高饱和彩色标签，不要使用大面积红色背景，不要生成无意义乱码或长段占位文字，不要让操作按钮分散在页面各处，不要让时间线节点间距不一致，不要让对比表各列错位。
```

### 详情页生成验收标准

| 检查项 | 标准 |
|---|---|
| 类型明确 | 能区分抽屉式详情或独立页面详情 |
| 标题完整 | 对象名称、状态标签、关闭或返回入口完整 |
| 信息分组 | 基础信息、分析信息、关联信息分组清晰 |
| 视觉层级 | 摘要、图表、表格、操作层级明确 |
| 图表合理 | 仪表盘、热力图、雷达图、条形图使用场景正确 |
| 表格可读 | 描述表格、对比表格、数据表格边界明确 |
| 操作清晰 | 主操作、次操作、模块操作不混淆 |
| 风险表达 | 风险色、状态标签、评分表达一致 |
| 长页组织 | 标签页、锚点或分组标题降低阅读压力 |
| 生图质量 | 文字基本可读，无明显乱码、变形、错位 |

---
