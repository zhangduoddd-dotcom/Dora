# 表单页模板

## 表单页面模板规范

表单页面用于完成数据录入、参数配置、规则编辑、策略设置、对象新增与对象修改。表单页面的核心目标是降低填写成本、减少配置错误、明确保存范围，并在复杂场景中通过分组、步骤、标签页、抽屉和条件构建器保持清晰的操作路径。

表单页面分为两大类：

```text
独立页面表单
  ↓
适合字段较多、配置层级较深、需要完整页面承载的场景

抽屉式表单
  ↓
适合新增、编辑、添加规则、轻量参数配置等局部操作场景
```

### 表单页面通用结构

表单页应遵循以下基本结构：

```text
页面路径 / 页面标题 / 返回入口
  ↓
标签页 / 步骤条 / 左侧对象树
  ↓
表单内容区
  ↓
分组标题 / 字段区 / 条件配置区 / 卡片配置区
  ↓
底部操作栏
```

通用规则：
- 表单内容应以白色容器承载，页面背景使用浅灰色。
- 字段标签左对齐，输入控件宽度统一。
- 必填项使用红色星号。
- 复杂表单必须分组，不应把所有字段无序堆叠。
- 保存、取消、上一步、下一步等操作应固定或靠近表单底部。
- 影响范围较大的配置应提供提示说明。
- 表单控件状态需包含默认、聚焦、禁用、错误、已选、只读。
- 表单页不应被设计成 Dashboard，不应加入大量图表。

---

### 独立页面表单模板

独立页面表单用于完整承载配置流程，适合较长、较复杂或需要多区域协作的配置任务。

#### 标准单栏表单页

适用于常规信息录入、基础配置、终端管理配置等。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题
  ↓
白色表单卡片
  ↓
顶部标签页
  ↓
单栏字段纵向排列
  ↓
底部保存按钮
```

#### 字段类型

可包含：
- 单行输入框
- 下拉选择器
- 日期 / 时间选择器
- 单选框
- 复选框
- IPv4 输入框
- 数字输入框
- 文件上传
- 开关
- 多行文本域

#### 布局规则

- 表单标签位于左侧，输入控件位于右侧。
- 标签宽度应统一，建议 120–160px。
- 控件宽度应统一，普通输入框建议 360–480px。
- 每行字段垂直间距保持一致。
- 提示说明放在字段下方，使用浅灰文字。
- 文本域右下角可显示字数计数，如 `0/100`。
- 保存按钮位于底部左侧或底部操作区左侧。

#### 视觉规则

- 当前标签页使用科技绿高亮。
- 必填星号使用红色。
- 输入框边框使用浅灰色。
- 聚焦状态使用主色描边。
- 开关开启状态使用主色，关闭状态使用灰色。
- 上传按钮为描边按钮，上传格式说明位于按钮下方。

#### 多层级导航表单页

适用于页面层级较深、左侧存在多级菜单或打开多个检测报告页签的场景。

#### 页面结构

```text
顶部导航
  ↓
左侧多级导航
  ↓
顶部页签 / 报告标签
  ↓
主表单区域
  ↓
底部保存按钮
```

#### 规则

- 左侧导航最多展示到 4–5 级，当前项高亮。
- 顶部可使用打开页签表示当前任务，如检测报告标签。
- 表单区仍采用单栏纵向布局。
- 当层级较深时，应通过面包屑或顶部页签帮助用户定位。
- 底部保存按钮应固定在内容区底部，避免长页面中找不到保存入口。

#### 左树右表单配置页

适用于按终端分组、组织、设备、策略对象进行配置的页面。

#### 页面结构

```text
页面标题 / 副标题
  ↓
左侧对象树 / 分组树
  ↓
右侧标签页
  ↓
右侧配置表单 / 卡片式配置区
```

#### 左侧树结构

可包含：
- 分组标题
- 搜索框
- 新增按钮
- 展开 / 收起箭头
- 节点名称
- 节点数量
- 当前选中项高亮

规则：
- 左侧树宽度应稳定。
- 当前选中节点使用浅绿色背景。
- 节点过长时使用省略号。
- 节点数量右对齐。
- 左右分栏之间使用竖向分隔线。
- 左树用于定位对象，右侧用于配置内容。

#### 右侧配置区

可包含：
- 标签页
- 分组标题
- logo / 图标预览
- 输入框
- 按钮
- 版本卡片
- 功能菜单卡片
- 场景菜单卡片

规则：
- 右侧内容跟随左侧选中对象变化。
- 当前标签页使用主色高亮。
- 配置说明应放在当前模块附近。
- 卡片式配置项应统一尺寸、间距和圆角。

#### 卡片式配置选择表单页

适用于版本选择、功能套餐、菜单模板、策略模板等。

#### 页面结构

```text
左侧对象树
  ↓
右侧配置卡片容器
  ↓
版本选项卡片
  ↓
功能菜单 / 场景菜单卡片
  ↓
底部操作按钮
```

#### 卡片内容

可包含：
- 单选圆点
- 图标
- 标题
- 适用说明
- 菜单模块卡片
- 状态标签，如“荐”“未购买授权”
- 恢复默认入口

#### 规则

- 当前选中卡片使用主色边框或浅绿色背景。
- 未选中卡片使用白底浅边框。
- 卡片内文字层级为：标题 > 描述 > 标签。
- 多组卡片之间保持明显分区。
- 卡片不宜过度装饰，保持后台配置属性。

#### 条件组合查询表单页

适用于高级查询、规则条件、筛选表达式、字段组合判断等场景。

#### 页面结构

```text
页面标题
  ↓
基础类型单选区
  ↓
条件组合容器
  ↓
多选条件区
  ↓
查询按钮
```

#### 条件组合区

可包含：
- 分组标题，如“操作系统”
- 关系选择器，如“且 / 或”
- 字段选择器
- 运算符选择器，如“包含 / 模糊 / 相等”
- 值选择器
- 新增条件按钮

#### 规则

- 条件容器使用浅灰边框卡片。
- 每组条件内部控件横向排列。
- 逻辑关系与字段选择应有清晰顺序。
- 新增按钮使用主色方形按钮。
- 多个条件组之间保持一致宽度和间距。
- 条件组支持展开 / 收起。
- 查询按钮应位于条件区下方，靠左或靠右保持统一。

#### 分步式策略配置页

适用于策略配置、规则启用、分阶段配置、配置向导等流程型表单。

#### 页面结构

```text
返回标题
  ↓
顶部步骤条
  ↓
左侧策略列表
  ↓
右侧当前步骤内容
  ↓
底部固定操作栏
```

#### 顶部步骤条

步骤状态包括：
- 已完成
- 当前步骤
- 未开始

规则：
- 已完成步骤使用对勾。
- 当前步骤使用主色圆点数字。
- 未完成步骤使用灰色。
- 步骤之间使用连接线。
- 步骤数量建议 3–5 个。

#### 左侧策略列表

可包含：
- 策略名称
- 完成数 / 总数
- 状态标签，如已配置、未配置、配置中
- 当前选中状态

规则：
- 当前选中策略使用主色边框或浅绿色背景。
- 策略列表可滚动。
- 数量格式如 `2/6`、`13/13`。
- 状态标签保持低饱和。

#### 右侧步骤内容

可包含：
- 折叠分组
- 开关
- 复选框
- 输入框
- 警告提示条
- 说明文本
- 多个可开关配置项

规则：
- 当前配置分组展开，其余分组可折叠。
- 分组右侧可放置开关。
- 警告提示使用浅黄色背景。
- 内容区应保持宽松，避免配置项过密。

#### 底部操作栏

可包含：
- 存为草稿
- 信息提示
- 上一步
- 下一步
- 取消

规则：
- 底部栏固定在页面底部。
- 主按钮为下一步。
- 上一步为次按钮。
- 存为草稿位于左侧。
- 中间可展示当前流程完成提示。

#### 模块能力选择表单页

适用于配置终端能力、软件模块、终端软件库、界面定制等。

#### 页面结构

```text
左侧终端分组树
  ↓
右侧配置页签
  ↓
顶部说明卡片
  ↓
配置分组
  ↓
可选模块 / 上传图标 / 开关 / 单选
  ↓
右侧锚点目录
```

#### 规则

- 顶部说明卡片使用浅灰或浅蓝底。
- 分组标题使用主色竖线。
- 锁定子分组状态使用标签提示。
- 模块开关或选项应位于分组内容中。
- 图片上传/图标预览区域应使用虚线边框或轻边框。
- 右侧锚点目录可浮动显示当前分组位置。
- 该类页面信息层级较深，必须通过分组和锚点降低阅读压力。

---

### 抽屉式表单模板

抽屉式表单用于在不离开当前页面的情况下完成新增、编辑、规则配置等任务。

#### 抽屉式表单通用结构

```text
背景页面遮罩
  ↓
右侧抽屉容器
  ↓
抽屉标题 + 关闭按钮
  ↓
表单内容区
  ↓
底部固定按钮区
```

通用规则：
- 抽屉从右侧滑出。
- 背景页面添加灰色遮罩。
- 抽屉标题固定在顶部。
- 右上角必须有关闭按钮。
- 内容区可纵向滚动。
- 底部按钮区固定，避免长表单中丢失确认入口。
- 默认按钮顺序为：取消 / 确认或取消 / 保存。
- 抽屉宽度根据复杂度使用中宽或宽抽屉。
- 中等复杂规则配置可使用大宽度抽屉，接近 60%–70% 页面宽度。

#### 标准新增 / 编辑抽屉表单

适用于新增节点、编辑节点、基础对象信息维护。

#### 表单字段

可包含：
- 节点名称
- 节点地址
- 访问令牌
- 描述
- 节点联系人
- 联系人电话

#### 布局规则

- 字段采用单列纵向排列。
- 必填项使用红色星号。
- 访问令牌字段可为只读输入框。
- 访问令牌右侧可放置复制按钮。
- 描述字段使用多行文本域。
- 编辑态预填数据，新增态为空白输入。
- 保存按钮使用主按钮，取消按钮使用次按钮。

#### 视觉规则

- 弹层遮罩为中性灰，不能过黑。
- 抽屉或弹窗主体为白底。
- 表单标签宽度统一。
- 输入框高度、宽度统一。
- 底部按钮右对齐。
- 编辑态禁用字段使用浅灰底。

#### 添加规则抽屉表单

适用于规则创建、监控规则、采集周期、阈值设置、白名单配置等。

#### 页面结构

```text
抽屉标题：添加规则
  ↓
规则名称
  ↓
规则描述
  ↓
配置方式
  ↓
组件选择
  ↓
规则内容配置卡片
  ↓
底部按钮区
```

#### 表单字段

可包含：
- 规则名称
- 规则描述
- 配置方式：JSON / 参数配置
- 组件：IVI / T-BOX
- 探针类型
- 采集周期
- 告警阈值
- IP 白名单
- 端口白名单
- 域名解析白名单
- CPU 过载运行监控
- 内存过载运行监控
- 磁盘空间过载运行监控

#### 规则内容区

规则内容区应使用浅灰底卡片承载复杂参数。

布局规则：
- 分组标题位于卡片顶部。
- 相关参数按 2–3 列排列。
- 单位如“秒”“%”放在输入框右侧。
- 白名单类字段支持动态新增与删除。
- 新增入口使用主色文字或按钮。
- 删除入口可使用减号圆形图标。
- 不同组件选项对应不同规则内容。

#### 参数配置型抽屉

适用于“选择不同组件后展示不同参数组”的场景。

#### IVI 组件配置

可包含：
- 探针：系统资源监控
- CPU 过载运行监控
- 内存过载运行监控
- 磁盘空间过载运行监控
- 采集周期
- 告警阈值

布局规则：
- CPU、内存、磁盘三个监控块可并列或分区显示。
- 每个监控块包含采集周期与告警阈值。
- 数字输入框旁显示单位。
- 必填字段带红色星号。

#### T-BOX 组件配置

可包含：
- 探针：网络监控
- 采集周期
- IP 白名单
- 端口白名单
- 域名解析白名单

布局规则：
- 白名单按列分组展示。
- 每组支持新增多条。
- 删除图标靠近对应输入项。
- 输入框应保持固定宽度。
- 域名输入框宽度可大于 IP / 端口输入框。

#### 动态增删字段抽屉

适用于 IP 列表、端口列表、白名单、黑名单、域名列表等可重复录入项。

规则：
- 每个动态组有明确标题。
- 新增按钮放在当前组下方。
- 删除按钮或减号图标放在字段右侧。
- 多行字段之间间距一致。
- 动态字段为空时至少保留一行输入。
- 需要校验重复值、格式和值域。

---

### 表单控件使用规范

#### 基础输入

- 普通文本输入用于名称、账号、标题、地址等。
- 数字输入用于端口、天数、阈值、采集周期等。
- IPv4 输入用于 IP 地址输入。
- Textarea 用于备注、规则描述、长文本说明。
- 密码或令牌输入默认脱敏。
- 只读字段使用禁用态或只读浅灰底。

#### 选择控件

- Select 用于状态、类型、策略、组件、字段、运算符选择。
- Radio 用于互斥选择，如开启/关闭、是/否、JSON/参数配置。
- Checkbox 用于多选配置项。
- Switch 用于功能启停。
- Date/Time Picker 用于时间选择。
- Tabs 用于平级配置模块切换。
- Steps 用于流程型配置。

#### 文件上传

- 上传按钮使用描边按钮。
- 上传说明放在按钮下方。
- 支持扩展名示例应明确，如 `.rar .zip .doc .docx .pdf .jpg...`。
- 文件上传字段不应与普通按钮混淆。
- 上传失败需要错误提示。

#### 提示与说明

表单提示可包含：
- Tooltip 问号图标
- 字段下方帮助文案
- 浅黄色警告提示条
- 浅蓝色信息说明块
- 字数计数器
- 默认值说明
- 锁定说明

规则：
- Tooltip 解释短概念。
- 较长说明使用提示块。
- 警告类提示使用浅黄色，不使用大面积红色。
- 字数计数器应位于文本域右下角。

---

### 表单布局样式规范

#### 字段对齐

- 左侧标签宽度统一。
- 控件左边缘对齐。
- 必填星号贴近标签文字。
- 帮助图标放在标签右侧。
- 同组字段垂直间距一致。

#### 表单宽度

- 单栏表单控件建议宽度 360–480px。
- 描述文本域可扩展到 480–640px。
- 抽屉表单控件可根据抽屉宽度扩展。
- 条件构建器中的控件宽度应按字段类型区分。
- 多列参数配置中，输入框不宜过宽。

#### 分组样式

- 分组标题可使用主色竖线。
- 折叠分组使用箭头。
- 分组之间用浅灰分割线或留白区隔。
- 当前分组展开时内容白底显示。
- 禁用分组使用灰色开关或浅灰状态。

#### 底部操作栏

- 独立表单可使用页面底部操作栏。
- 抽屉表单必须使用固定底部操作栏。
- 步骤流程表单应保留上一步、下一步、取消。
- 支持草稿的页面可在左侧放“存为草稿”。
- 保存按钮优先使用主色。

---

### 表单交互规范

#### 校验规则

需校验：
- 必填项
- 字符长度
- IP 格式
- 端口范围
- URL 格式
- 手机号
- 邮箱
- 数值范围
- 重复值
- 文件格式
- 文件大小

错误反馈：
- 字段边框变为错误色。
- 字段下方显示错误原因。
- 保存时定位到第一个错误字段。
- 抽屉表单保存失败时不关闭抽屉。

#### 保存与离开

- 保存成功后展示成功提示。
- 保存失败后展示失败原因。
- 未保存离开页面时应提示。
- 点击取消时，如内容已修改，应确认是否放弃。
- 分步表单下一步前应校验当前步骤。

#### 动态表单

- 选项切换后可动态展示不同字段。
- 不可用字段应禁用而非直接消失，除非业务明确要求。
- 动态新增字段需支持删除。
- 动态字段为空时应保留输入入口。
- 已填写内容在切换前应提示是否保留或清空。

---

### 表单页面 AI 生图约束

- 必须先明确表单类型：独立页面表单或抽屉式表单。
- 独立页面表单需包含导航、页面标题、表单容器和底部操作。
- 抽屉式表单需包含背景遮罩、右侧抽屉、标题、关闭按钮和底部按钮。
- 表单必须体现标签、输入框、选择器、单选、多选、开关等控件的真实结构。
- 复杂表单必须有分组、tabs、步骤条或左树辅助组织。
- 分步表单必须有步骤条和底部上一步/下一步操作。
- 条件组合表单必须有关系、字段、运算符和值选择。
- 规则配置抽屉必须有规则名称、规则描述、配置方式、组件和规则内容区。
- 不要把表单页生成成纯表格列表页。
- 不要把抽屉表单生成成小弹窗，除非明确要求弹窗。
- 不要省略保存/确认/取消按钮。
- 不要让必填星号、帮助提示和错误提示混乱。
- 不要在表单页加入大量无关图表。
- 不要使用大面积装饰背景影响填写效率。

---

### 表单页面 Prompt 模板

#### 标准独立页面表单 Prompt

```text
生成一张企业级 B 端后台独立表单页面高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、页面标题和白色表单容器。表单顶部有标签页，当前标签使用科技绿高亮。表单采用单栏左标签右控件布局，包含必填输入框、下拉选择器、时间选择器、单选组、多选组、IPv4输入框、数字输入框、文件上传按钮、开关和备注文本域，字段下方有帮助说明和上传格式提示。底部有绿色保存按钮。整体为浅灰背景、白色内容区、控件对齐整齐、企业级后台风格。
```

#### 左树右表单配置页 Prompt

```text
生成一张企业级 B 端后台左树右表单配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、页面标题和主体双栏布局。主体左侧为终端分组树，包含搜索框、新增按钮、展开收起箭头、节点名称和数量，当前节点使用浅绿色背景高亮。右侧为配置表单区，顶部有标签页，内容包含标题、说明文字、logo预览、输入框、按钮、版本选择卡片和功能菜单卡片。整体布局清晰，主色为科技绿，适合复杂配置管理。
```

#### 条件组合查询表单 Prompt

```text
生成一张企业级 B 端后台高级条件组合查询表单页面高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧导航、面包屑和白色表单容器。表单中包含基础类型单选组、多个条件组合卡片，每个卡片标题为操作系统，内部包含关系选择器、字段选择器、运算符选择器和值选择器，并提供绿色加号按钮新增条件。下方包含多选字段区和查询按钮。整体简洁、低饱和、控件排列整齐，体现复杂筛选表达式构建。
```

#### 分步式策略配置页 Prompt

```text
生成一张企业级 B 端后台分步式策略配置表单页面高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧导航、返回标题和顶部三步步骤条，第一步已完成，第二步为当前步骤，第三步未完成。主体左侧为策略列表卡片，展示策略名称、完成数/总数和已配置/未配置状态。右侧为当前步骤配置内容，包含多个折叠分组、开关、复选框、输入框、浅黄色警告提示条和说明文本。底部固定操作栏包含存为草稿、信息提示、上一步、下一步和取消按钮。整体白底浅灰背景，科技绿主色。
```

#### 抽屉式标准表单 Prompt

```text
生成一张企业级 B 端后台抽屉式编辑表单高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景页面被灰色遮罩弱化，右侧打开白色抽屉。抽屉顶部显示编辑节点标题和关闭按钮。表单采用单栏布局，字段包括节点名称、节点地址、访问令牌、描述、节点联系人、联系人电话，节点名称和节点地址带红色必填星号，访问令牌为只读输入框并在右侧提供复制按钮，描述为多行文本域。抽屉底部固定操作栏包含取消和保存按钮。整体简洁、对齐整齐、企业级配置风格。
```

#### 添加规则抽屉表单 Prompt

```text
生成一张企业级 B 端后台添加规则抽屉表单高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景页面为灰色遮罩，右侧打开宽抽屉。抽屉标题为添加规则，右上角有关闭按钮。表单包含规则名称、规则描述、配置方式单选项 JSON/参数配置、组件单选项 IVI/T-BOX。下方规则内容区使用浅灰底卡片，包含探针下拉选择器、采集周期、告警阈值等必填输入项。若组件为 T-BOX，则展示 IP 白名单、端口白名单、域名解析白名单三组动态输入，并提供新增和删除图标。底部固定确认和取消按钮。整体结构清晰、参数分组明确。
```

---

### 表单页面 Negative Prompt

```text
不要把表单页面做成 Dashboard，不要加入无关图表，不要使用营销式大插图，不要缺少保存按钮，不要缺少取消或确认按钮，不要让标签和输入框不对齐，不要把必填星号放错位置，不要让字段间距混乱，不要把抽屉表单生成成普通页面，不要省略背景遮罩和关闭按钮，不要让条件组合控件没有关系/字段/运算符/值，不要把分步表单缺少步骤条，不要让复杂表单没有分组。
```

### 表单页面生成验收标准

| 检查项 | 标准 |
|---|---|
| 类型明确 | 能区分独立页面表单和抽屉式表单 |
| 页面骨架 | 导航、标题、表单容器、操作区完整 |
| 字段结构 | 标签、控件、必填、帮助文案对齐清晰 |
| 控件完整 | 输入框、选择器、单选、多选、开关、上传等使用合理 |
| 分组清晰 | 复杂表单有分组、tabs、步骤条或左树 |
| 抽屉规范 | 遮罩、标题、关闭按钮、底部按钮完整 |
| 步骤流程 | 步骤条、当前步骤、底部上一步/下一步完整 |
| 条件配置 | 关系、字段、运算符、值、新增条件完整 |
| 校验表达 | 必填、错误、禁用、只读等状态可识别 |
| 生图质量 | 文字基本可读，控件不变形，布局不混乱 |

---
