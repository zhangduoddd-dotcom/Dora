## 文件清单

| 文件                            | 用途                                   |
| ----------------------------- | ------------------------------------ |
| 01\_全局基础规范.md                 | 全局风格、布局、导航、文案、交付与验收                  |
| 02\_通用组件规范.md                 | 按钮、输入、表格、分页、抽屉、弹窗、标签、树、步骤条、导航菜单等组件   |
| 03\_Dashboard工作台模板.md         | Dashboard / 工作台页面模板和图表适配             |
| 04\_列表页模板.md                  | 标准列表、高级搜索列表、左筛选列表、字段配置、分页            |
| 05\_详情页模板.md                  | 抽屉详情、独立详情、时间轴、资产画像、报告型详情             |
| 06\_表单页模板.md                  | 独立表单、左树右表单、分步表单、条件组合表单、抽屉表单          |
| 07\_用户管理模板.md                 | 用户列表、组织联动、用户详情、编辑用户、用户组管理            |
| 08\_系统设置模板.md                 | 基础设置、数据导出、数据清理、CDN 设置、系统工具           |
| 09\_系统通用配置模板.md               | 组织、任务、告警、日志、邀请、备份、应用、网络、检测、多级配置      |
| 10\_AI生图强约束与NegativePrompt.md | AI 生图调用方法、页面短约束、Negative Prompt、失败修正 |

## 调用建议

每次生成单页时只使用：

```text
01_全局基础规范.md
+ 当前页面模板文件
+ 10_AI生图强约束与NegativePrompt.md
+ 当前页面业务内容
```

不要整套文件一次性全部投喂给 AI。

## 本次排查修订内容

- 删除多余历史版本文件，避免同名规范冲突。
- 删除 `11_完整版母库.md`，避免与拆分文件重复。
- 将 `02_通用组件规范.md` 中页面级的列表页和 Dashboard 适配内容归入对应页面模板。
- 清理各文件标题中的旧章节编号，避免出现 `6 / 8A / 25` 等跨文件编号。
- 删除 `01_全局基础规范.md` 中重复的 H1 标题。
- 压缩 `10_AI生图强约束与NegativePrompt.md`，保留可执行的调用矩阵、页面短约束和失败修正。

***

# 全局基础规范

> 适用对象：企业级 SaaS / B 端管理后台 / AI 原生多智能体协作平台\
> 文档目标：形成一套可直接用于 Figma 设计、AI 生图提示词、前端组件开发与设计验收的 B 端界面规范。

***

## 规范体系总览

这套规范不是单纯的 UI 样式集合，而是一套完整的 B 端界面设计标准。它的核心结构可以理解为：

```text
设计原则
  ↓
全局样式 Token
  ↓
页面架构与响应式布局
  ↓
导航与页面组织
  ↓
基础操作组件
  ↓
数据录入组件
  ↓
数据展示组件
  ↓
反馈与异常处理
  ↓
业务场景模板与验收标准
```

### 规范核心价值

| 层级   | 作用                         | 对界面落地的意义                 |
| ---- | -------------------------- | ------------------------ |
| 全局样式 | 规定颜色、字体、栅格、投影、布局           | 保证不同页面看起来属于同一产品体系        |
| 页面架构 | 规定顶部、侧边栏、内容区、弹层等关系         | 保证页面结构稳定，不因业务复杂而混乱       |
| 导航组件 | 规定顶部导航、侧边导航、面包屑、标签页、步骤条    | 帮助用户理解“我在哪里、能去哪里、下一步是什么” |
| 数据录入 | 规定表单、输入框、选择器、日期时间、上传等      | 降低输入成本，减少错误              |
| 数据展示 | 规定表格、卡片、标签、统计数值、描述列表等      | 提高复杂信息的阅读效率              |
| 反馈机制 | 规定全局提示、通知、警告、抽屉、弹窗、空状态、异常页 | 让系统状态、操作结果和风险后果被用户及时理解   |
| 业务范式 | 规定删除、创建、更新、离开等敏感操作反馈模板     | 保证高风险操作的提示一致、清晰、可追责      |

### 本规范的使用方式

本规范可以用于四类场景：

1. **设计前**：作为页面架构、组件选型、信息层级的依据。
2. **设计中**：作为 Figma 组件、页面布局、交互状态的统一标准。
3. **设计后**：作为设计走查、前端验收、体验一致性检查表。
4. **AI 生成界面时**：作为提示词中的结构约束和视觉约束，避免生成结果“好看但不可用”。

***

## 设计原则

### 一致性

同一产品内的页面结构、组件样式、交互反馈、文案表达应保持一致。

**执行要求：**

- 同类页面使用同一布局骨架。
- 同类操作使用同一按钮层级。
- 同类状态使用同一颜色和提示方式。
- 同类表单使用同一标签、校验和帮助信息规则。
- 同类风险操作使用同一弹窗反馈模板。

### 可适配

页面应适配主流屏幕分辨率和不同信息密度场景。

**执行要求：**

- 设计输出默认以 `1440px` 画板为基准。
- 适配主流分辨率：`1920 / 1440 / 1366 / 1280`。
- 内容区域使用响应式布局。
- 栅格、卡片、表格和图表应根据视窗宽度自适应。
- 侧边栏在小屏下应支持自动收起或图标态。

### 可控性

界面需要清晰、直接、可预期，减少用户的记忆负担。

**执行要求：**

- 页面主任务必须明确。
- 一个操作区域只保留一个最重要的主按钮。
- 高风险操作必须解释后果。
- 复杂任务应拆分为步骤。
- 系统状态必须及时反馈。
- 异常、空状态、加载态不能留白或让用户猜测。

### 业务安全性

对于安全、审批、删除、断开、禁用、覆盖、重置等敏感操作，必须强化确认机制。

**执行要求：**

- 弹窗标题必须包含动作和对象。
- 描述必须说明操作后的结果。
- 高风险操作必须说明是否可恢复。
- 不可逆操作不得只用 Toast 提示。
- 操作前、操作中、操作后都应有清晰反馈。

***

## 全局样式规范

### 布局基准

#### 布局定义

空间布局是体系化设计的起点。页面整体布局是产品最外层的框架结构，一般包含导航、侧边栏、面包屑、内容区、弹层等。布局需要从动态、体系化的角度处理界面空间秩序，让用户在复杂 B 端任务中能够快速识别页面结构和操作路径。

布局方式决定界面元素之间的关系、韵律和节奏。对于企业级 SaaS / B 端后台 / AI 原生协作平台，布局不是装饰问题，而是信息组织、任务优先级和操作效率的问题。

#### 布局设计原则

| 原则  | 解释                               | 落地要求                                            |
| --- | -------------------------------- | ----------------------------------------------- |
| 一致性 | 确保产品布局在结构和视觉上保持一致，为用户提供稳定、舒适的使用感 | 同类页面使用同一布局骨架；同类模块保持相同间距、卡片结构和操作区位置              |
| 可适配 | 布局应根据不同设计环境、浏览器宽度和屏幕分辨率进行响应式适配   | 支持 1920 / 1440 / 1366 / 1280 等主流分辨率；内容区随宽度变化自适应 |
| 可控性 | 页面需要简单、直白、可识别，减少用户记忆负担           | 主任务清晰；复杂功能可扩展；页面结构能随业务复杂度延展                     |

#### 布局执行原则

| 项目     | 标准                          |
| ------ | --------------------------- |
| 默认设计画板 | 1440px                      |
| 主流适配宽度 | 1920px、1440px、1366px、1280px |
| 最小原子单位 | 4px                         |
| 间距计算方式 | `间距 = x × 4px`              |
| 栅格体系   | 12 栅格                       |
| 常见布局方式 | T 字形布局、左右布局、上下布局            |
| 内容区    | 响应式区域                       |
| 设计输出原则 | 统一画板尺寸输出布局，适配方案遵循一致的响应式布局原则 |

#### 页面四层框架

根据用户操作行为进行层级区分，页面框架可分为四层。设计时应从下到上建立信息层级，避免所有内容同时争抢视觉注意力。

| 层级    | 内容                     | 说明                  |
| ----- | ---------------------- | ------------------- |
| 背景区域  | 页面底色、背景纹理、全局背景         | 提供环境氛围，不承担主要信息表达    |
| 内容层   | 表格、列表、卡片、图表、表单、工作区     | 用户完成任务的主要区域         |
| 全局控制层 | 顶部导航、侧边导航、全局搜索、通知、账户入口 | 负责全局跳转、系统级操作和跨页面控制  |
| 内容弹层  | 弹窗、抽屉、气泡确认框、浮层详情       | 承载临时任务、确认、补充说明和局部编辑 |

#### 响应式布局定义

响应式布局是指在不同分辨率下，让内容以最优展示形式呈现给用户，从而提升使用体验。响应式布局系统用于调整界面结构，以适应不同屏幕分辨率和浏览器视窗大小。

**执行要求：**

- 使用基于百分比的计算，而不是完全依赖固定像素。
- 图像、卡片、图表和内容区域应可缩放。
- 通过断点控制布局切换。
- 容器可以固定，也可以根据断点流体变化；最终以设计输出要求为准。
- 当宽度小于断点临界值时，卡片宽度应基于可用宽度自适应，同时必须考虑卡片最小宽度。

#### 响应式断点与侧边栏策略

| 视窗宽度           | 栅格列数 | Gutter | Margin | 侧边栏收起动作     | 展开动作   | 展开方式    |
| -------------- | ---: | -----: | -----: | ----------- | ------ | ------- |
| `< 768px`      |    4 |   16px |   24px | 自动完全收起      | 点击按钮展开 | Overlay |
| `768px–991px`  |    8 |   16px |   24px | 自动收起，展示图标   | 点击按钮展开 | Push    |
| `992px–1199px` |   12 |   24px |   24px | 点击按钮收起，展示图标 | 点击按钮展开 | Push    |
| `≥ 1200px`     |   12 |   24px |   24px | 点击按钮收起，展示图标 | 点击按钮展开 | Push    |

**补充规则：**

- 侧边栏区域可设置为响应式布局。
- 当浏览器宽度小于配置断点值时，侧边导航自动从展开态变为收起态。
- 默认侧边栏响应断点为 `992px`。
- 页面最小宽度定义为 `768px`。
- 收起态侧边栏宽度定义为 `56px`。
- 当内容区域小于 `768px` 时，页面不再继续缩小，浏览器应出现横向滚动条。

#### 主流分辨率适配逻辑

| 分辨率    | 适配逻辑                  |
| ------ | --------------------- |
| 1920px | 左侧导航栏固定，右侧内容区域基于内容自适应 |
| 1440px | 左侧导航栏固定，右侧内容区域基于内容自适应 |
| 1366px | 左侧导航栏固定，右侧内容区域基于内容自适应 |
| 1280px | 左侧导航栏固定，右侧内容区域基于内容自适应 |

***

### 栅格系统

#### 基础规则

| 项目     | 规范                                                            |
| ------ | ------------------------------------------------------------- |
| 栅格体系   | 12 栅格                                                         |
| 主要适用   | Dashboard、概览页、统计页、卡片流、分析页                                     |
| Column | 弹性布局                                                          |
| Gutter | 16px 或 24px，常规桌面场景优先 24px                                     |
| Margin | 常规桌面场景优先 24px                                                 |
| 盒子模型   | Border & Corner、Padding、Margin、Background、Shadow、Content、Size |

#### 卡片栅格划分

| 栅格占比            | 页面呈现      | 适用场景                  |
| --------------- | --------- | --------------------- |
| 1               | 一行 1 个大卡片 | 大图表、核心工作区、复杂表单        |
| 1/2 + 1/2       | 一行 2 个卡片  | 左右并列分析、双指标对比          |
| 1/3 + 1/3 + 1/3 | 一行 3 个卡片  | 常规 Dashboard 指标区      |
| 1/4 × 4         | 一行 4 个卡片  | 小型统计卡片、状态概览           |
| 1/5 × 5         | 一行 5 个卡片  | 较高密度指标展示，需谨慎控制文本量     |
| 1/6 × 6         | 一行 6 个卡片  | 极高密度状态入口，不建议承载复杂图表    |
| 1/12 × 12       | 细粒度栅格     | 仅用于布局对齐，不建议直接作为业务卡片宽度 |

#### 盒子模型

每一个卡片在前端实现时都应被视为一个盒子。盒子由以下属性构成：

```text
Box = Border & Corner + Padding + Margin + Background + Shadow + Content + Size
```

| 属性              | 含义    | 推荐标准                             |
| --------------- | ----- | -------------------------------- |
| Border & Corner | 边框与圆角 | 保持同类卡片统一                         |
| Padding         | 内边距   | Dashboard 卡片常规使用 24px；高密度列表可适当压缩 |
| Margin          | 外边距   | 模块之间依据 12px / 16px / 24px 控制     |
| Background      | 背景    | 白色或浅灰卡片背景，避免破坏数据阅读               |
| Shadow          | 投影    | 轻投影，仅用于层级区分                      |
| Content         | 内容    | 文本、图表、表格、操作区                     |
| Size            | 尺寸    | 依据栅格和断点自适应                       |

#### 栅格响应策略

| 断点        | 响应区间           | 推荐布局                             |
| --------- | -------------- | -------------------------------- |
| 1200px    | `> 1200px`     | 布局方式一：多列布局，可承载 A / B / C 等多个并列模块 |
| 992px     | `992px–1199px` | 布局方式二：减少横向列数，优先保证主要模块完整          |
| 768px     | `768px–991px`  | 布局方式三：进一步减少列数，卡片逐步纵向堆叠           |
| `< 768px` | 小于最小页面宽度       | 不再压缩页面，出现横向滚动条                   |

**强制要求：**

- 卡片宽度不能无限缩小。
- 图表类卡片必须定义最小宽度。
- 信息展示完整性优先于“强行适配一屏”。
- 页面宽度不足时，应通过换行、堆叠、横向滚动处理，而不是压缩到不可读。

#### 间距建议

| 场景             | 建议                        |
| -------------- | ------------------------- |
| 页面边距           | 24px                      |
| 常规卡片内边距        | 24px                      |
| Dashboard 卡片之间 | 12px / 16px / 24px，根据密度选择 |
| 列表页卡片间距        | 优先 12px，部分卡片场景可使用 16px    |
| 标题与内容          | 8px / 12px / 16px         |
| 操作按钮之间         | 列表页统一 8px；非高密度页面可使用 12px  |
| 模块分组之间         | 16px / 24px               |

### 字体规范

#### 字体家族

优先使用系统默认界面字体，保证跨平台可读性。

```css
font-family:
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "Helvetica Neue",
  "PingFang SC",
  "Hiragino Sans GB",
  "Microsoft YaHei UI",
  "Microsoft YaHei",
  Arial,
  sans-serif;
```

#### 字阶与行高

| 类型     |   字号 |   行高 |               字重 | 使用场景         |
| ------ | ---: | ---: | ---------------: | ------------ |
| 超大标题   | 48px | 54px |             Bold | 大屏标题、品牌主标题   |
| 一级标题   | 28px | 36px |             Bold | 页面主标题        |
| 二级标题   | 18px | 26px |             Bold | 模块标题、卡片标题    |
| 头部/大按钮 | 16px | 28px | Regular / Medium | 顶部导航、重要按钮    |
| 正文     | 14px | 22px |          Regular | 表格、表单、普通文本   |
| 辅助文本   | 12px | 20px |          Regular | 提示、说明、标签、元信息 |

#### 字重

| 字重      |   CSS 值 | 使用场景         |
| ------- | ------: | ------------ |
| Regular |     400 | 正文、说明、表单内容   |
| Medium  |     500 | 导航选中、按钮、重点文字 |
| Bold    | 600/700 | 标题、强调信息      |

***

### 颜色规范

#### 主色 Primary：科技绿体系

| Token | 色值        | 语义 / 使用场景                                   |
| ----- | --------- | ------------------------------------------- |
| `p1`  | `#D9F4EE` | 部分组件选中态填充色、浅色图形化配色                          |
| `p2`  | `#B9F7E2` | 图形化配色、浅绿色辅助色                                |
| `p3`  | `#71EDC2` | 图形化配色、视觉辅助色                                 |
| `p4`  | `#4AD3A4` | 图形化配色、进阶辅助色                                 |
| `p5`  | `#1DB887` | 可交互控件 hover 填充色、图形化配色                       |
| `p6`  | `#00AB7A` | **主色 / 主按钮 normal / 可交互控件默认填充色 / 文本 hover** |
| `p7`  | `#039972` | 可交互控件 click / active；文本 normal              |
| `p8`  | `#008563` | 文本 click / 深层交互色                            |
| `p9`  | `#006853` | 深色图形化配色                                     |
| `p10` | `#013C32` | 最深图形化配色，不建议大面积背景使用                          |

#### 主色使用规则

- 普通主按钮：默认使用 `p6`，hover 使用 `p5`，active/click 使用 `p7`。
- 选中态浅底：优先使用 `p1`。
- 链接文字：常规可使用 `p7`，hover 可使用 `p6`，click 可使用 `p8`。
- 图表中可使用 `p1-p10` 形成同色系递进，但不要让深色 `p9/p10` 大面积压住页面。

#### AI 渐变色：只用于 AI 能力入口

| Token   | CSS 值                                                                                                                                   | 使用场景                             |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| `AI 默认` | `linear-gradient(307deg, #004DFF 0%, #1093FF 65%, #1AFFAF 100%)`                                                                        | AI 主按钮默认态，如“智能生成”“AI 分析”“智能推荐”   |
| `AI 悬停` | `linear-gradient(0deg, rgba(255,255,255,0.20), rgba(255,255,255,0.20)), linear-gradient(126deg, #1AFFAF 0%, #1093FF 37%, #004DFF 100%)` | AI 按钮 hover 态，叠加 20% 白色蒙层        |
| `AI 按下` | `linear-gradient(0deg, rgba(0,0,0,0.20), rgba(0,0,0,0.20)), linear-gradient(309deg, #004DFF 0%, #1093FF 54%, #1AFFAF 100%)`             | AI 按钮 active/click 态，叠加 20% 黑色蒙层 |

#### AI 渐变使用边界

- 只用于 AI 能力入口、AI 生成、AI 分析、智能推荐、自动补全等“智能能力”操作。
- 不用于普通提交、保存、删除、筛选、导出等常规业务按钮。
- 同一页面 AI 渐变按钮数量应克制；如果所有按钮都用渐变，AI 入口会失去识别度。
- AI 按钮文字建议使用白色，图标使用白色或浅色线性图标。

#### 中性色 Neutral

| Token     | 色值        | 语义 / 使用场景               |
| --------- | --------- | ----------------------- |
| `gray-1`  | `#FFFFFF` | 白色容器、卡片背景、弹窗内容区         |
| `gray-2`  | `#F8F9FA` | 页面浅背景、表格表头背景、弱分区背景      |
| `gray-3`  | `#F2F3F5` | 白背景 hover、浅灰页面背景        |
| `gray-4`  | `#E8EAED` | 分割线、表格线、弱边界             |
| `gray-5`  | `#DFE1E6` | 边框、输入框边界、次要按钮边框         |
| `gray-6`  | `#AEB2B8` | 禁用态、placeholder、不可交互文本  |
| `gray-7`  | `#8A9099` | 次要文字、辅助说明、默认图标色         |
| `gray-8`  | `#525966` | 正文、常规信息文字               |
| `gray-9`  | `#242933` | 标题、主要文字、高强调文本           |
| `gray-10` | `#000000` | 极深色，仅用于特殊强对比场景，不建议大面积使用 |

#### 中性色使用规则

- 页面背景优先使用 `gray-2` 或 `gray-3`，容器背景使用 `gray-1`。
- 正文使用 `gray-8`，标题使用 `gray-9`。
- 辅助文本、说明、时间、来源信息使用 `gray-7`。
- 禁用态和 placeholder 使用 `gray-6`。
- 分割线用 `gray-4`，边框用 `gray-5`，不要混用。

#### 功能色 Semantic Colors

| 类型        | 浅背景                    | 辅助色 / 边框                           | 主色                     | 深色                                  | 使用场景             |
| --------- | ---------------------- | ---------------------------------- | ---------------------- | ----------------------------------- | ---------------- |
| 成功 / 完成   | `Green-1 #EFF8EE`      | `Green-3 #BCE0B4`                  | `Green-6 #52C41A`      | `Green-7 #389E0D`                   | 成功、完成、安全、通过      |
| 警告 / 待处理  | `Orange-1 #FFF8E6`     | `Orange-4 #FFC054`                 | `Orange-6 #F58A02`     | `Orange-7 #CF6E00`                  | 警告、处理中、待审核、需关注   |
| 错误 / 失败   | `Red-1 #FFE8E6`        | `Red-2 #FFBEB8` / `Red-3 #FF948F`  | `Red-6 #F0131E`        | `Red-7 #C90616`                     | 失败、错误、危险操作       |
| 信息 / 链接   | `Blue-1 #E6F7FF`       | `Blue-3 #91D5FF` / `Blue4 #69C0FF` | `Blue-6 #1890FF`       | `Blue-7 #096DD9` / `Blue-8 #0050B3` | 信息提示、链接、说明入口     |
| 低强度提示     | `Yellow-1 #FFFFE6`     | `Yellow-5 #EBD426`                 | `Yellow-6 #DFBC00`     | `Yellow-7 #B89600`                  | 低风险提醒、轻提示、弱警示    |
| 高风险提醒     | `Orange red-1 #FFF3E6` | `Orange red-3 #FFBC85`             | `Orange red-6 #F05A0A` | `Orange red-7 #C94000`              | 高危、强警告、需优先处理     |
| 致命 / 极高风险 | `Deep red-1 #FCE7E3`   | `Deep red-2 #F0A399`               | `Deep red-6 #BC0004`   | `Deep red-7 #960008`                | 致命风险、严重阻断、强破坏性操作 |

#### 安全等级色映射

| 安全等级 | 推荐 Token 组合                                                 | 使用说明               |
| ---- | ----------------------------------------------------------- | ------------------ |
| 致命   | `Deep red-1 / Deep red-2 / Deep red-6 / Deep red-7`         | 安全产品中的最高风险等级，必须强提示 |
| 严重   | `Red-1 / Red-3 / Red-6 / Red-7`                             | 严重错误、重大风险、不可忽略状态   |
| 高危   | `Orange red-1 / Orange red-3 / Orange red-6 / Orange red-7` | 高优先级风险、紧急待处理       |
| 中危   | `Orange-1 / Orange-4 / Orange-6 / Orange-7`                 | 中等风险、待处理、警告        |
| 低危   | `Yellow-1 / Yellow-5 / Yellow-6 / Yellow-7`                 | 低风险、弱警示            |
| 安全   | `Green-1 / Green-3 / Green-6 / Green-7`                     | 安全、通过、正常           |
| 未知   | `gray-4 / gray-6 / gray-8`                                  | 未检测、未分类、不可判断       |

#### 状态描边 / Focus Ring

| Token        | CSS 值                     | 使用场景                |
| ------------ | ------------------------- | ------------------- |
| `Default 默认` | `0px 0px 0px 2px #D9F4EE` | 默认聚焦、选中、可编辑控件 focus |
| `Error 错误`   | `0px 0px 0px 2px #FFE8E6` | 表单错误、校验失败、危险输入聚焦    |
| `Success 成功` | `0px 0px 0px 2px #EFF8EE` | 成功校验、通过状态聚焦         |
| `Warning 警告` | `0px 0px 0px 2px #FFF8E6` | 警告状态、待确认输入聚焦        |

#### 描边使用规则

- Focus ring 不应改变组件本体尺寸，应使用 `box-shadow` 外描边方式实现。
- 表单控件的错误态优先使用 `Error 错误` 描边，同时配合错误文案。
- 选中态、键盘聚焦态必须可见，不能只依赖颜色深浅变化。
- 不建议把 focus ring 做成过重发光，否则会破坏 B 端界面的稳定感。

#### Token 命名与重复变量处理

| 情况                       | 处理规则                       |
| ------------------------ | -------------------------- |
| `Blue-7` 重复出现，色值一致       | 保留一个标准定义即可                 |
| `gray-6/7/8/9` 重复出现，色值一致 | 保留第一次定义即可                  |
| `P6` 与 `p6` 一致           | 前端交付统一使用小写 `p6`            |
| `P7` 与 `p7` 一致           | 前端交付统一使用小写 `p7`            |
| 中文变量名如 `AI 默认`、`投影-默认`   | 设计文档保留中文名；代码落地建议增加英文 alias |

***

### 投影规范

> 投影不是装饰，而是表达层级关系：卡片浮起用一级投影，抽屉/弹窗/遮罩浮层用二级投影，超级框 hover 使用专门阴影。

#### 一级投影：卡片与模块浮起

| Token   | CSS 值                                   | 使用场景                     |
| ------- | --------------------------------------- | ------------------------ |
| `一级-向上` | `0px -4px 16px 0px rgba(36,41,51,0.05)` | 顶部浮起卡片、向上投影模块            |
| `一级-向下` | `0px 4px 16px 0px rgba(36,41,51,0.05)`  | 常规卡片、Dashboard 模块、白底容器浮起 |
| `一级-向右` | `4px 0px 16px 0px rgba(36,41,51,0.05)`  | 左侧卡片、侧栏轻量浮起              |
| `一级-向左` | `-4px 0px 16px 0px rgba(36,41,51,0.05)` | 右侧卡片、右栏轻量浮起              |

#### 二级投影：抽屉、弹窗、遮罩浮层

| Token   | CSS 值                                   | 使用场景            |
| ------- | --------------------------------------- | --------------- |
| `二级-向上` | `0px -8px 80px 0px rgba(36,41,51,0.08)` | 底部抽屉、底部浮层       |
| `二级-向下` | `0px 8px 80px 0px rgba(36,41,51,0.08)`  | 弹窗、Popover、大型浮层 |
| `二级-向右` | `6px 0px 80px 0px rgba(36,41,51,0.08)`  | 左侧抽屉、左侧浮层       |
| `二级-向左` | `-8px 0px 80px 0px rgba(36,41,51,0.08)` | 右侧抽屉、右侧浮层       |

#### 通用投影与特殊 hover 投影

| Token        | CSS 值                                                                                                     | 使用场景                         |
| ------------ | --------------------------------------------------------------------------------------------------------- | ---------------------------- |
| `投影-默认`      | `0px 3px 6px -4px rgba(0,0,0,0.12), 0px 6px 16px 0px rgba(0,0,0,0.08), 0px 9px 28px 8px rgba(0,0,0,0.04)` | 标准浮层、卡片、下拉面板默认阴影             |
| `投影-悬停`      | `0px 1px 2px -2px rgba(0,0,0,0.16), 0px 3px 6px 0px rgba(0,0,0,0.12), 0px 5px 12px 4px rgba(0,0,0,0.08)`  | 卡片 hover、可点击模块 hover         |
| `超级框hover阴影` | `0px 4px 12px 0px rgba(183,179,255,0.50)`                                                                 | AI 超级输入框、智能搜索框、AI 主输入区 hover |

#### 投影使用原则

- 卡片浮起优先使用一级投影，不要直接套用二级投影。
- 弹窗、抽屉、遮罩浮层必须明显高于普通卡片，使用二级投影。
- Dashboard 卡片可以使用 `一级-向下`，hover 时可切换为 `投影-悬停`。
- AI 超级输入框、智能搜索框这类核心入口可以使用 `超级框hover阴影`，但不要用于普通输入框。
- 表格、表单等高密度内容区域不应滥用强投影，否则会降低阅读效率。

***

## 页面架构规范

### 标准页面结构

推荐结构：

```text
顶部导航 / 顶栏
  ├── Logo / 产品名
  ├── 全局搜索
  ├── 通知
  ├── 用户信息
  └── 全局操作

左侧导航 / 侧边栏
  ├── 一级导航
  ├── 二级导航
  └── 折叠/展开控制

主内容区
  ├── 面包屑
  ├── 页头
  ├── 页面级操作
  ├── 筛选 / 查询区域
  ├── 数据展示区域
  └── 分页 / 批量操作

弹层区
  ├── 弹窗
  ├── 抽屉
  ├── 气泡卡片
  └── 全局提示
```

### 页面类型

| 页面类型          | 主要组件                | 适用场景                 |
| ------------- | ------------------- | -------------------- |
| Dashboard 概览页 | 统计数值、卡片、图表、表格、时间轴   | 总览、态势、运营看板           |
| 列表页           | 筛选区、表格、分页、批量操作、标签   | 数据管理、任务管理、专家管理       |
| 详情页           | 页头、描述列表、标签、时间轴、操作记录 | 查看对象详情、审计记录          |
| 表单页           | 表单、输入框、选择器、日期时间、上传  | 创建、编辑、配置             |
| 分步流程页         | 步骤条、表单、确认页、结果页      | 创建项目、发布智能体、审批流程      |
| 配置页           | 表单、开关、树选择、穿梭框、权限树   | 系统设置、权限配置            |
| 异常页           | 插图、标题、说明、建议操作       | 无权限、404、500、加载失败、维护中 |

### 信息层级

页面信息从高到低建议分为：

1. 页面标题与当前任务。
2. 页面级主操作。
3. 筛选条件与搜索。
4. 关键数据与状态。
5. 明细数据。
6. 辅助说明与元信息。
7. 次级操作与更多操作。

***

## 导航规范

### 顶部导航

#### 使用场景

- 提供全局级类目和功能。
- 放置产品名称、全局搜索、通知、主题切换、用户账户。
- 适合多模块平台型产品。

#### 设计要求

- 顶部导航应稳定存在，不随页面内容变化频繁改变。
- 全局搜索、通知、头像等位置应固定。
- 顶部导航不应承载过多业务操作，业务操作应放入页面内。

### 侧边导航

#### 使用场景

- 展示产品核心功能模块。
- 承载多级信息架构。
- 适合企业后台、SaaS 平台、复杂系统。

#### 设计要求

- 支持展开和收起。
- 收起态保留图标。
- 当前模块必须有清晰选中态。
- 导航层级不宜过深，超过三级需要重新梳理信息架构。
- 图标必须与模块语义一致，不能只为装饰。

### 面包屑

#### 使用场景

- 系统层级超过两级。
- 用户需要知道“当前在哪里”。
- 用户需要快速返回上级页面。

#### 规则

- 当前层级不需要可点击。
- 上级层级可点击返回。
- 层级名称应与页面标题或导航名称一致。
- 不要用面包屑替代主导航。

### 标签页

#### 使用场景

- 对平级内容进行分类收纳。
- 用于主导航层级之下的页面细分模块。
- 适合三级、四级分类切换。

#### 规则

- 标签页数量建议不超过 5 个。
- 当前选中态必须明显。
- 标签之间应为平级关系，不要混入父子关系。
- 如果内容差异过大，应使用导航或独立页面，而不是标签页。

### 步骤条

#### 使用场景

- 任务复杂且存在前后顺序。
- 用户需要知道当前进度。
- 创建、发布、审批、配置向导等场景。

#### 规则

- 建议多于 2 步、少于 5 步。
- 只有两步能完成的流程不建议使用步骤条。
- 每一步的名称应是结果导向，例如“填写信息”“配置权限”“确认发布”。
- 每一步完成后应保留可回看能力。

### 下拉菜单

#### 使用场景

- 页面操作较多，需要收纳低频操作。
- 表格行操作超过 3–5 个。
- 顶部导航或按钮组合中需要扩展操作。

#### 规则

- 菜单项保持单行显示。
- 面板宽度建议等于或大于触发区域。
- 危险操作需要视觉区分或二次确认。
- 不要把高频主操作藏进下拉菜单。

### 分页

#### 使用场景

- 数据量较大，一次加载影响性能。
- 用户需要切换页码浏览数据。

#### 规则

- 基础页码最多默认展示 5 页。
- 表格无数据时不显示分页。
- 分页应放在数据列表底部右侧或底部区域。
- 支持每页条数切换时，应保持与数据密度匹配。

***

## AI 原生多智能体平台适配规范

> 如果该规范用于 AgentTeams / 多智能体协作平台，建议在原 B 端规范上增加以下 AI 场景标准。

### AI 能力入口

| 入口类型    | 推荐组件              | 说明           |
| ------- | ----------------- | ------------ |
| 智能生成    | AI 按钮 / 主按钮       | 用于生成报告、方案、摘要 |
| 智能推荐    | AI 按钮 / 卡片推荐区     | 推荐专家、技能、模板   |
| 智能分析    | 图标按钮 / 主按钮        | 分析日志、风险、任务   |
| 自动填充    | 输入框后缀按钮           | 根据上下文补全表单    |
| AI 审核   | 表格行操作 / 抽屉        | 对内容进行风险审核    |
| AI 助手说明 | Tooltip / Popover | 解释模型能力和限制    |

### 智能体状态

| 状态  | 文案  | 颜色建议    |
| --- | --- | ------- |
| 正常  | 运行中 | 成功色     |
| 待配置 | 待配置 | 提醒色     |
| 执行中 | 执行中 | 蓝色 / 主色 |
| 异常  | 异常  | 错误色     |
| 暂停  | 已暂停 | 中性色     |
| 审批中 | 审批中 | 警告色     |
| 已下线 | 已下线 | 中性色     |

### AI 任务生命周期

```text
创建任务
  ↓
选择专家 / 智能体
  ↓
配置输入与约束
  ↓
执行中
  ↓
生成结果
  ↓
人工审核
  ↓
应用 / 发布 / 归档
```

#### 设计要求

- 任何 AI 输出都不能默认为最终结果。
- 关键结果必须提供人工审核入口。
- AI 操作记录必须保留来源、输入、输出、执行时间、操作者。
- 失败时需要说明失败原因和重试建议。
- 多智能体协作时需要展示每个智能体的贡献或状态。

### AI 结果展示

推荐结果结构：

```text
结果标题
生成时间 / 使用模型 / 来源数据
核心结论
证据或依据
风险提示
可执行操作：[复制] [编辑] [重新生成] [应用结果]
```

#### 规则

- AI 结果不能只给结论，需要给依据。
- 涉及风险判断时必须展示置信度或不确定性说明。
- 应用 AI 结果前必须允许用户编辑。

***

## 文案规范

### 基本原则

- 简洁：避免长句。
- 明确：说明用户要做什么。
- 结果导向：让用户知道操作后会发生什么。
- 风险透明：不可逆、覆盖、断开、删除必须说清楚。
- 一致：同类操作使用同类表达。

### 常用文案模板

#### 成功反馈

```text
保存成功
创建成功
提交成功
发布成功
上传成功
```

#### 失败反馈

```text
保存失败，请重试
上传失败，请检查文件格式
加载失败，请刷新重试
提交失败，请检查必填项
```

#### 空状态

```text
暂无数据
暂无任务
暂无搜索结果
暂无可用专家
当前无权限查看该内容
```

#### 确认弹窗

```text
确定删除 {对象} 吗？
删除后将无法恢复，请谨慎操作。
```

#### 离开确认

```text
确定离开当前页面吗？
离开后，未保存的内容将会丢失。
```

***

## 设计交付规范

### Figma 交付要求

| 交付项      | 要求                                          |
| -------- | ------------------------------------------- |
| 页面命名     | 使用业务模块 + 页面类型                               |
| Frame 尺寸 | 默认 1440px                                   |
| 组件命名     | 按组件类型 / 状态 / 尺寸命名                           |
| 状态完整性    | Default、Hover、Active、Disabled、Loading、Error |
| 颜色 Token | 使用统一命名，不直接写散色                               |
| 字体样式     | 使用统一 Text Style                             |
| 间距       | 遵循 4px 原子单位                                 |
| 图标       | 使用统一图标库                                     |
| 备注       | 关键交互、异常、空状态需要标注                             |

### 前端交付 Token 示例

交付时建议保留原始 CSS Token，同时增加英文 alias，方便前端在工程中稳定调用。原因是原始变量包含中文和空格，例如 `--AI 默认`、`--投影-默认`，可读性强但不一定适合所有工程构建链。

#### CSS Token

```css
:root {
  --AI 悬停: linear-gradient(0deg, rgba(255,255,255,0.20), rgba(255,255,255,0.20)),linear-gradient(126deg, #1AFFAF 0%,#1093FF 37%,#004DFF 100%);
  --AI 按下: linear-gradient(0deg, rgba(0,0,0,0.20), rgba(0,0,0,0.20)),linear-gradient(309deg, #004DFF 0%,#1093FF 54%,#1AFFAF 100%);
  --AI 默认: linear-gradient(307deg, #004DFF 0%,#1093FF 65%,#1AFFAF 100%);

  --p1: #D9F4EE;
  --p2: #B9F7E2;
  --p3: #71EDC2;
  --p4: #4AD3A4;
  --p5: #1DB887;
  --p6: #00AB7A;
  --p7: #039972;
  --p8: #008563;
  --p9: #006853;
  --p10: #013C32;

  --gray-1: #FFFFFF;
  --gray-2: #F8F9FA;
  --gray-3: #F2F3F5;
  --gray-4: #E8EAED;
  --gray-5: #DFE1E6;
  --gray-6: #AEB2B8;
  --gray-7: #8A9099;
  --gray-8: #525966;
  --gray-9: #242933;
  --gray-10: #000000;

  --Green-1: #EFF8EE;
  --Green-3: #BCE0B4;
  --Green-6: #52C41A;
  --Green-7: #389E0D;

  --Orange-1: #FFF8E6;
  --Orange-4: #FFC054;
  --Orange-6: #F58A02;
  --Orange-7: #CF6E00;

  --Red-1: #FFE8E6;
  --Red-2: #FFBEB8;
  --Red-3: #FF948F;
  --Red-5: #FC3D40;
  --Red-6: #F0131E;
  --Red-7: #C90616;

  --Blue-1: #E6F7FF;
  --Blue-3: #91D5FF;
  --Blue4: #69C0FF;
  --Blue-5: #40A9FF;
  --Blue-6: #1890FF;
  --Blue-7: #096DD9;
  --Blue-8: #0050B3;

  --Yellow-1: #FFFFE6;
  --Yellow-5: #EBD426;
  --Yellow-6: #DFBC00;
  --Yellow-7: #B89600;

  --Orange-red-1: #FFF3E6;
  --Orange-red-3: #FFBC85;
  --Orange-red-6: #F05A0A;
  --Orange-red-7: #C94000;

  --Deep-red-1: #FCE7E3;
  --Deep-red-2: #F0A399;
  --Deep-red-6: #BC0004;
  --Deep-red-7: #960008;

  --Default: 0px 0px 0px 2px #D9F4EE;
  --Error: 0px 0px 0px 2px #FFE8E6;
  --Success: 0px 0px 0px 2px #EFF8EE;
  --Warning: 0px 0px 0px 2px #FFF8E6;

  --shadow-level-1-up: 0px -4px 16px 0px rgba(36,41,51,0.05);
  --shadow-level-1-down: 0px 4px 16px 0px rgba(36,41,51,0.05);
  --shadow-level-1-right: 4px 0px 16px 0px rgba(36,41,51,0.05);
  --shadow-level-1-left: -4px 0px 16px 0px rgba(36,41,51,0.05);

  --shadow-level-2-up: 0px -8px 80px 0px rgba(36,41,51,0.08);
  --shadow-level-2-down: 0px 8px 80px 0px rgba(36,41,51,0.08);
  --shadow-level-2-right: 6px 0px 80px 0px rgba(36,41,51,0.08);
  --shadow-level-2-left: -8px 0px 80px 0px rgba(36,41,51,0.08);

  --shadow-default: 0px 3px 6px -4px rgba(0,0,0,0.12),0px 6px 16px 0px rgba(0,0,0,0.08),0px 9px 28px 8px rgba(0,0,0,0.04);
  --shadow-hover: 0px 1px 2px -2px rgba(0,0,0,0.16),0px 3px 6px 0px rgba(0,0,0,0.12),0px 5px 12px 4px rgba(0,0,0,0.08);
  --shadow-superbox-hover: 0px 4px 12px 0px rgba(183,179,255,0.50);
}
```

#### 推荐英文 Alias：便于工程调用

```css
:root {
  /* Brand */
  --color-primary-bg: var(--p1);
  --color-primary-hover: var(--p5);
  --color-primary: var(--p6);
  --color-primary-active: var(--p7);
  --color-primary-text-active: var(--p8);

  /* AI */
  --color-ai-default: var(--AI 默认);
  --color-ai-hover: var(--AI 悬停);
  --color-ai-active: var(--AI 按下);

  /* Text */
  --color-text-title: var(--gray-9);
  --color-text-primary: var(--gray-8);
  --color-text-secondary: var(--gray-7);
  --color-text-disabled: var(--gray-6);
  --color-text-placeholder: var(--gray-6);

  /* Background and border */
  --color-bg-container: var(--gray-1);
  --color-bg-page: var(--gray-2);
  --color-bg-hover: var(--gray-3);
  --color-divider: var(--gray-4);
  --color-border: var(--gray-5);

  /* Semantic */
  --color-success-bg: var(--Green-1);
  --color-success-border: var(--Green-3);
  --color-success: var(--Green-6);
  --color-success-active: var(--Green-7);

  --color-warning-bg: var(--Orange-1);
  --color-warning-border: var(--Orange-4);
  --color-warning: var(--Orange-6);
  --color-warning-active: var(--Orange-7);

  --color-error-bg: var(--Red-1);
  --color-error-border: var(--Red-2);
  --color-error: var(--Red-6);
  --color-error-active: var(--Red-7);

  --color-info-bg: var(--Blue-1);
  --color-info-border: var(--Blue-3);
  --color-info: var(--Blue-6);
  --color-info-active: var(--Blue-7);

  /* Focus ring */
  --focus-default: var(--Default);
  --focus-error: var(--Error);
  --focus-success: var(--Success);
  --focus-warning: var(--Warning);

  /* Shadow */
  --shadow-card: var(--shadow-level-1-down);
  --shadow-layer: var(--shadow-level-2-down);
  --shadow-card-hover: var(--shadow-hover);
  --shadow-ai-input-hover: var(--shadow-superbox-hover);
}
```

#### 前端落地注意事项

- 如果构建链对中文 CSS 变量名、空格变量名兼容性不好，应以英文 alias 为主，中文 Token 只保留在设计文档和 Figma Token 中。
- 重复 Token 不要重复声明：`Blue-7`、`gray-6/7/8/9` 等重复变量在工程里保留一个即可。
- `P6/P7` 是 `p6/p7` 的别名，工程交付统一使用小写。
- 不要把 AI 渐变赋给普通 primary button；普通主按钮继续使用 `p6/p5/p7`。
- 旧项目如果已经使用蓝色作为主色，需要在主题层做映射，不要直接修改语义色。

***

## 页面设计验收清单

### 全局一致性

- [ ] 页面是否使用统一画板和栅格？
- [ ] 间距是否为 4px 的倍数？
- [ ] 字号、行高、字重是否符合规范？
- [ ] 颜色是否来自统一 Token？
- [ ] 卡片、表格、表单、按钮是否使用统一组件？
- [ ] 页面层级是否清晰？

### 页面架构

- [ ] 用户是否能一眼看懂当前页面任务？
- [ ] 页面标题是否清晰？
- [ ] 面包屑是否正确？
- [ ] 主操作是否明显？
- [ ] 筛选区、内容区、操作区是否分区清楚？
- [ ] 小屏下侧边栏是否能正确收起？

### 表格

- [ ] 表头是否清晰？
- [ ] 单位是否放在表头？
- [ ] 对齐方式是否正确？
- [ ] 文本溢出是否有省略和 Tooltip？
- [ ] 是否有空状态？
- [ ] 是否有分页？
- [ ] 操作列是否过多？
- [ ] 横向滚动时是否冻结主列和操作列？

### 表单

- [ ] 标签是否简洁？
- [ ] 必填项是否清晰？
- [ ] 错误提示是否紧贴控件？
- [ ] 帮助说明是否必要且清楚？
- [ ] 输入框、选择器、日期等组件是否选型正确？
- [ ] 提交失败时是否说明原因？

### 按钮

- [ ] 一个操作区域是否只有一个主按钮？
- [ ] 按钮文案是否表达具体动作？
- [ ] 危险操作是否二次确认？
- [ ] 是否包含 hover / click / disabled / loading 状态？
- [ ] 多个操作是否按主次排列？

### 反馈

- [ ] 成功、失败、警告是否使用正确反馈组件？
- [ ] Message 是否用于轻量反馈？
- [ ] Notification 是否用于系统主动通知？
- [ ] Alert 是否用于常驻警告？
- [ ] Modal 是否用于阻断式确认？
- [ ] 抽屉是否用于不离开当前页面的附加任务？
- [ ] 空状态、异常页是否有下一步操作？

### AI 场景

- [ ] AI 操作是否说明生成内容和影响范围？
- [ ] AI 执行中是否有 Loading 或进度反馈？
- [ ] AI 结果是否可编辑、可审核、可应用？
- [ ] AI 结果是否展示来源或依据？
- [ ] 高风险 AI 操作是否需要人工确认？
- [ ] 是否保留操作日志和审计信息？

***

### 布局、列表与适配检查

| 检查项          | 合格标准                             |
| ------------ | -------------------------------- |
| 布局画板         | 是否以 1440px 为常规设计稿基准              |
| 原子单位         | 布局、间距、尺寸是否使用 4px 倍数              |
| 主流分辨率        | 是否至少检查 1920 / 1440 / 1366 / 1280 |
| 侧边栏断点        | 是否设置 992px 默认断点                  |
| 页面最小宽度       | 是否明确 768px 最小宽度和横向滚动策略           |
| 收起侧边栏        | 是否明确 56px 收起态宽度                  |
| 列表行高         | 表头和单元格是否统一 40px                  |
| 列表字号         | 列表文字是否统一 12px                    |
| 列表间距         | 列表距操作区和分页是否为 8px                 |
| 操作按钮         | 列表页操作按钮高度是否为 32px，按钮间距是否为 8px    |
| 筛选区          | 有标题/无标题筛选项间距是否符合 8A 规则           |
| Dashboard 图表 | 是否为环形、趋势、柱形等图表定义最小宽度             |
| 图表压缩         | 宽度不足时是否横向滚动，而不是强行压缩              |
| 抽屉弹窗         | 是否按 560px / 880px / 80% 三类宽度选择   |
| 表格横向滚动       | 是否固定关键列/操作列并保证表头内容对齐             |

### CSS Token 检查

| 检查项        | 验收标准                                                                          |
| ---------- | ----------------------------------------------------------------------------- |
| 主色         | 普通主操作是否使用 `p6 #00AB7A`，hover 是否使用 `p5`，active 是否使用 `p7`                       |
| AI 按钮      | AI 功能入口是否使用 `AI 默认 / AI 悬停 / AI 按下` 三态渐变                                      |
| AI 与普通按钮区分 | 普通保存、提交、筛选、导出按钮是否没有误用 AI 渐变                                                   |
| 中性色        | 背景、hover、边框、分割线、正文、标题是否分别调用正确 `gray-*`                                        |
| 功能色        | 成功、警告、错误、信息是否使用对应 Green / Orange / Red / Blue Token                           |
| 安全等级       | 致命、严重、高危、中危、低危、安全是否按 Deep red / Red / Orange red / Orange / Yellow / Green 映射 |
| Focus ring | 表单聚焦、错误、成功、警告状态是否使用 2px 外描边 Token                                             |
| 投影         | 卡片是否用一级投影；弹窗/抽屉是否用二级投影；AI 超级输入框 hover 是否用专门阴影                                 |
| 重复变量       | 是否清理了 `P6/P7`、重复 `Blue-7`、重复 `gray-*` 等冗余声明                                   |
| 工程命名       | 是否为中文 Token 建立英文 alias，避免工程兼容问题                                               |

## 最终落地建议

这套规范最值得继承的不是某一个颜色或某一个按钮样式，而是它的组织逻辑：

1. **先统一全局 Token**：颜色、字体、间距、栅格、投影。
2. **再统一页面骨架**：顶部、侧栏、内容区、弹层区。
3. **再统一组件选型**：什么时候用表格，什么时候用卡片，什么时候用抽屉。
4. **最后统一业务反馈**：尤其是删除、禁用、覆盖、发布、审批等敏感场景。

对于 AI 原生平台，不建议一味追求“科技感”。真正能提升产品质感的是：\
**清晰的信息架构、稳定的组件体系、可预期的交互反馈、透明的 AI 结果解释，以及关键操作的安全确认。**

***

***

# 通用组件规范

## 按钮规范

### 按钮类型

| 类型    | 用途                         |
| ----- | -------------------------- |
| 主按钮   | 引导用户完成当前最重要操作              |
| 次按钮   | 与主按钮配合，执行次级操作              |
| 辅助按钮  | 弱化操作、辅助功能                  |
| 文字按钮  | 最次级操作，常用于表格行操作             |
| 链接按钮  | 跳转到其他页面或位置                 |
| 图标按钮  | 空间有限或轻量操作                  |
| 下拉按钮  | 收纳多个同类或低频操作                |
| AI 按钮 | 触发 AI 生成、智能分析、自动填充、智能推荐等能力 |

### 按钮层级

#### 基本规则

- 一个操作区域只保留一个主按钮。
- 页面级主按钮一般放在页头右侧。
- 弹窗内主按钮放在右侧，取消按钮放在左侧。
- 多个按钮组合时，推荐 `1 个主操作 + N 个次操作`。
- 6 个以上操作时，应把低频操作放入“更多”下拉。

#### 示例

```text
[取消] [保存草稿] [确定发布]
```

其中：

- `确定发布` 为主操作。
- `保存草稿` 为次操作。
- `取消` 为辅助操作。

### 按钮状态

按钮必须至少包含以下状态：

| 状态             | 说明      |
| -------------- | ------- |
| Default        | 默认可点击状态 |
| Hover          | 鼠标移入状态  |
| Active / Click | 点击状态    |
| Disabled       | 不可用状态   |
| Loading        | 操作处理中状态 |
| Danger         | 危险操作状态  |

### 图标按钮

#### 使用场景

- 表格行内操作。
- 工具栏中的轻量操作。
- 空间有限的表单项操作。
- 需要快速识别的功能入口。

#### 规则

- 图标语义必须清晰。
- 不易理解的图标必须加 Tooltip。
- 一个页面避免过多带图标的强引导按钮。
- 图标按钮不应替代关键文字说明。

### AI 按钮规范

#### 使用场景

| AI 按钮文案 | 使用场景            |
| ------- | --------------- |
| 智能生成    | 生成文案、报告、任务方案    |
| AI 分析   | 对数据、日志、风险进行分析   |
| 智能推荐    | 推荐专家、策略、模板      |
| 自动填充    | 根据上下文填充表单       |
| 重新生成    | 对 AI 结果进行迭代     |
| 应用结果    | 将 AI 生成结果写入业务流程 |

#### AI 按钮视觉状态

| 状态             | Token   | CSS 值                                                                                                                                   | 说明             |
| -------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| Default        | `AI 默认` | `linear-gradient(307deg, #004DFF 0%, #1093FF 65%, #1AFFAF 100%)`                                                                        | 默认可点击状态        |
| Hover          | `AI 悬停` | `linear-gradient(0deg, rgba(255,255,255,0.20), rgba(255,255,255,0.20)), linear-gradient(126deg, #1AFFAF 0%, #1093FF 37%, #004DFF 100%)` | 悬停时叠加 20% 白色蒙层 |
| Active / Click | `AI 按下` | `linear-gradient(0deg, rgba(0,0,0,0.20), rgba(0,0,0,0.20)), linear-gradient(309deg, #004DFF 0%, #1093FF 54%, #1AFFAF 100%)`             | 按下时叠加 20% 黑色蒙层 |

#### AI 按钮设计要求

- AI 按钮必须和普通主按钮区分：普通主按钮走 `p6/p5/p7`，AI 按钮走三态渐变。
- AI 按钮应说明执行后会产生什么结果。
- 涉及写入、发布、删除、变更配置的 AI 操作，不能直接自动执行，必须由用户确认。
- AI 生成中必须有 loading 状态。
- AI 结果必须支持查看、修改、应用和撤销。
- 关键 AI 操作应保留日志，便于审计。
- 不要把所有主要按钮都做成 AI 渐变；否则用户无法判断哪些操作真的调用 AI 能力。

***

## 数据录入规范

### 表单

#### 使用场景

- 创建、编辑、配置、审批、提交数据。
- 由输入框、选择器、单选框、多选框等控件组成。
- 需要收集、校验、提交数据。

#### 表单标签规则

| 项目   | 规范                 |
| ---- | ------------------ |
| 标签长度 | 建议单行，不超过 10 个中文字符  |
| 标签表达 | 简洁、准确、业务可理解        |
| 必填项  | 使用统一必填标识           |
| 非必填项 | 不建议大量标注“选填”，避免视觉噪音 |
| 帮助说明 | 放在标签旁或输入框下方        |
| 错误说明 | 紧贴出错控件展示           |

#### 表单布局

| 类型   | 使用场景               |
| ---- | ------------------ |
| 单列表单 | 简单创建、编辑            |
| 多列表单 | 字段较多但信息结构清晰        |
| 分组表单 | 字段多且有明显类别          |
| 分步表单 | 流程长、需要逐步确认         |
| 抽屉表单 | 在不离开当前页面的情况下完成附加编辑 |
| 弹窗表单 | 轻量创建或确认，不适合复杂录入    |

### 输入框

#### 使用场景

- 用户需要输入文本、数字、符号。
- 单行或多行文本输入。
- 搜索、自动完成、提及、带单位输入等。

#### 状态

| 状态       | 说明   |
| -------- | ---- |
| 常规       | 默认状态 |
| Hover    | 鼠标移入 |
| Focus    | 输入中  |
| Filled   | 输入完成 |
| Disabled | 不可输入 |
| Error    | 校验失败 |

#### 占位文案

- 默认使用“请输入”。
- 需要具体说明时使用“请输入 XX”。
- 有格式要求时写明示例，例如“请输入邮箱地址”。
- 有字数限制时可提示“不超过 18 个字符”。

#### 长文本框

- 右下角显示字数限制与当前字数，例如 `0/100`。
- 超出字数限制时应阻止继续输入或明确提示。
- 内容超过默认高度时可出现滚动条。
- 可拖拽改变高度时需要限制最大高度。

### 选择器 Select

#### 使用场景

- 弹出下拉菜单供用户选择。
- 选项较多，不适合平铺展示。
- 支持单选、多选、搜索。

#### 规则

- 选项少于 5 个时，优先考虑 Radio 或 Checkbox。
- 单选可支持搜索。
- 多选应支持标签展示和清除。
- 无结果时展示“暂无数据”。
- 加载时展示“加载中”。

### 单选框 Radio

#### 使用场景

- 多个备选项中选择一个。
- 所有选项默认可见。
- 用户需要对比后选择。

#### 规则

- 选项不宜过多。
- 横向排列间距建议 24px。
- 纵向排列间距建议 12px。
- 如果选项超过 5 个，建议改用 Select。

### 多选框 Checkbox

#### 使用场景

- 在一组选项中选择一个或多个。
- 单独使用时也可表示两种状态之间的标记。

#### Checkbox 与 Switch 区别

| 组件       | 区别                  |
| -------- | ------------------- |
| Checkbox | 通常作为状态标记，需要配合提交按钮生效 |
| Switch   | 切换后立即生效             |

### 开关 Switch

#### 使用场景

- 表示开启 / 关闭。
- 两种互斥状态之间切换。
- 用户选择后系统立即执行。

#### 规则

- 适合简单、直观的状态切换。
- 对高风险开关，切换前应二次确认。
- 切换后必须反馈结果。
- 禁用态需要说明原因。

### 日期选择器

#### 使用场景

- 选择单个日期。
- 选择日期范围。
- 日期与时间组合选择。
- 使用预设范围。

#### 设计原则

- 标识当天日期。
- 明确选中日期或范围。
- 必要时标注周末、节假日或特殊日期。
- 支持键盘输入时必须容错。
- 日期范围应明确开始和结束。

### 时间选择器

#### 使用场景

- 选择小时、分钟、秒。
- 避免用户手动输入格式错误。
- 配合日期选择器形成完整时间范围。

#### 规则

- 时间格式必须统一。
- 禁用不可选时间。
- 错误格式需要即时提示。
- 默认值应符合业务场景。

### 级联选择

#### 使用场景

- 从关联数据集合中选择。
- 典型场景：省市区、公司层级、分类体系、组织结构。

#### 规则

- 层级关系必须清晰。
- 支持搜索时应提示匹配路径。
- 如果只选择最终节点，应避免误选中间层。
- 层级过深时建议改为树选择。

### 树选择

#### 使用场景

- 文件夹、组织架构、分类、地区等树形结构。
- 需要展开、收起、选择层级节点。

#### 规则

- 建议最多 4 级。
- 支持单选和多选。
- 多选时应明确父子节点联动关系。
- 节点过多时必须支持搜索。

### 穿梭框

#### 使用场景

- 在两栏之间移动元素。
- 需要从候选集合中选择一个或多个对象。

#### 规则

- 左侧为来源 Source。
- 右侧为目标 Target。
- 支持搜索。
- 支持已选数量展示。
- 移动按钮必须有禁用态。

### 上传

#### 使用场景

- 文件上传。
- 图片上传。
- 拖拽上传。
- 展示上传进度。

#### 规则

- 明确支持的扩展名。
- 明确文件大小限制。
- 上传中显示进度。
- 上传失败说明原因。
- 涉及敏感文件时应提示安全风险。

***

## 数据展示规范

### 表格

表格用于展示多条结构类似的数据，可支持排序、筛选、对比、自定义操作。

#### 使用场景

- 大量结构化数据。
- 需要排序、搜索、分页、筛选。
- 需要批量操作。
- 需要行级操作。

#### 表格规则

| 项目   | 规范                              |
| ---- | ------------------------------- |
| 布局   | 默认采用百分比布局，适配多分辨率                |
| 表头   | 单位统一放在表头内，表头文本不建议换行             |
| 排列   | 按业务重要程度和强相关性从左到右排列              |
| 首列   | 通常为主信息列                         |
| 对齐   | 名称、链接、信息、日期、姓名、操作左对齐；数据类右对齐     |
| 冻结   | 横向滚动时冻结主信息列和操作列                 |
| 树形层级 | 树形表格节点不得超过 3 层                  |
| 排序   | 数据类型列可排序，默认按业务逻辑排序              |
| 筛选   | 类别属性列可筛选                        |
| 文本溢出 | 单行文本超出后用 `...`，Hover 出现 Tooltip |
| 空数据  | 显示“暂无数据”，保留表头，不显示分页             |
| 操作列  | 操作过多时使用“更多”或下拉，不建议超过 5 个        |

#### 表格筛选区

推荐结构：

```text
[搜索框] [筛选项] [日期范围] [更多筛选]       [重置] [查询]
```

规则：

- 高频筛选项放在表格上方。
- 低频筛选项放入“更多筛选”。
- 表头筛选适合轻量类别过滤。
- 搜索条件复杂时应提供“重置”。

### 卡片

#### 使用场景

- Dashboard 概览。
- 信息聚合。
- 任务列表。
- 专家卡片。
- 项目卡片。

#### 规则

- 卡片承载一组相关信息或类似任务。
- 卡片标题要明确。
- 卡片内操作不要过多。
- 卡片之间间距保持一致。
- 可点击卡片必须有 Hover 状态。
- 卡片内信息应遵循“标题 → 状态 → 关键指标 → 描述 → 操作”的顺序。

### 统计数值

#### 使用场景

- 突出展示一个或一组数字。
- Dashboard 指标。
- 请求次数、成功率、风险数量、任务数等。

#### 规则

- 数字应明显大于说明文字。
- 单位必须清晰。
- 同组指标的格式保持一致。
- 对比数据应说明变化方向。
- 重要异常指标可结合功能色。

### 标签 Tag

#### 使用场景

- 标记属性。
- 状态分类。
- 等级展示。
- 可删除筛选条件。
- 多彩标签。

#### 规则

- 标签文字简短。
- 标签颜色必须和语义一致。
- 状态标签和等级标签不要混用。
- 可删除标签需要明确关闭图标。
- 风险等级标签必须使用统一安全等级色。

### 徽标数 Badge

#### 使用场景

- 通知数量。
- 待办数量。
- 告警数量。
- 状态点。

#### 规则

- 数量过大使用 `999+`。
- 状态点用于在线、离线、异常、处理中等状态。
- 徽标不能遮挡主体图标识别。

### 描述列表

#### 使用场景

- 详情页只读字段展示。
- 成组展示对象属性。
- 审批详情、项目详情、专家详情。

#### 规则

- 字段名称和字段值分区清晰。
- 字段过多时分组展示。
- 可使用带边框样式强化结构。
- 空字段统一显示 `--` 或“暂无”。

### 时间轴

#### 使用场景

- 操作记录。
- 审批流程。
- 任务进度。
- 日志事件。

#### 规则

- 可正序或倒序，但同一产品内应统一。
- 状态点颜色必须表达进度或结果。
- 重要节点可加入图标。
- 时间、操作者、动作、结果应完整展示。

### Tooltip 文字提示

#### 使用场景

- 对图标、按钮、术语进行解释。
- 表格文本溢出展示完整内容。
- 轻量辅助说明。

#### 规则

- 不承载复杂内容。
- 不承载操作。
- 不用于主体内容展示。
- 文案必须短。

### Popover 气泡卡片

#### 使用场景

- 目标元素有进一步说明和相关操作。
- 需要承载链接、按钮、简短详情。
- 用户可与浮层内容交互。

#### Tooltip 与 Popover 区别

| 组件      | 是否可操作 | 内容复杂度         |
| ------- | ----- | ------------- |
| Tooltip | 不可操作  | 简短文字          |
| Popover | 可操作   | 文本、链接、按钮、简短列表 |

### 折叠面板

#### 使用场景

- 复杂区域分组。
- 隐藏低频信息。
- 保持页面整洁。

#### 规则

- 标题必须概括内容。
- 只允许单项展开时使用手风琴模式。
- 默认展开项应符合用户主要任务。
- 不要把高频信息默认折叠。

***

***

## 反馈与异常规范

### 全局提示 Message

#### 使用场景

- 用户主动操作后的轻量反馈。
- 只需要阅读，不需要后续操作。
- 加载完成、保存成功、删除失败等。

#### 规则

- 位于页面顶部居中。
- 约 3 秒后自动消失。
- 不打断用户操作。
- 类型仅保留常规、成功、预警、失败。
- 文案尽量短，例如“保存成功”“删除失败，请重试”。

### 通知提醒框 Notification

#### 使用场景

- 系统主动推送。
- 后台任务完成。
- 工作流结果通知。
- 复杂内容提醒。

#### 规则

- 出现在页面角落。
- 不遮挡关键内容。
- 内容比 Message 更复杂。
- 可包含标题、说明和操作入口。
- 不重要通知应进入消息中心，不要弹出打扰用户。

### 警告提示 Alert

#### 使用场景

- 页面级重要提示。
- 用户需要持续关注的警告信息。
- 常驻展示，不自动消失。

#### 规则

- 可关闭，但关闭前不应影响理解。
- 用于整个页面或模块提示。
- 不适合直接用于弹窗内复杂提示，弹窗内应改为轻量文字说明。
- 文案应说明问题、影响和建议操作。

### 弹窗 Modal

#### 使用场景

- 用户必须确认后才能继续。
- 高风险操作确认。
- 短表单或结果反馈。
- 阻断当前流程的关键动作。

#### 规则

- 弹窗必须有明确标题。
- 标题要说明动作和对象。
- 描述要说明后果。
- 主按钮文案不要只写“确定”，应尽量写明动作，如“确认删除”。
- 不可逆操作使用危险按钮。

#### 高风险操作反馈模板

```text
标题：确定 {动作} {对象} 吗？
说明：{动作} 后 {结果}，请谨慎操作。
增强说明：{进一步业务影响或恢复方式}
按钮：[取消] [确认{动作}]
```

示例：

```text
标题：确定删除客户端策略吗？
说明：删除后将无法恢复，请谨慎操作。
增强说明：删除后客户端将无法执行该策略，请确认后继续。
按钮：[取消] [确认删除]
```

### 气泡确认框 Popconfirm

#### 使用场景

- 目标元素附近的轻量二次确认。
- 表格行内删除、停用、移除等操作。
- 操作风险较低，但需要用户确认。

#### 规则

- 只用于确认操作。
- 不承载复杂表单。
- 不承载详细说明。
- 高风险不可逆操作应使用 Modal，而不是 Popconfirm。

### 抽屉 Drawer

#### 使用场景

- 在不离开当前页面的情况下插入附加任务。
- 查看详情。
- 编辑辅助信息。
- 配置筛选条件。
- 表格行详情。

#### 规则

- 从页面边缘滑出。
- 覆盖部分父页面。
- 用户完成后能平滑回到原任务。
- 不适合承载特别复杂的多步骤流程。
- 抽屉宽度要根据内容复杂度设置。

### 加载中 Loading

#### 使用场景

- 页面或区块异步加载。
- 数据渲染等待。
- AI 生成中。
- 文件上传中。

#### 规则

- 加载时间较短时使用轻量 Loading。
- 超过 2 秒的任务建议显示进度或说明。
- AI 生成类任务应说明当前状态，例如“正在分析日志”“正在生成报告”。
- 加载失败必须给出重试入口。

### 进度条 Progress

#### 使用场景

- 操作耗时较长。
- 需要显示完成百分比。
- 后台任务执行。
- 上传、扫描、生成、部署。

#### 规则

- 超过 2 秒的任务建议显示进度。
- 支持成功、失败、处理中状态。
- 不能准确计算进度时，可以使用阶段进度或环形进度。
- 完成后需要结果反馈。

### 空状态 Empty

#### 使用场景

- 数据为空。
- 搜索无结果。
- 暂无任务。
- 暂无通知。
- 暂无权限下的数据入口。

#### 规则

- 不要只显示空白。
- 需要包括：插图、标题、说明、可选操作。
- 如果用户可以创建数据，应提供创建入口。
- 如果用户无权限，应说明原因和申请方式。
- 图标插图建议使用统一画板，例如 120×120。

### 异常页面

#### 使用场景

- 无权限。
- 网络不可用。
- 系统维护。
- 加载失败。
- 404。
- 500。

#### 规则

- 由插图、标题、说明、建议操作组成。
- 标题最多一行。
- 说明解释发生了什么。
- 操作给出下一步，如“返回首页”“刷新重试”“联系管理员”。
- 不同异常类型使用统一视觉风格。

***

## 组件库规范

### 组件尺寸总表

| 组件族        | 主要组件别名                | 标准尺寸/面板尺寸                                                          | 字号/行高             | 圆角          | 关键状态                            |
| ---------- | --------------------- | ------------------------------------------------------------------ | ----------------- | ----------- | ------------------------------- |
| 基础按钮       | `button`              | 紧凑约 24px；常规 32px；宽松 40px；图标按钮常见 24 / 32px                          | 12/20、14/22、16/28 | 4 / 6 / 8px | 默认、悬停、按下、禁用、加载                  |
| 胶囊单选       | `radio-btn`           | 单项 74 × 32px                                                       | 14/22             | 左/中/右分段圆角   | 默认、hover、选中、未选禁用、已选禁用           |
| 基础单选       | `radio`               | 常见 52 × 22px、94 × 22px                                             | 14/22             | 控件自身圆形      | 可选、悬浮、已选、未选失效、已选失效              |
| 多选框        | `checkbox-basic`      | 控件 16 × 16px                                                       | -                 | 4px         | 默认、悬浮、选中、半选、未选失效、已选失效、半选失效      |
| 多选项        | `checkbox-group`      | 选项行高 22px                                                          | 14/22             | -           | 默认、悬停、选中、部分选中、禁用                |
| 输入框        | `input-basic`         | 小 24px；中 32px；大 40px；基础宽度 200px                                    | 12/20、14/22、16/28 | 4 / 6 / 8px | 默认、悬停、激活、禁用、输入完成、失败             |
| 文本域        | `input-textarea`      | 常见 200 × 100px；带字数控制 200 × 122px                                   | 12/20、14/22       | 6 / 8px     | 默认、悬停、激活、禁用、输入完成、失败             |
| 数字输入框      | `inputnumber`         | 小 200 × 24px；中 200 × 32px；大 200 × 40px                             | 12/20、14/22、16/24 | 4 / 6 / 8px | 默认、hover、输入中、输入完成、报错、禁用、悬浮增减图标  |
| IPv4 输入框   | `input-ipv4`          | 默认 188 × 32px；错误提示态约 188 × 52px                                    | 12/20、14/22       | 4 / 6px     | 默认、悬停、激活、禁用、输入完成、失败             |
| Select     | `select`              | 小 200 × 24px；中 200 × 32px；大 200 × 40px；下拉示例约 200 × 208px           | 12/20、14/22、16/24 | 4 / 6 / 8px | 默认、hover、激活、选择完成、加载中、禁用、多选、删除悬浮 |
| Cascader   | `cascader`            | 一级 120 × 160px；二级 240 × 160px；三级 360 × 160px                       | 14/22             | 6px         | 单选、多选，一/二/三级                    |
| TreeSelect | `treeselect`          | 基本样式约 267 × 232px；多选框样式约 239 × 232px                               | 14/22             | 6px         | 父级默认/禁用，子级默认/悬浮/选中/禁用           |
| DatePicker | `datepicker-basic`    | 单日期常见 240 × 32px；区间常见 400 × 32px；大尺寸 40px；下拉面板 280 / 560 / 657px 宽 | 12/20、14/22、16/24 | 4 / 6 / 8px | 未填写、已填写、默认、悬停、聚焦、输入中、禁用、悬停清空    |
| TimePicker | `timepicker`          | 单时间 240 × 32px；区间 320 × 40px；面板 112 / 168 / 340 × 248px            | 12/20、14/22、16/24 | 4 / 6 / 8px | 未填写、已填写、默认、悬停、聚焦、输入中、禁用、悬停清空    |
| 文件上传       | `upload-basic`        | 常见 306 × 40px；失败/过程态会扩展到 62 / 64 / 254px 高                         | 14/22             | 按内部容器处理     | 点击上传、上传过程、上传中、暂停、上传完成、上传失败      |
| 图片上传       | `upload-image`        | 基础格 80 × 80px；示例区 302 × 108 / 356 × 108 / 448 × 200px              | 12/20             | 8px         | 默认、hover、上传中、上传成功、上传失败、成功/失败后悬浮 |
| 拖拽上传       | `upload-drag`         | 默认 384 × 194px；带上传文件列表 384 × 346px                                 | 14/22、16/24       | 8px         | 默认、hover、禁用、上传文件                |
| 上传对话框      | `upload-dialog-group` | 宽 560px；高度约 346 / 372 / 384 / 399 / 400px                          | 14/22、16/24       | 8px         | 默认、拖拽、上传中、上传成功、导入失败、格式错误、文件大小错误 |

### 按钮与图标按钮规范

#### 按钮类型

基础按钮组件包含四类按钮：

- **主按钮**：页面主操作、表单提交、关键流程确认。
- **次要按钮**：与主按钮并列的次级操作，如取消、返回、普通筛选。
- **辅助按钮**：低权重辅助动作，如更多配置、次级入口。
- **文字按钮**：表格行内操作、轻量链接动作、局部次级操作。

#### 按钮尺寸

| 尺寸 | 推荐高度 | 典型使用场景                | 字体          |  圆角 |
| -- | ---: | --------------------- | ----------- | --: |
| 紧凑 | 24px | 表格行内操作、筛选区轻量按钮、空间受限区域 | 12px / 20px | 4px |
| 常规 | 32px | 页面主要按钮、表单按钮、工具栏按钮     | 14px / 22px | 6px |
| 宽松 | 40px | 强视觉主按钮、表单底部主操作、引导型按钮  | 16px / 28px | 8px |

按钮支持前置图标、后置图标、纯图标、纯文字以及图标+文字组合。图标按钮应优先使用 24px 或 32px 正方形容器，避免在高密度表格中使用 40px 图标按钮。

#### 按钮状态

| 状态 | 使用规则                          |
| -- | ----------------------------- |
| 默认 | 正常可点击状态                       |
| 悬停 | 鼠标 hover 时强化边框、填充或文字颜色        |
| 按下 | 点击瞬间状态，颜色应比 hover 更深或更收敛      |
| 禁用 | 不可点击，使用禁用灰阶，禁止 hover 反馈       |
| 加载 | 提交、保存、上传、AI 生成等等待场景，按钮需阻止重复点击 |

AI 能力按钮仍按第 6.5 节使用 AI 渐变三态；普通业务按钮不要滥用 AI 渐变。

### 单选与多选规范

#### 胶囊单选 `radio-btn`

胶囊单选适合在筛选条件、模式切换、周期切换、视图切换中使用。单项标准尺寸为 **74 × 32px**，内边距为 **5px 16px**，文字为 **14px / 22px**。

| 方位 | 圆角规则              |
| -- | ----------------- |
| 左  | 左上、左下圆角 6px；右侧为直角 |
| 中  | 左右均为直角            |
| 右  | 右上、右下圆角 6px；左侧为直角 |

状态必须包含：默认、hover、选中、未选禁用、已选禁用。

#### 基础单选 `radio` 与单选组 `radio-group`

基础单选适合表单中二选一或少量互斥选项。常见选项高度为 **22px**，文字为 **14px / 22px**。单选组支持：

- 常规横向排列；
- 常规纵向排列；
- 胶囊样式排列。

#### 多选框 `checkbox-basic` 与多选项 `checkbox-group`

基础多选框控件尺寸为 **16 × 16px**，控件圆角为 **4px**。必须包含以下状态：

- 默认；
- 悬浮；
- 选中；
- 半选；
- 未选失效；
- 已选失效；
- 半选失效。

多选项组的文字高度为 **22px**，文字为 **14px / 22px**。在树选择、权限配置、批量选择场景中，半选状态不能省略。

### 输入框族规范

#### 普通输入框 `input-basic`

输入框支持小、中、大三种尺寸，默认宽度常见为 **200px**。中号输入框是 B 端表单默认规格，高度为 **32px**，内边距常见为 **5px 12px**。

| 尺寸 |   高度 | 字号/行高       |  圆角 | 使用场景            |
| -- | ---: | ----------- | --: | --------------- |
| 小  | 24px | 12px / 20px | 4px | 表格筛选、紧凑工具栏、行内编辑 |
| 中  | 32px | 14px / 22px | 6px | 常规表单、搜索、筛选      |
| 大  | 40px | 16px / 28px | 8px | 重点输入、引导页、低密度表单  |

输入框状态统一为：默认、悬停、激活、禁用、输入完成、失败。失败状态必须配合错误文案，例如“输入错误原因”，不要只改变边框颜色。

输入框支持以下组合：

- 纯文字输入；
- 前置标签；
- 后置标签；
- 前置图标；
- 后置图标；
- 字数统计，例如 `0/10`。

#### 文本域 `input-textarea`

文本域常见默认尺寸为 **200 × 100px**；启用字数控制时常见尺寸为 **200 × 122px**。文本域支持常规文字尺寸、字数控制、可拖拽、滚动条。

| 状态   | 规则                 |
| ---- | ------------------ |
| 默认   | 显示占位文案“请输入...”     |
| 悬停   | 边框或背景轻微反馈          |
| 激活   | 使用主色边框或 focus ring |
| 输入完成 | 显示输入内容，必要时保留字数统计   |
| 失败   | 显示错误边框和错误说明        |
| 禁用   | 文本与边框进入禁用灰阶        |

文本域中滚动条宽度示例为 **4px**，应使用弱视觉灰阶，避免抢占输入内容注意力。

#### 数字输入框 `inputnumber`

数字输入框宽度常见为 **200px**，三档高度为 **24 / 32 / 40px**。它不是普通输入框的简单替代，应包含数值范围、增减控制、错误提示和禁用态。

必须包含的状态包括：默认、hover、键入、输入中、输入完成、报错、禁用、悬浮增减图标。

典型提示文案：

- `0-100`；
- `请输入0-100之间的数值`。

#### IPv4 输入框 `input-ipv4`

IPv4 输入框是专用输入组件，默认尺寸为 **188 × 32px**。当出现格式错误时，组件高度可扩展到约 **52px**，用于承载错误提示。

输入结构应按四段 IP 地址组织：

```text
0 . 0 . 0 . 0
192 . 168 . 1 . 1
```

校验规则：每段只允许 **0–255 的正整数**。错误提示可使用：

```text
格式错误，请输入0-255的正整数
```

### 选择器族规范

#### Select 选择器 `select`

Select 选择器的输入框外观与普通输入框保持一致，但状态复杂度更高。它必须支持单选、多选、加载、禁用、选中、清空和删除图标悬浮等交互。

| 尺寸 |   高度 |  常见宽度 | 使用场景        |
| -- | ---: | ----: | ----------- |
| 小  | 24px | 200px | 紧凑筛选、表格内筛选  |
| 中  | 32px | 200px | 常规表单选择器     |
| 大  | 40px | 200px | 低密度表单、引导式配置 |

状态包含：默认、hover、激活-未选下拉、激活-选中、选择完成、选择后 hover、鼠标再次悬浮、悬浮删除图标、选项加载中、禁用、多选-选择、多选-选择完成、多选-禁用、级联-选择完成。

下拉选项需要区分：默认、悬浮、选中、禁用、加载中。选项文字默认 **14px / 22px**。

#### Cascader 级联选择器 `cascader`

级联选择器适合选择具有父子层级的数据，例如地区、组织、权限分类、业务对象分类。面板宽度按层级扩展：

| 层级 | 面板尺寸        |
| -- | ----------- |
| 一级 | 120 × 160px |
| 二级 | 240 × 160px |
| 三级 | 360 × 160px |

级联选择器支持单选与多选。每一级选项文字为 **14px / 22px**，面板圆角为 **6px**。

#### TreeSelect 树选择 `treeselect`

树选择用于层级结构更深、需要展开/折叠节点的选择场景。组件支持基本样式和多选框样式。

| 样式    | 面板尺寸          | 使用场景       |
| ----- | ------------- | ---------- |
| 基本样式  | 约 267 × 232px | 单选层级对象     |
| 多选框样式 | 约 239 × 232px | 权限、组织、分类多选 |

树节点状态至少包含：父级默认、父级禁用、子级默认、子级悬浮、子级选中、子级禁用、三级子级。展开图标尺寸示例为 **12 × 12px**。

### 日期与时间选择器规范

#### DatePicker 日期选择器 `datepicker-basic`

日期选择器支持三类核心类型：

- 选日期/日期时间；
- 日期区间；
- 终端自定义日期选择。

| 类型     | 常见尺寸       | 说明          |
| ------ | ---------- | ----------- |
| 单日期中号  | 240 × 32px | 默认表单日期选择    |
| 日期区间中号 | 400 × 32px | 开始日期 + 结束日期 |
| 大号日期输入 | 高度 40px    | 低密度表单或重点输入  |
| 小号日期输入 | 高度 24px    | 紧凑筛选区       |

状态分为两个层级：

1. **填写状态**：未填写完成、已填写完成。
2. **交互状态**：默认、悬停、聚焦、输入中、禁用、悬停清空图标。

日期单元格支持标题、周几、年份/月、日期、时间等类型。日期格常见 **24 × 24px**，面板标题行常见 **280 × 40px**。日期下拉面板支持日、月、年、日+时、日-双月、双月+时间范围等结构；常见面板宽度为 **280px、560px、657px**。

#### TimePicker 时间选择器 `timepicker`

时间选择器支持单时间与时间区间。输入框尺寸与普通输入框一致，默认中号高度为 **32px**。

| 类型     | 常见尺寸       |
| ------ | ---------- |
| 单时间中号  | 240 × 32px |
| 时间区间大号 | 320 × 40px |
| 小号时间输入 | 高度 24px    |

时间下拉面板常见尺寸：

| 面板类型   | 尺寸          |
| ------ | ----------- |
| 单列     | 112 × 248px |
| 双列/复合列 | 168 × 248px |
| 区间或多列  | 340 × 248px |

时间选项示例为 `01`、`02`、`03` 等，列表需要支持悬停、选中、禁用与滚动。

### 上传组件规范

#### 文件上传 `upload-basic`

文件上传组件支持点击上传、上传过程、上传中、暂停、上传完成、上传失败六类状态。

| 状态        | 尺寸/表现                    |
| --------- | ------------------------ |
| 点击上传      | 常见 306 × 40px，通常由按钮和说明组成 |
| 上传中/暂停/完成 | 保留文件名、进度、状态图标或操作按钮       |
| 上传失败      | 显示失败原因，例如“上传失败！失败原因xxx”  |
| 上传过程      | 可扩展为更高容器，用于显示多个文件进度      |

说明文案示例：

```text
支持扩展名：.rar .zip .doc .docx .pdf .jpg...
```

#### 图片上传 `upload-image`

图片上传基础格尺寸为 **80 × 80px**，圆角为 **8px**。适合头像、封面、素材图、证件图等上传场景。

必须包含状态：默认、hover、上传中、上传成功、上传失败、上传成功后鼠标悬浮、上传失败后鼠标悬浮。

说明文案示例：

```text
建议尺寸 900*400，最多 9 张，单个图片不超过 20M。
```

#### 拖拽上传 `upload-drag`

拖拽上传默认尺寸为 **384 × 194px**，带上传文件列表时可扩展到 **384 × 346px**。圆角为 **8px**。

状态包含：默认、hover、禁用、上传文件。

主文案示例：

```text
点击或将文件拖拽到这里上传
```

#### 上传/导入对话框 `upload-dialog-group`

上传对话框以 **560px 宽** 为主要规格，与前文弹窗标准宽度一致。高度根据内容变化，常见为 **346 / 372 / 384 / 399 / 400px**。

必须包含以下状态：默认、拖拽、上传中、上传成功、上传成功 hover、导入失败、格式错误、文件大小不符合导入失败。

常见文案：

```text
单次仅支持上传 1 个文件，支持文件格式：Excel、CSV
下载模版
请下载模版完成填写后上传
上传文件格式错误！
上传失败！文件大小不超过x
导入中
已完成进度 4   65%
文件总数: 4   已上传: 1   上传中: 1   暂停: 1   上传失败: 1
```

### 投影规范补充

投影不是装饰项，而是层级表达工具。

| 投影层级 | 方向                | 应用场景                |
| ---- | ----------------- | ------------------- |
| 一级投影 | 向上 / 向下 / 向左 / 向右 | 浮起投影，主要用于卡片背景浮起状态   |
| 二级投影 | 向上 / 向下 / 向左 / 向右 | 浮层投影，主要用于侧拉抽屉、遮罩弹窗等 |

投影颜色基准为 **#242933**。普通卡片不要使用二级投影；抽屉、弹窗、浮层不要只使用一级投影，否则层级不够明确。

### 组件使用验收清单

| 检查项  | 必须满足的标准                                                    |
| ---- | ---------------------------------------------------------- |
| 尺寸   | 输入、选择、按钮等基础控件是否优先使用 24 / 32 / 40px 三档高度                    |
| 字体   | 常规表单控件是否使用 14px / 22px；紧凑控件是否使用 12px / 20px                |
| 圆角   | 小/中/大控件是否对应 4 / 6 / 8px，不随意新增圆角值                           |
| 状态   | 每个交互控件是否至少包含默认、hover、激活/按下、禁用；输入类是否适配失败和输入完成               |
| 错误提示 | 失败态是否有明确错误文案，而不仅仅是红色边框                                     |
| 选择器  | Select / Cascader / TreeSelect 是否包含面板、选项 hover、选中、禁用、加载等状态 |
| 日期时间 | DatePicker / TimePicker 是否包含区间、清空悬浮、禁用、聚焦、输入中等状态           |
| 上传   | 上传组件是否适配上传中、暂停、完成、失败、拖拽、格式错误和文件大小错误                        |
| 多选半选 | Checkbox 和 TreeSelect 是否包含半选/部分选中状态                        |
| 弹窗   | 上传/导入对话框是否使用 560px 作为默认宽度，并与全局弹窗规范一致                       |
| 投影   | 卡片只用一级投影；抽屉、弹窗、遮罩浮层使用二级投影                                  |

### AI 生成界面提示词补充约束

当使用 AI 生成 B 端界面、Figma 草图或前端 Demo 时，在原提示词后补充以下约束：

```text
Use the component specification: standard B-end SaaS component library, compact and high-density layout. Use 24 / 32 / 40px control height system. Default medium input/select/button height is 32px. Use 4 / 6 / 8px radius according to small / medium / large components. Input, select, date picker, time picker, textarea, number input and IPv4 input must include default, hover, active/focused, disabled, completed and error states where applicable. Checkbox must include selected and indeterminate states. Upload components must include click upload, image upload, drag upload, uploading, paused, success and failure states. Do not create arbitrary component sizes outside the token system.
```

中文提示词补充版：

```text
请严格遵循组件规范生成界面：整体为高密度 B 端 SaaS 组件库风格，控件高度优先使用 24 / 32 / 40px 三档，中号输入框、选择器、按钮默认高度为 32px；圆角按小/中/大分别使用 4 / 6 / 8px；输入框、选择器、日期选择器、时间选择器、文本域、数字输入框、IPv4 输入框需要体现默认、悬停、激活/聚焦、禁用、输入完成、失败等状态；多选框必须包含半选状态；上传组件必须区分点击上传、图片上传、拖拽上传、上传中、暂停、成功、失败等状态；禁止随意生成不符合规范的控件高度、圆角、字体和面板尺寸。
```

***

# Dashboard工作台模板

## Dashboard / 工作台页面模板规范

Dashboard / 工作台用于承载企业级系统的整体态势、核心指标、趋势分析、风险告警、任务待办和明细入口。页面应遵循“先概览、再诊断、后行动”的阅读路径，优先保证数据可信、层级清晰和操作明确。

### 通用页面骨架

```text
顶部全局导航
左侧一级 / 二级导航
页面标题区 / 面包屑 / 时间筛选 / 刷新操作
核心概览区
风险告警 / 状态摘要区
图表分析区
排行 / 分布 / 趋势区
明细表格 / 任务列表 / 事件列表
分页 / 辅助操作区
```

| 区域    | 规范                                                   |
| ----- | ---------------------------------------------------- |
| 顶部导航  | 固定在页面顶部，深色背景优先，承载产品 Logo、一级导航、全局状态、通知、用户入口           |
| 左侧导航  | 白色或浅灰背景，当前项使用主色竖条与浅绿色选中底色，支持展开 / 收起 / 二级菜单           |
| 内容背景  | 使用浅灰背景，卡片使用白色底，模块之间通过 16px / 24px 间距区分               |
| 页面标题区 | 标题左对齐，时间筛选、刷新、搜索等操作右对齐                               |
| 卡片容器  | 白底、弱边框或轻投影，圆角 4px / 6px / 8px，内边距 16px / 20px / 24px |
| 数据模块  | 核心指标优先放在首屏上方，图表放在中部，表格和明细放在下方                        |
| 视觉重心  | 重要数值、风险、异常、AI 评分或中心图表作为首屏视觉焦点                        |
| 滚动方式  | 单屏 Dashboard 使用 16:9；长页工作台可采用 1440px 宽度与自适应页面高度      |

### 顶部导航与侧边栏规范

#### 顶部导航

- 顶部导航高度建议 56px。
- 背景使用深色中性底，文字使用白色或浅灰。
- 一级导航横向排列，当前项使用主色下划线或主色高亮。
- 右侧放置系统运行状态、订阅信息、通知、帮助、用户身份与下拉入口。
- 顶部导航不承载过多业务筛选条件，业务筛选应放入内容区或二级标签区。

#### 侧边栏

- 展开态侧边栏建议 200px / 220px。
- 收起态侧边栏建议 56px。
- 当前菜单项使用浅绿色背景 + 主色竖条 + 主色文字。
- 二级菜单缩进显示，父级菜单使用展开 / 收起箭头。
- 菜单图标保持线性、轻量、统一描边。
- 页面底部可放置版本号、折叠菜单入口或系统辅助信息。

### 页面顶部工具区规范

页面顶部工具区用于承载当前页面标题、范围筛选和高频操作。

| 元素   | 规范                                    |
| ---- | ------------------------------------- |
| 页面标题 | 4–12 字，字号 16px / 18px，字重 500 / 600    |
| 时间筛选 | 优先放在右侧，高度 32px，支持近 7 天、近 30 天、自定义时间范围 |
| 刷新按钮 | 位于时间筛选附近，使用次按钮样式                      |
| 搜索框  | 可用于资产、IP、域名、事件、任务检索，高度 32px           |
| 筛选器  | 单行不超过 4 个，复杂筛选放入展开面板                  |
| 二级标签 | 用于切换二级模块、分类看板或数据维度，当前项使用主色下划线         |

### 核心指标卡片规范

核心指标卡用于快速呈现系统最重要的数量、比例、趋势和风险状态。

```text
图标 / 状态角标
指标标题
核心数值 + 单位
趋势箭头 / 同比环比 / 状态说明
```

| 项目   | 规范                        |
| ---- | ------------------------- |
| 单屏数量 | 4–8 个                     |
| 卡片高度 | 80px / 96px / 120px       |
| 图标容器 | 40px / 48px，浅色底，主色图标      |
| 指标标题 | 12px / 14px，中性色           |
| 核心数值 | 24px / 28px / 32px，深色，高字重 |
| 单位   | 放在数值右侧，字号低一级              |
| 趋势   | 上升使用绿色，下跌或风险使用红色 / 橙色     |
| 分隔方式 | 横向指标卡之间可使用竖向分割线或独立卡片间距    |

指标卡应避免堆叠长句。每张卡只表达一个核心指标。

### 风险告警与状态摘要规范

风险告警区用于突出异常、风险、告警、待处理事项和系统健康状态。

#### 风险告警条

```text
警告图标
风险摘要文案
关键风险数量
辅助指标
主操作按钮
```

- 风险告警条应靠近首屏上方。
- 高危风险使用红色或橙红色强调，背景使用浅红 / 浅橙色。
- 风险文案应包含数量和风险等级，例如“存在高危风险 33 分”。
- 主操作按钮用于“立即体检”“查看详情”“处理风险”等直接行动。
- 告警条不宜承载复杂表格，只表达核心风险与行动入口。

#### 状态摘要区

- 可采用三列结构：安全状态、风险资产、安全运维。
- 每列包含图标、指标名称、数量与单位。
- 状态类数据应使用成功、警告、错误、风险等级色区分。
- 事件类区域可使用标签页切换：安全事件 / 威胁告警 / 任务动态。

### 图表区布局规范

Dashboard 图表区应根据数据关系选择布局，不应为了填充页面而堆叠图表。

| 图表区类型   | 推荐布局      | 使用场景              |
| ------- | --------- | ----------------- |
| 两列图表区   | 1:1 或 2:1 | 趋势 + 占比、排名 + 分布   |
| 三列图表区   | 4:4:4     | 多个同级指标对比          |
| 主图 + 辅图 | 8:4 或 9:3 | 核心趋势 + 辅助分布       |
| 卡片网格    | 3 / 4 列等宽 | 监控状态、资源卡片、趋势小图    |
| 长页分析区   | 按主题纵向分区   | 安全运营、资源监控、资产盘点    |
| 全宽趋势区   | 12 栅格     | 时间跨度较长、数据点较多的趋势分析 |
| 全宽表格区   | 12 栅格     | 明细数据、事件列表、监控记录    |

### 图表类型组合规范

| 数据目标   | 推荐图表               | 展示规则                     |
| ------ | ------------------ | ------------------------ |
| 总体评分   | 仪表盘 / 环形进度 / 中心大数值 | 可作为首屏视觉中心                |
| 趋势变化   | 折线图 / 面积图          | 适合流量、风险、事件、使用率变化         |
| 类别占比   | 环形图 / 饼图           | 中心可放总数，右侧放图例             |
| Top 排名 | 横向条形图              | 用于漏洞 Top、病毒 Top、攻击来源 Top |
| 设备状态   | 进度条 / 横向排行 / 环形图   | 展示在线率、设备类型、容量占用          |
| 风险分布   | 环形图 / 分级柱状图        | 使用高 / 中 / 低风险色彩映射        |
| 能力覆盖   | 雷达图                | 用于防护能力、检测能力、技术维度对比       |
| 矩阵分析   | 热力矩阵 / 分组网格        | 用于策略覆盖、防护效果、攻击面分布        |
| 明细记录   | 表格                 | 放在页面下方，支持排序、分页和详情入口      |

单屏图表类型不宜超过 3 种。长页 Dashboard 可按主题分区使用更多图表类型，但每个分区内部应保持视觉一致。

### Dashboard 页面类型模板

#### 综合态势工作台

用于展示企业、项目、系统或安全业务的整体运行状态。

```text
顶部导航
侧边栏
企业 / 项目概览横幅
关键指标总览
风险告警条
安全状态与事件摘要
资产盘点模块
运营看板模块
服务资源模块
```

必备模块：

- 企业 / 项目名称与授权状态。
- 3–5 个核心待办 / 待处理 / 待施工 / 待评估指标。
- 风险告警条，包含风险数量、风险等级和处理入口。
- 安全状态检测或健康度摘要。
- 安全事件 / 威胁告警 / 任务动态标签区。
- 资产盘点模块，包含终端统计、设备概况、设备类型分布。
- 运营分析模块，包含类型分布、Top 排名、趋势曲线。
- 服务资源模块，包含 CPU / 内存 / 磁盘 / 容量占用等资源指标。

生成约束：

- 首屏必须突出风险或待办，不要只展示普通图表。
- 长页内容应按“概览—风险—资产—运营—资源”纵向排列。
- 每个大分区必须有清晰标题。
- 图表和事件列表应保持 6:4 或 1:1 的视觉平衡。

#### 实时监控 Dashboard

用于展示系统、组件、设备或服务的实时状态。

```text
顶部导航
二级标签栏
刷新按钮
状态总览卡
组件拓扑 / 状态关系图
实时趋势图
容量与排行卡片
监控明细表格
```

必备模块：

- 总体状态卡，展示运行时长、异常状态或健康度。
- 组件状态图，中心为核心组件，周围为正常 / 异常节点。
- 最近 5 分钟或最近 10 分钟趋势图。
- 容量卡片，展示已用容量、剩余容量、磁盘列表。
- Top 排名卡片，展示数据源、设备、任务或资源排行。
- 底部监控表格，展示名称、类型、状态、分片、目录、更新时间等字段。

生成约束：

- 卡片网格优先采用 3 列或 4 列布局。
- 实时监控类图表应显示时间轴与单位。
- 正常节点使用绿色，异常节点使用红色，告警节点使用橙色。
- 底部明细表格应横跨全宽。

#### AI 决策分析 Dashboard

用于展示 AI 检测、风险判断、决策评分和策略建议。

```text
顶部导航
页面标题与时间范围
AI 决策评分英雄区
检测能力指标
风险总数与风险等级
AI 决策建议
趋势柱状图
指标摘要卡
分析图表区
分项分析标签页
```

必备模块：

- 中央大号评分，例如 80 分、92 分、A级等。
- 评分周围展示检测项、识别项、风险项、工具项等能力指标。
- 左侧展示流量、峰值、处理量等基础指标。
- 右侧展示事件总数、高风险、中风险、低风险数量。
- 中部展示 AI 决策建议或策略更新提醒。
- 下方展示柱状趋势、折线趋势、环形占比、Top 排名。
- 分项分析使用标签页，例如证书安全分析、协议分析、异常分析。

生成约束：

- AI 决策区可使用浅蓝渐变背景，但不得过度发光。
- AI 评分是首屏视觉中心，字号应明显大于其他数值。
- AI 建议必须配合人工确认或查看入口。
- 风险等级必须使用固定色彩映射：高危红色、中危橙色、低危黄色或绿色。

#### 防护分析 / 能力矩阵 Dashboard

用于展示防护覆盖、风险类型、策略执行和安全能力矩阵。

```text
顶部导航
侧边栏
筛选工具条
指标卡片条
防护能力摘要
排行条形图
趋势图 / 雷达图 / 标签云
风险明细表格
防护效果矩阵
```

必备模块：

- 顶部筛选区包含时间、类型、关键词搜索、查询和重置按钮。
- 指标卡片条展示总量、检测量、占比、防护率等。
- 防护能力模块可使用卡片、流程箭头或能力节点表达。
- 排名模块使用横向进度条或条形图。
- 标签云用于展示攻击类型、漏洞家族、风险关键词。
- 表格用于展示设备、漏洞、事件或策略明细。
- 矩阵用于展示不同对象在多个维度下的防护结果。

生成约束：

- 分析类页面允许信息密度略高，但每个模块必须有清晰边界。
- 表格与矩阵区域应置于页面下半部分。
- 矩阵颜色应使用低饱和风险色，不使用刺眼高饱和色块。
- 不使用厚重黑色分割线，模块之间使用浅灰背景间距区隔。

#### 通用数据分析 Dashboard

用于通用业务分析、运营统计、数据报表和管理后台首页。

```text
顶部导航
侧边栏
筛选区
指标卡片区
双列图表区
全宽趋势图
数据表格
分页
```

必备模块：

- 筛选区包含 2–4 个选择器、搜索框、搜索按钮、重置按钮。
- 指标卡片区包含 4 个核心指标，图标 + 标题 + 数值。
- 双列图表区包含一个占比图与一个趋势图。
- 全宽趋势图用于展示长期或细粒度变化。
- 表格展示明细数据，底部包含分页和每页条数选择。

生成约束：

- 页面结构应克制，避免过多装饰。
- 图表标题、图例、tooltip、坐标轴应清晰可读。
- 表格列数控制在 6–8 列，行数控制在 8–10 行。
- 分页置于表格右下方或底部居中偏右区域。

### 安全类 Dashboard 内容字段规范

| 模块    | 推荐字段                           |
| ----- | ------------------------------ |
| 风险概览  | 风险总数、高危数量、中危数量、低危数量、待处理数量      |
| 安全事件  | 事件名称、风险等级、事件类型、处理状态、发生时间、责任人   |
| 资产盘点  | 设备总数、在线数量、在线率、设备类型、操作系统、客户端安装率 |
| 漏洞分析  | 漏洞名称、CVE 编号、影响资产、执行次数、防护率、处置状态 |
| 威胁分析  | 病毒类型、攻击来源、攻击成功告警、拦截次数、清除结果     |
| 资源监控  | CPU 占用率、内存占用率、磁盘占用率、服务状态、运行时长  |
| AI 决策 | 综合评分、检测项、识别项、风险项、策略建议、人工确认入口   |

### Dashboard 色彩与状态映射

| 含义           | 推荐颜色规则                           |
| ------------ | -------------------------------- |
| 主色 / 正常 / 安全 | 科技绿 `#00AB7A` 及其浅色背景             |
| 趋势辅助线        | 蓝色、绿色、橙色按数据系列区分                  |
| 高危 / 严重      | 红色系，配浅红背景                        |
| 中危 / 警告      | 橙色系，配浅橙背景                        |
| 低危 / 提醒      | 黄色系或浅绿色系                         |
| 禁用 / 无数据     | 灰色系                              |
| AI 能力入口      | 蓝—青—绿色渐变，仅用于 AI 按钮、AI 评分、AI 建议入口 |

风险等级、任务状态、设备状态在同一套 Dashboard 中必须保持颜色含义一致。

### Dashboard 文案与假数据规范

| 类型   | 示例                                                  |
| ---- | --------------------------------------------------- |
| 页面标题 | 工作台、分析总览、安全运营看板、服务资源看板                              |
| 时间范围 | 最近 7 天、最近 30 天、2023-08-11 00:00 \~ 2023-08-11 23:59 |
| 风险提示 | 距上次评估已有 0 天，安全体检综合分数 33 分，存在高危风险                    |
| 操作按钮 | 立即体检、查看详情、刷新、搜索、重置、导出                               |
| 事件状态 | 高危、处理中、已修复、请验收、添加信任                                 |
| 设备状态 | 在线、离线、正常、异常、待处理                                     |
| 数据单位 | 个、台、次、条、GB、MB/s、KB/s、%                              |
| 表格操作 | 详情、编辑、处置、忽略、更多                                      |

数值展示应包含单位，不应只出现孤立数字。大数值可使用千分位或“万”作为单位。

### Dashboard AI 生图 Prompt 模板

#### 综合态势工作台 Prompt

```text
生成一张企业级安全态势工作台 Dashboard 高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、右侧主内容区。主内容区从上到下包括：企业信息概览横幅、待办指标、风险告警条、安全状态检测、安全事件列表、资产盘点、运营分析图表、服务资源仪表盘。视觉风格为浅色科技商务风，白色卡片、冷灰背景、科技绿主色，风险使用红色和橙色，AI 能力入口使用蓝—青—绿色渐变。图表包含环形图、横向排名条、折线趋势图和资源仪表盘，文字和数值清晰可读，布局专业克制，不使用游戏风和过度发光效果。
```

#### 实时监控 Dashboard Prompt

```text
生成一张企业级实时监控 Dashboard 高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧分级导航、二级标签栏和刷新按钮。主内容区采用卡片网格布局：总体状态卡、组件状态拓扑图、最近 5 分钟使用率趋势、读写趋势、入口出口流量趋势、容量卡片、数据源排名、底部监控明细表格。卡片白底、轻投影、浅灰页面背景，正常状态用绿色，异常状态用红色，趋势线使用绿色和蓝色。整体信息密度适中，图表标题、坐标、图例和表格字段清晰可读。
```

#### AI 决策分析 Dashboard Prompt

```text
生成一张企业级 AI 决策分析 Dashboard 高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含左侧导航、顶部状态栏、页面标题和时间范围筛选。首屏核心区域为浅蓝科技感 AI 决策评分模块，中间显示大号综合决策评分 80，周围展示检测项、识别项、风险项和工具项，右侧展示事件总数、高风险、中风险、低风险数量，下方有 AI 决策建议和策略更新入口。页面下半部分包含柱状趋势图、指标摘要卡、折线面积图、环形占比图、标签页分析区、Top 排名条形图。风格清爽、企业级、低饱和，AI 元素轻微渐变，不使用强霓虹和复杂 3D。
```

#### 通用数据分析 Dashboard Prompt

```text
生成一张企业级通用数据分析 Dashboard 高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、顶部筛选区、4 个指标卡片、双列图表区、全宽趋势图和底部数据表格。筛选区包含选择器、搜索框、搜索按钮和重置按钮。指标卡使用图标、标题、核心数值和单位。图表包含环形占比图、折线趋势图和面积趋势图。表格包含 8–10 行数据、6–8 列字段和分页。整体为白色卡片与浅灰背景，科技绿主色，图表颜色统一，文字清晰可读。
```

### Dashboard Negative Prompt

```text
不要生成暗黑赛博朋克风，不要游戏化 UI，不要高饱和霓虹色，不要复杂 3D 装饰，不要透视过强，不要设备外框，不要厚重黑色分割线，不要无意义图表，不要乱码文本，不要把图表堆满页面，不要让图例和坐标轴重叠，不要让按钮风格不一致，不要让风险颜色含义混乱，不要把 Dashboard 做成营销落地页。
```

### Dashboard 生成验收标准

| 检查项   | 标准                      |
| ----- | ----------------------- |
| 页面骨架  | 顶部导航、侧边栏、主内容区完整         |
| 首屏重点  | 核心指标、风险、评分或重点图表明确       |
| 阅读路径  | 符合概览 → 分析 → 风险 → 行动     |
| 数据可信度 | 指标、单位、趋势、状态、时间格式合理      |
| 图表匹配  | 图表类型与数据目标一致             |
| 风险表达  | 高 / 中 / 低风险颜色与标签一致      |
| 操作入口  | 搜索、筛选、刷新、查看详情、处理风险等入口清晰 |
| 信息密度  | 不拥挤、不空洞，图表和表格比例协调       |
| 视觉一致性 | 卡片、圆角、投影、图标、颜色统一        |
| 生图质量  | 文字尽量可读，无明显乱码、变形和错位      |

***

***

***

## Dashboard 图表及多分辨率适配规范

> 适用于 Dashboard 图表页、常规内容页、抽屉、弹窗等需要在多分辨率下保持信息完整的界面。

### 适配目标

基于多产线对自适应适配问题的反馈，Dashboard 和常规页面需要建立统一的多分辨率适配规则，以提升产品在主流浏览器分辨率下的展示质量。

**适用范围：**

- 常规页面
- Dashboard 图表页
- 特殊设计页面
- 抽屉
- 弹窗
- 表格
- 异形及非常规图表

### 多分辨率页面适配

| 分辨率    | 适配逻辑                  |
| ------ | --------------------- |
| 1920px | 左侧导航栏固定，右侧内容区域基于内容自适应 |
| 1440px | 左侧导航栏固定，右侧内容区域基于内容自适应 |
| 1366px | 左侧导航栏固定，右侧内容区域基于内容自适应 |
| 1280px | 左侧导航栏固定，右侧内容区域基于内容自适应 |

**侧边栏响应：**

- 侧边栏可设置为响应式布局。
- 浏览器宽度小于配置断点时，侧边导航自动从展开态变为收起态。
- 默认断点值为 `992px`。
- 页面最小宽度为 `768px`。
- 侧边栏收起态宽度为 `56px`。
- 内容区小于 `768px` 时，页面不再缩小，浏览器出现横向滚动条。

### Dashboard 卡片布局适配

Dashboard 采用 12 栅格布局。卡片可按照以下比例进行划分：

| 划分方式 | 适用内容           |
| ---- | -------------- |
| 1    | 大型趋势图、地图、核心工作区 |
| 1/2  | 两个并列图表或左右对比    |
| 1/3  | 三个核心分析模块       |
| 1/4  | 四个指标卡片或小图表     |
| 1/5  | 五个轻量指标卡片       |
| 1/6  | 六个极简状态卡片       |
| 1/12 | 对齐辅助，不建议承载复杂内容 |

**关键原则：**

- 卡片宽度随页面宽度自适应。
- 设计时必须考虑卡片最小宽度。
- 当卡片达到最小宽度后，不应继续压缩内容。
- 宽度不足时优先换行或出现横向滚动，而不是造成信息丢失。

### 图表自适应总原则

```text
图表自适应优先级：
信息完整性 > 信息有效传达 > 页面整齐 > 强行压缩到一屏
```

**执行要求：**

- 图表必须定义最小适配宽度。
- 图表不可因容器缩小而出现标签重叠、图例丢失、坐标轴不可读。
- 当容器宽度小于图表最小宽度时，应出现横向滚动条。
- 图表适配规则不仅适用于页面，也适用于抽屉、弹窗等场景。

### 环形 / 饼图 / 玫瑰图 / 雷达图 / 仪表盘适配

| 项目    | 标准                       |
| ----- | ------------------------ |
| 适用图表  | 环形图、饼图、玫瑰图、雷达图、仪表盘       |
| 适配逻辑  | 图表区域定宽，卡片留白区域基于页面宽度自适应拉伸 |
| 宽度不足  | 小于图表定宽区域时出现横向滚动条         |
| 推荐列数  | 2 列、3 列                  |
| 不推荐列数 | 1 列、4 列及更多               |

**原因：**

- 1 列场景下使用环形图容易显得内容过于松散。
- 4 列及更多列数下，图表内容拥挤，信息传达和美观性都会受影响。
- 2 列和 3 列能较好保证信息展示完整性与有效传达。

### 趋势类图表适配

| 项目    | 标准                                  |
| ----- | ----------------------------------- |
| 适用图表  | 折线图、面积图等趋势类图表                       |
| 适配逻辑  | 图表两端固定宽度，图表内容在保证显示完全的情况下基于页面宽度自适应拉伸 |
| 最小宽度  | 需要定义，避免窄宽度下图表变形或展示不全                |
| 推荐列数  | 1 列、2 列、3 列                         |
| 不推荐列数 | 4 列及更多                              |

**执行说明：**

- 趋势类图表通常需要横向空间表达时间序列。
- 四列及更多列数会导致图表过窄，容易造成信息展示不完整。
- 对折线图、面积图，应优先保证坐标轴、图例、数据点和趋势方向可读。

### 柱形图类图表适配

| 项目    | 标准                                  |
| ----- | ----------------------------------- |
| 适用图表  | 柱状图、分组柱状图、柱形混合图、多柱形图、气泡图            |
| 适配逻辑  | 图表两端固定宽度，图表内容在保证显示完全的情况下基于页面宽度自适应拉伸 |
| 最小宽度  | 需要定义，避免窄宽度下柱体拥挤、标签重叠、图例丢失           |
| 推荐列数  | 优先 1 列、2 列、3 列                      |
| 谨慎列数  | 4 列                                 |
| 不推荐列数 | 4 列以上，尤其是维度较多或分组较多时                 |

**执行说明：**

- 柱形图用于比较，横向空间不足会严重影响标签阅读。
- 多柱形图、分组柱状图对空间要求更高，应减少并列列数。
- 若必须放入窄卡片，应减少维度数量或改用指标卡 / 排名列表。

### 抽屉与弹框适配规则

目前定义三类尺寸，以满足不同场景下的业务需求：

| 类型    |        宽度 | 适用场景                  |
| ----- | --------: | --------------------- |
| 小尺寸   |     560px | 简单查看、轻量编辑、确认信息        |
| 中尺寸   |     880px | 表单编辑、详情查看、多字段配置       |
| 自适应尺寸 | 屏幕宽度的 80% | 复杂配置、图表详情、批量处理、较大内容承载 |

**执行要求：**

- 抽屉和弹窗中的图表也必须遵循图表最小宽度规则。
- 内容复杂时优先使用 80% 屏幕宽度，而不是在 560px 内强行压缩。
- 弹窗/抽屉的操作区应固定在底部或顶部可见区域，避免用户滚动后找不到操作按钮。

### 表格适配规则

表格整体基于页面宽度自适应拉伸，优先缩放列表空白区域。

| 阶段     | 适配方式                  |
| ------ | --------------------- |
| 页面宽度充足 | 表格整体随页面宽度拉伸           |
| 页面宽度缩小 | 优先压缩列之间的空白区域          |
| 空白区域消失 | 固定表头和操作列，中间区域基于屏幕宽度拉伸 |
| 仍然不足   | 表格底部出现左右横向滚动条         |
| 无多选表头  | 可取消固定表头中的多选区域         |

**强制要求：**

- 表格不能为了适配而压缩到字段不可读。
- 操作列需要优先固定，避免用户滚动后找不到操作。
- 批量选择列、关键识别列、状态列可根据业务优先级选择固定。
- 横向滚动条出现时，应确保表头与内容列对齐。

### 异形及非常规图表适配规则

异形及非常规图表由于结构特殊，不适合用单一规则机械套用，应进行宏观约束。

| 规则          | 说明                |
| ----------- | ----------------- |
| 定义最小适配宽度    | 小于最小宽度时出现横向滚动条    |
| 基于最小宽度自适应拉伸 | 需要明确标注拉伸区域        |
| 常规拉伸方式      | 两侧留白区域或内容区进行自适应拉伸 |
| 核心原则        | 优先保证信息展示完整和有效传达   |

**设计交付要求：**

- 必须在设计稿中标注最小宽度。
- 必须标注可拉伸区域和不可拉伸区域。
- 必须说明低于最小宽度时的表现方式。
- 如果图形不可压缩，应直接定义横向滚动，而不是交给前端临时处理。

### Dashboard 适配验收清单

| 检查项                                   | 是否必须 |
| ------------------------------------- | ---- |
| 是否适配 1920 / 1440 / 1366 / 1280 四类常规宽度 | 必须   |
| 是否定义侧边栏展开/收起规则                        | 必须   |
| 是否明确 992px 断点                         | 必须   |
| 是否明确页面最小宽度 768px                      | 必须   |
| 是否明确收起侧边栏宽度 56px                      | 必须   |
| Dashboard 卡片是否遵循 12 栅格                | 必须   |
| 每类图表是否定义最小宽度                          | 必须   |
| 图表低于最小宽度时是否出现横向滚动                     | 必须   |
| 表格是否优先压缩空白区域而非字段内容                    | 必须   |
| 操作列是否在横向滚动时可见                         | 建议必须 |
| 抽屉/弹框是否使用 560 / 880 / 80% 三类宽度        | 必须   |

***

# 列表页模板

## 列表页页面模板规范

列表页用于承载数据检索、筛选、批量操作、状态识别、单行处理和分页浏览。列表页的核心目标是提升查找效率、判断效率和处理效率，视觉上应保持克制、清晰、规整，不应被设计成数据看板或营销页面。

### 列表页通用结构

标准列表页应遵循以下结构：

```text
页面标题 / 面包屑 / 顶部标签
  ↓
查询区 / 高级筛选区
  ↓
工具栏 / 批量操作区 / 右侧功能按钮
  ↓
表格主体
  ↓
分页区
```

复杂列表页可扩展为：

```text
页面标题 / 标签页
  ↓
查询区
  ↓
左侧分类筛选栏 + 右侧结果表格
  ↓
表格工具栏 / 字段配置
  ↓
分页区
```

通用规则：

- 页面背景使用浅灰，主体卡片使用白色。
- 查询区、工具栏、表格区、分页区应分层清楚。
- 表格始终是列表页的核心主体。
- 列表页不应加入大面积图表或装饰插图。
- 首屏应保证搜索、筛选、表头、核心字段、操作列和分页可见。
- 表格信息密度中高，但不应拥挤。

### 列表页类型

#### 标准表格列表页

适用于常规记录、配置项、对象列表。

页面结构：

```text
页面标题
  ↓
工具栏
  ↓
表格
  ↓
分页
```

典型元素：

- 主操作按钮
- 次级操作按钮
- 搜索框
- 表格字段
- 行操作
- 分页

#### 查询结果列表页

适用于事件查询、日志检索、任务记录、风险结果。

页面结构：

```text
查询条件区
  ↓
结果总数 / 导出按钮
  ↓
结果表格
  ↓
分页
```

典型元素：

- 输入框
- 下拉选择器
- 日期范围
- 查询按钮
- 重置按钮
- 导出按钮
- 结果总数

#### 左筛选 + 右列表页

适用于分类、类型、层级或资产目录明显的业务。

页面结构：

```text
顶部快捷筛选
  ↓
左侧分类筛选栏 + 右侧结果表格
```

左侧筛选可包含：

- 类别
- 类型
- 层级
- 数量
- 展开 / 收起
- 当前选中项

右侧表格可包含：

- IP
- 域名
- 协议
- 端口
- 状态码
- 产品应用
- 发现时间
- 操作

#### 高级搜索列表页

适用于字段多、查询条件复杂、支持 HQL 或字段配置的列表。

页面结构：

```text
高级查询表单
  ↓
展开 / 收起
  ↓
查询 / 清空 / HQL
  ↓
工具栏
  ↓
表格
  ↓
字段配置浮层
  ↓
分页
```

典型元素：

- 多行筛选条件
- 展开收起按钮
- 下载按钮
- 字段配置按钮
- 字段勾选浮层
- 横向滚动条
- 多选表格

#### 标签切换列表页

适用于按状态或集合切换的数据列表。

页面结构：

```text
页面标题
  ↓
标签页
  ↓
表格
  ↓
分页
```

规则：

- 标签数量建议 2–5 个。
- 当前标签使用科技绿高亮。
- 标签用于切换顶层集合，不替代复杂筛选。

### 查询区规范

查询区用于缩小结果范围，应保持整齐、清晰、低干扰。

常见控件：

- 输入框
- 下拉选择器
- 日期范围选择器
- 时间范围选择器
- 搜索按钮
- 重置按钮
- 展开 / 收起
- HQL 入口

布局规则：

- 简单查询一行展示 3–5 个字段。
- 复杂查询可使用 2–3 列栅格。
- 查询按钮使用主按钮。
- 重置按钮使用次按钮。
- 展开高级查询时应保持字段对齐。
- 查询区不应占据过多首屏空间。
- 日期范围应采用“开始日期 ～ 结束日期”结构。

### 工具栏规范

工具栏用于承载批量操作、页面级操作和表格配置。

常见操作：

- 联动处置
- 联动记录
- 导出列表
- 下载
- 刷新
- 字段配置
- 批量选择
- 删除
- 复制
- 新建

规则：

- 左侧优先放批量操作。
- 右侧优先放创建、导出、设置、刷新等页面操作。
- 主操作按钮最多 1 个。
- 次级操作建议 2–4 个。
- 批量操作在未选择数据时应为禁用态。
- 工具栏与表格之间应有清晰分隔。

### 表格主体规范

表格是列表页最核心的信息区。

基本规则：

- 表头使用浅灰底。
- 行高默认 40px。
- 字号建议 12px 或 14px。
- 文字左对齐优先。
- 数字、状态、操作可按需要居中。
- 表格分割线使用浅灰细线。
- 空值使用 `-`。
- 操作列置于最右侧。
- 超长内容使用省略号，必要时配合 tooltip。
- 列较多时支持横向滚动。

常见字段：

- 选择框
- 序号
- 名称
- 类型
- 状态
- 风险等级
- IP
- 域名
- 端口
- 协议
- 时间
- 标签
- 操作

### 状态与标签规范

状态应同时依靠颜色和文字表达。

处理状态：

- 已处理：绿色
- 未处理：橙色 / 黄色
- 未接通：灰色
- 处理中：蓝色或科技绿弱态
- 已失败：红色

风险等级：

- 高风险 / 严重：红色
- 中风险 / 警告：橙色
- 低风险 / 提醒：黄色
- 安全 / 正常：绿色

标签规则：

- 标签文字建议 2–6 字。
- 同一状态在同一产品内颜色保持一致。
- 一个单元格内不宜堆叠过多彩色标签。
- 普通类型标签使用灰色描边或浅色底。

### 行操作规范

常见行操作：

- 查看
- 查看详情
- 编辑
- 处理
- 删除
- 启用
- 停用
- 复制
- 下载

规则：

- 行操作置于最右侧。
- 单行高频操作建议 1–3 个。
- 低频操作收进更多菜单。
- 删除、停用、取消等危险操作应弱化或二次确认。
- 操作链接颜色以主色为主，不要多色混用。

### 批量操作规范

- 表格首列使用复选框。
- 支持全选当前页。
- 选中后应展示“已选 N 个”。
- 未选中数据时批量操作禁用。
- 批量删除、禁用、导出等操作需有反馈。
- 高风险批量操作应确认影响数量。

### 字段配置浮层规范

复杂列表可提供字段配置能力。

字段配置结构：

```text
字段配置入口
  ↓
下拉浮层 / 抽屉
  ↓
全选 / 重置
  ↓
字段勾选列表
```

规则：

- 字段配置入口位于表格右上方。
- 浮层应靠近触发按钮。
- 支持全选、重置、勾选、禁用字段。
- 默认不可关闭字段应展示禁用态。
- 字段名称应与表头一致。
- 配置变更后即时影响表格列展示。

### 左侧分类筛选规范

左侧分类筛选用于快速缩小结果范围。

结构：

```text
分类组标题
  ↓
分类项 + 数量
  ↓
展开 / 收起
```

规则：

- 分组标题如类别、类型、层级。
- 每组支持展开 / 收起。
- 当前选中项使用主色或浅绿色高亮。
- 数量右对齐。
- 内容过长使用省略号。
- 左筛选栏与右侧表格之间使用竖向分隔。

### 分页规范

分页位于表格底部，通常靠右。

分页应包含：

- 总条数
- 每页条数
- 上一页 / 下一页
- 页码
- 跳页输入

规则：

- 当前页使用主色高亮。
- 每页条数建议支持 10 / 20 / 50。
- 总条数格式如 `共计 4568 条`。
- 列表页不应缺少分页，除非数据量极少。
- 分页与表格之间应留出清晰间距。

### 列表页信息密度规范

推荐值：

- 表格列数：6–10 列
- 首屏行数：8–12 行
- 查询字段：3–8 个
- 标签页数量：2–5 个
- 工具按钮：2–5 个
- 单行操作：1–3 个

控制原则：

- 表格可密，但不能乱。
- 查询区不应比表格更抢眼。
- 普通列表不应加入复杂图表。
- 主信息与辅助信息应有层级差异。

### 列表页文案与假数据规范

- 页面标题：4–12 字。
- 查询标签：2–6 字。
- 按钮文案：2–6 字。
- 表头字段：2–8 字。
- 行操作文案：2–6 字。
- 标签文案：2–6 字。
- 数量格式：`11,360 条`。
- 时间格式：`2024/05/29 15:30:12`。
- IP 格式：`172.171.49.45`。
- 域名格式：`example.global.pr`。
- 状态示例：`已处理 / 未处理 / 未接通`。
- 风险示例：`高风险 / 中风险 / 低风险`。

### 列表页 AI 生图约束

- 必须明确列表页类型后再生成。
- 表格面积应明显大于查询区。
- 不要把列表页做成 Dashboard。
- 不要在列表页中加入大面积图表。
- 查询区必须整齐排列。
- 工具栏主次操作必须清楚。
- 表格必须包含表头、数据行、状态/标签、操作列。
- 分页必须完整显示。
- 如果有左侧筛选，必须展示分类分组和选中状态。
- 如果有字段配置，必须展示浮层或抽屉配置面板。
- 不要使用过重阴影、粗边框或厚重黑色分割线。

### 列表页 Prompt 模板

#### 标准列表页 Prompt

```text
生成一张企业级 B 端后台标准列表页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、页面标题、查询区、工具栏、白色表格卡片和分页。查询区包含输入框、下拉选择器、搜索按钮和重置按钮。工具栏左侧包含批量操作按钮，右侧包含创建、导出和设置按钮。表格包含 8–10 列字段、10 行左右数据、状态标签、风险标签和最右侧操作列。底部包含总条数、每页条数、页码和跳页。整体为浅灰背景、白色内容区、科技绿主色、低饱和企业级后台风格。
```

#### 左筛选列表页 Prompt

```text
生成一张企业级 B 端后台左筛选列表页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧系统导航和主内容区双栏布局。主内容区左侧为分类筛选栏，分为类别、类型、层级等分组，每组支持展开收起并显示数量；右侧为结果表格，表格上方有关键词筛选和下拉选择器，表格中包含 IP、域名、协议、端口、状态码、产品应用、发现时间等字段，底部有总条数、每页条数和分页器。风格为白底浅灰背景、主色科技绿、数据密度中高、适合安全类业务。
```

#### 高级搜索列表页 Prompt

```text
生成一张企业级 B 端后台高级搜索列表页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、标题区、大型高级筛选表单区和下方表格结果区。筛选区包含多行输入框、下拉选择器、日期选择器、展开收起控件、查询按钮、清空按钮和 HQL 按钮。表格上方有下载按钮、字段配置按钮和批量操作入口，表格包含多选框、时间列、名称列、告警等级标签列、IP 列、操作列，右侧展开字段配置浮层。底部包含总条数、每页条数和页码分页器。整体简洁、高效、可读性强。
```

### 列表页 Negative Prompt

```text
不要生成大面积图表，不要把列表页做成工作台，不要使用过强透视，不要加入手机或电脑设备外框，不要使用厚重黑边和粗分割线，不要文字乱码，不要让表格列间距混乱，不要让查询区和表格区层级不清，不要用过多颜色表达普通状态，不要把所有操作都做成高强调按钮，不要缺失分页器、结果总数和操作列。
```

### 列表页生成验收标准

| 检查项    | 标准                   |
| ------ | -------------------- |
| 页面骨架   | 标题、查询区、工具栏、表格区、分页区完整 |
| 查询效率   | 查询项布局整齐，查询与重置清晰      |
| 工具栏    | 主次操作分明，数量适中          |
| 表格可读性  | 字段清楚、行高统一、标签识别明确     |
| 状态表达   | 颜色含义统一，风险和处理状态不混淆    |
| 行操作    | 高频操作清晰，危险操作需谨慎处理     |
| 分页完整性  | 总数、每页条数、页码、跳页完整      |
| 左筛选可用性 | 分组清晰，数量可见，选中状态明确     |
| 字段配置   | 浮层结构清晰，可全选、重置、勾选     |
| 生图质量   | 文字基本可读，无明显变形、错位、乱码   |

***

## 列表页基础原则补充

> 列表页是 B 端产品中最高频的信息浏览与处理页面之一，应优先考虑屏效、阅读效率和批量操作效率。

### 列表页定义

列表页用于查看和处理大量条目，常承担导航至详情页的作用。列表也是组织和整理数据的重要手段，便于用户对数据进行筛选、对比、新增、分析，并下钻至条目完整详情页。

典型场景包括：

- 安全事件列表页
- 策略列表页
- 日志列表页
- 任务列表页
- 智能体列表页
- 审批列表页
- 项目列表页

### 列表页当前痛点

| 痛点       | 说明                                  |
| -------- | ----------------------------------- |
| 布局间距不一致  | 不同产品、页面、模块中的列表间距存在差异，导致视觉节奏不统一      |
| 屏效较低     | 列表承载客户浏览、查找、管理数据的核心任务，低屏效会直接影响操作效率  |
| 信息布局松散   | 操作区、筛选区、表格、分页之间距离过大，会降低单位屏幕的信息承载能力  |
| 字号和行高不统一 | 12px / 14px、40px / 48px 并存，影响列表阅读节奏 |

### 列表页设计目标

| 目标       | 说明                        |
| -------- | ------------------------- |
| 统一列表布局间距 | 保持不同页面和功能模块的列表在视觉上保持一致    |
| 优化列表屏效   | 在有限屏幕空间内容纳更多有效信息          |
| 提升信息获取效率 | 帮助用户快速浏览、查找、定位和处理条目       |
| 降低认知负荷   | 通过清晰的信息分层和操作分组，减少用户反复判断成本 |

### 列表页设计原则

| 原则  | 说明                            | 执行方式                       |
| --- | ----------------------------- | -------------------------- |
| 易读性 | 提取用户需要感知的重点信息优先展示             | 重点字段前置，弱化次要字段；复杂信息通过交互分层展示 |
| 高效性 | 符合用户浏览的视觉逻辑顺序，支持快速筛选、新增、删除、处置 | 操作区固定位置；筛选与批量操作清晰；列表密度更紧凑  |

### 列表页类型与适用场景

| 类型      | 构成                     | 使用场景                    |
| ------- | ---------------------- | ----------------------- |
| 列表-基础类  | 基础表格 + 常规操作按钮 + 少量筛选条件 | 全场景通用；适合常规数据管理          |
| 列表-筛选   | 表格 + 筛选项 + 分类/状态/来源等条件 | 需要对数据进行分类筛选、溯源和寻找原因     |
| 列表-卡片   | 多列卡片布局，信息聚合展示          | 资源列表、模板列表、智能体/专家列表、项目列表 |
| 列表-分析   | 统计分析组件 + 列表，呈上下结构      | 列表数据需要统计和分析             |
| 列表-说明引导 | 顶部带场景说明或引导说明           | 场景复杂、用户需要先理解规则或流程       |
| 列表-分组   | 左右分组或树形分组 + 列表         | 组织架构树、虚拟组树、分类资源管理       |
| 列表-展开列表 | 行内展开扩展信息               | 系统版本较老或只需快速查看当前数据条的扩展信息 |

### 列表尺寸与间距

#### 纵向屏效优化

| 项目         | 标准                               |
| ---------- | -------------------------------- |
| 表头行高       | 40px                             |
| 单元格行高      | 40px                             |
| 列表文字字号     | 12px                             |
| 卡片间距       | 由 16px 统一压缩为 12px；部分卡片列表可使用 16px |
| 列表与上方操作区间距 | 8px                              |
| 列表与下方分页间距  | 8px                              |

#### 横向屏效优化

| 项目        | 标准                                             |
| --------- | ---------------------------------------------- |
| 操作区按钮高度   | 32px                                           |
| 操作区按钮左右间距 | 8px                                            |
| 筛选项紧凑间距   | 无标题筛选项左右间距 8px                                 |
| 带小标题筛选项   | 小标题字号 12px；小标题距离选项 12px；筛选项左右间距 32px，上下间距 16px |

### 基础列表默认样式

| 元素     | 标准   |
| ------ | ---- |
| 表头高度   | 40px |
| 单元格高度  | 40px |
| 列表字号   | 12px |
| 操作按钮高度 | 32px |
| 操作按钮间距 | 8px  |
| 表格距操作区 | 8px  |
| 表格距分页  | 8px  |

**执行说明：**

- 基础列表应优先保证信息扫描效率，不应使用过大的行高。
- 操作区应与列表形成明确关系，不能距离过远。
- 分页应紧贴列表底部，但需保留 8px 呼吸空间。
- 如果列表承载大量数据，应优先使用 40px 行高，而不是 48px 行高。

### 筛选区规范

| 场景       | 标准          |
| -------- | ----------- |
| 无标题筛选项   | 筛选项左右间距 8px |
| 有小标题筛选项  | 小标题字号 12px  |
| 小标题与选项距离 | 12px        |
| 筛选项左右间距  | 32px        |
| 筛选项上下间距  | 16px        |

**设计要求：**

- 筛选项不宜堆叠得过松，否则会吞噬列表首屏空间。
- 高频筛选项前置，低频筛选项可收纳到“更多筛选”。
- 筛选区与操作区需要区分，不要把筛选、批量操作、主按钮混在一起。

### 卡片列表规范

| 项目    | 标准                    |
| ----- | --------------------- |
| 卡片间距  | 12px / 16px           |
| 卡片内容区 | 应保持信息聚合，不应把表格字段机械搬进卡片 |
| 卡片操作  | 高频操作可外露，低频操作收纳        |
| 适用对象  | 项目、模板、智能体、专家、数据资产、策略包 |

**AI 原生平台补充：**

在多智能体平台中，“我的专家”“专家市场”“项目模板”等页面更适合使用卡片列表。卡片应突出：名称、来源、状态、能力标签、使用次数、成功率、最近更新时间、主要操作。

### 分析型列表规范

| 项目           | 标准                |
| ------------ | ----------------- |
| 统计分析区与上下卡片间距 | 12px              |
| 分析区位置        | 列表上方              |
| 分析区内容        | 关键指标、趋势、分类统计、风险分布 |
| 列表关系         | 分析区用于概览，列表用于下钻处理  |

**执行要求：**

- 统计分析区不能过高，避免挤压列表首屏。
- 图表只展示与列表筛选和数据判断相关的指标。
- 分析区和列表应共享同一筛选上下文。

### 说明引导型列表规范

| 项目         | 标准                   |
| ---------- | -------------------- |
| 说明引导距离下方内容 | 12px                 |
| 使用场景       | 场景复杂、规则较多、用户需要理解任务背景 |
| 展示方式       | 顶部说明卡片、轻量提示条、步骤说明    |

**执行要求：**

- 说明引导不能替代页面标题。
- 引导内容应解释任务规则，而不是写产品宣传语。
- 引导卡片可折叠，避免长期占用首屏空间。

### 分组列表规范

| 项目      | 标准                        |
| ------- | ------------------------- |
| 列表与分组间距 | 16px                      |
| 适用场景    | 组织架构树、虚拟组树、分类资源、项目频道      |
| 布局方式    | 左侧分组树 + 右侧列表，或顶部分类 + 下方列表 |

**执行要求：**

- 分组区应帮助用户缩小数据范围。
- 分组树宽度不宜过大，避免挤压表格。
- 分组切换后，右侧列表应即时刷新并保留清晰的当前筛选状态。

### 展开列表规范

| 项目   | 标准                |
| ---- | ----------------- |
| 展开方式 | 当前行向下展开           |
| 展开内容 | 当前数据条的扩展数据        |
| 使用条件 | 快速查看补充信息，不需要进入详情页 |
| 限制   | 不适合承载复杂编辑流程       |

***

***

# 详情页模板

## 详情页页面模板规范

详情页用于承载单个对象、事件、任务、资产、模型、风险、报告或分析结果的完整信息。详情页的核心目标是帮助用户完成“看清对象状态、理解关键原因、定位关联信息、执行下一步操作”。

详情页分为两大类：

```text
抽屉式详情
  ↓
适合从列表页或监控页快速查看，不打断主流程

独立页面详情
  ↓
适合信息量较大、层级较深、需要持续阅读或综合分析的场景
```

### 详情页通用信息结构

详情页应按以下顺序组织信息：

```text
标题区 / 对象名称 / 状态标签 / 操作区
  ↓
基础信息 / 摘要信息 / 关键指标
  ↓
核心分析区 / 对比区 / 图表区 / 时间线区
  ↓
详细字段 / 描述表格 / 关联数据
  ↓
操作建议 / 处置动作 / 导出 / 返回
```

复杂详情页可采用：

```text
顶部对象摘要
  ↓
标签页 / 锚点导航
  ↓
分组内容模块
  ↓
图表 + 表格 + 说明文本组合
  ↓
底部关联记录 / 空状态 / 操作建议
```

### 抽屉式详情规范

抽屉式详情适用于从列表、告警、事件、监控卡片中快速打开详情。背景页面应保留上下文，并通过遮罩弱化。

#### 抽屉布局

- 抽屉从页面右侧滑出。
- 宽度可根据内容复杂度设置为 `560px / 880px / 80%`。
- 信息量较大的分析型详情可使用接近全屏的大抽屉。
- 背景页面应添加半透明遮罩，保持用户对来源页面的上下文感知。
- 抽屉顶部固定显示标题、状态标签和关闭按钮。
- 抽屉内容区纵向滚动，顶部标题区不随滚动消失。
- 抽屉底部如有确认、处置、导出操作，可使用固定底部操作栏。

#### 抽屉标题区

标题区应包含：

- 对象名称
- 风险等级或状态标签
- 关闭按钮
- 可选操作按钮，如导出详情、处置、查看原始数据

标题示例：

- `10.17.3.30的异常SSL检测分析详情`
- `这是标题`
- `模型评估详情`
- `风险事件详情`

状态标签示例：

- 严重
- 高危
- 风险
- 安全
- 拒识
- AI 评估

#### 抽屉内容区

抽屉内容应按模块分段，每段使用小标题和左侧主色竖线加强识别。

推荐模块：

- 综合决策评估
- 基本信息
- 应用层模型评估
- 传输层模型评估
- 流信息评估
- 对比分析
- 处置建议
- 关联记录

### 分析评估抽屉详情模板

适用于异常流量、SSL 检测、通信检测、模型评分、安全策略分析等详情。

#### 页面结构

```text
抽屉标题区：对象名称 + 严重程度标签 + 关闭按钮
  ↓
综合决策评估：仪表盘评分 + 关键特征标签 + 流量指标卡
  ↓
基本信息：描述表格
  ↓
应用层模型评估：热力图 + 排名条形图
  ↓
传输层模型评估：雷达图 + 指标表
  ↓
流信息评估：描述表格 + 指标统计卡片
```

#### 综合决策评估区

- 左侧放置半圆仪表盘或评分仪表，突出模型评估分值。
- 中间展示关键特征标签，如“双向流 / 有证书 / 握手异常”。
- 右侧展示关键流量指标卡，如通信次数、上行包数、下行包数、上行负载、下行负载、包长度等。
- 指标卡建议横向排列，单个卡片只展示一个核心数值。
- 评分数值应大号展示，并包含分值单位，如 `75 分`。

#### 基本信息区

基础信息建议采用描述表格，字段可包括：

- 客户端 IP
- 客户端端口
- 服务端 IP
- 服务端端口
- 协议类型
- 网络流量
- 异常特征
- 会话时间
- 研判标签

描述表格规则：

- 每行 2–4 组字段。
- 字段名使用浅灰底。
- 字段值保持左对齐。
- 标签或可编辑字段可使用描边标签或输入态样式。
- 上行 / 下行数据可配合方向箭头表达。

#### 应用层模型评估区

可使用以下图表组合：

- 热力图：展示特征区间、模型评分、风险分布。
- 横向条形图：展示 Top 证书、Top 特征、Top 风险项。
- Tooltip：用于解释某个格子的评分或类别。

热力图规范：

- 使用主色渐变表示强弱。
- 每个格子显示数值。
- 图例位于图表上方。
- 鼠标悬停时显示短 tooltip。
- 不使用强烈彩虹色或复杂 3D 效果。

横向条形图规范：

- 排名数量建议 5–8 项。
- 条形使用主色或主色浅色系。
- 右侧展示数值。
- 排名从高到低排列。

#### 传输层模型评估区

可使用雷达图 + 指标表组合。

雷达图适合展示：

- 数据包个数
- 载荷最大值
- 子流载荷均值
- 数据包 window size
- 子流个数
- 上行流 / 下行流对比

规则：

- 雷达图系列不超过 2–3 条。
- 坐标维度建议 5–7 个。
- 右侧表格展示精确指标值。
- 图表与表格保持左右平衡布局。

#### 流信息评估区

流信息区适合使用“描述表格 + 统计卡片”组合。

描述表格可包括：

- 支持版本
- 通信版本
- 套件数量
- 选择套件
- 源 IP
- 目的 IP
- 威胁描述
- 证书信息
- 证书有效期

统计卡片可包括：

- 载荷信息均值
- 包长信息均值
- 流时长
- 发包速率
- 收包速率
- 最小值 / 最大值

统计卡片规则：

- 图标置于左侧。
- 核心数值放大显示。
- 最小值和最大值作为辅助指标。
- 颜色可根据指标类型使用蓝、绿等低饱和色。

### 对比分析抽屉详情模板

适用于 AI 模型评估、算法对比、检测结果对比、评分结果对比。

#### 页面结构

```text
抽屉标题区：标题 + 关闭按钮
  ↓
基础信息区：时间、任务、数据集、模型名称、数量等
  ↓
操作区：导出详情
  ↓
对比分析表：维度列 + 多模型对比列
```

#### 基础信息区

- 基础信息区可使用浅蓝渐变背景，弱化装饰并突出信息分组。
- 信息字段建议 2–3 列排列。
- 重要字段可使用可复制、可点击或选中态样式。
- 导出详情按钮位于右上方。

基础字段示例：

- 时间
- 时间范围
- 数据集名称
- 模型名称
- 任务数量
- 评估对象

#### 对比分析表

对比表应采用横向模型对比结构：

```text
维度名称 / 指标名称
  ↓
模型 1 / 模型 2 / 模型 3 / 模型 4
```

每个模型列可包含：

- 模型图标
- 模型名称
- AI 评估标签
- 核心得分
- 时间
- 各项比例
- 维度指标

特殊状态：

- 最高分使用蓝色或绿色高亮。
- 最低分使用红色浅底高亮。
- 安全、拒识、风险等标签放在左侧维度区域。
- 同一行不同模型的数据应严格横向对齐。

#### 视觉规则

- 对比表列宽应一致。
- 分割线使用浅灰细线。
- 最高 / 最低标识只标注最关键位置，不要重复过多。
- 对比页不应使用过多图表，重点是表格对齐与可比较性。

### 独立详情页规范

独立详情页适合承载完整业务对象的信息，通常由面包屑、对象摘要、分组模块、图表、表格和操作建议组成。

#### 独立详情页结构

```text
顶部导航 / 侧边导航
  ↓
面包屑 / 页面标题
  ↓
对象摘要区
  ↓
标签页 / 锚点导航 / 分组导航
  ↓
详情内容区
  ↓
关联数据 / 表格 / 日志 / 空状态
```

#### 使用场景

- 风险事件详情
- 资产详情
- 模型详情
- 下载行为详情
- 安全评估详情
- 报告型详情
- 运行状态详情
- 消息推送详情

### 时间轴型详情页模板

适用于风险行为、事件过程、攻击链、操作记录、异常轨迹等。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
面包屑
  ↓
标题区：对象名称 + 状态标签 + 操作按钮
  ↓
摘要信息区
  ↓
风险行为时间轴
  ↓
时间节点卡片
  ↓
展开详情表格
  ↓
嵌套表格 / 关联记录
```

#### 标题与摘要区

标题区应包括：

- 对象名称
- 风险标签
- 次要按钮
- 主按钮
- 操作按钮组

摘要区可包括：

- 事件名称
- 时间
- 对象信息
- 用户信息
- 部门信息
- 风险描述

#### 时间轴规则

- 时间轴左侧使用时间点标记。
- 每个节点使用卡片承载行为摘要。
- 节点卡片中可包含标签、字段组、描述信息。
- 节点详情可支持展开 / 收起。
- 展开后展示描述表格或关联表格。
- 多个节点之间保持统一间距。
- 当前重点节点可使用主色或风险色强调。

#### 展开详情规则

- 默认展示摘要信息。
- 点击“展开详情”后展示表格式字段。
- 复杂数据可使用嵌套表格。
- 展开状态应有箭头方向变化。
- 收起后保留节点摘要与时间信息。

### 图文概览型详情页模板

适用于风险对象、文件对象、告警对象、资产对象的图文摘要型详情。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题 / 面包屑
  ↓
对象摘要卡片：插图 / 状态 / 标题 / 标签 / 关键字段 / 操作按钮
  ↓
标签页
  ↓
指标卡片 + 排名图表
  ↓
筛选区 + 表格
  ↓
分页
```

#### 对象摘要卡片

摘要卡片可包含：

- 左侧对象插图或图标
- 风险等级文字，如“高危”
- 主标题
- 标签组
- 次要信息卡片
- 描述字段
- 右上角主次操作按钮

规则：

- 插图不应过大，避免压缩信息区。
- 风险等级应与状态颜色一致。
- 标签数量建议 2–4 个。
- 主操作位于右上角。
- 摘要信息最多分为 2–3 列。

#### 标签页内容区

标签页用于组织不同维度信息，如：

- 概览
- 关联数据
- 处置记录
- 影响范围
- 日志记录

标签页规则：

- 激活标签使用主色高亮。
- 标签数量建议 3–6 个。
- 标签页下方模块应根据当前标签内容变化。
- 不同标签页的头部摘要保持一致。

### 资产画像型详情页模板

适用于资产、设备、主机、节点、服务、系统状态详情。

#### 页面结构

```text
返回 / 面包屑 / 标题
  ↓
顶部标签页或对象切换栏
  ↓
横向指标摘要
  ↓
资产基础卡片
  ↓
能力 / 状态 / 配置模块
  ↓
描述表格
  ↓
关联消息表格 / 空状态
  ↓
右侧锚点导航
```

#### 指标摘要区

- 横向展示多个对象或指标。
- 每项包含图标、标题和数值。
- 当前选中项使用浅绿色背景。
- 指标数量较多时可横向滚动或均分展示。
- 数值应大于标题，形成视觉重点。

#### 资产基础卡片

资产基础卡片可包含：

- 资产图标
- 资产名称
- 更新时间
- 用户名 / 用户组
- 操作图标
- 平台或系统标签
- 详情入口

#### 状态与配置模块

适合展示：

- 资源占比
- CPU / 内存 / 存储状态
- 硬件信息
- 软件信息
- 系统功能图标
- 模块启用状态

规则：

- 左侧可放进度条指标。
- 中间可放图标宫格。
- 右侧可放锚点导航。
- 锚点导航用于快速跳转基本信息、硬件信息、消息推送等模块。

#### 空状态

当关联消息或记录为空时，应显示：

- 轻量空状态图标
- “暂无数据”文案
- 空状态居中显示
- 不使用过强插图或大面积空白装饰

### 报告型长详情页模板

适用于安全评估报告、风险分析报告、运营分析报告、巡检详情、综合分析详情。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题 + 操作按钮
  ↓
标签路径 / 顶部页签 / 下拉菜单
  ↓
信息提示 / 风险提示条
  ↓
指标摘要行
  ↓
分组模块 1
  ↓
分组模块 2
  ↓
Top 排名表格
  ↓
日志表格
  ↓
安全建议
```

#### 长页面分组规则

- 长详情页必须使用清晰的分组标题。
- 每个分组之间保持足够垂直间距。
- 重要提示使用浅黄色提示条。
- 风险提示使用红色图标或红色文字，但不宜整块大红。
- 分组标题可使用主色竖线增强识别。
- 重复型模块应保持相同结构，方便阅读。

#### 报告型模块

可包含：

- 标题说明
- 提示条
- 指标卡
- Top10 表格
- 日志表格
- 风险统计
- 安全加固建议
- 应急响应建议

#### Top 表格规则

- 排名建议展示 5–10 条。
- 第一列为序号。
- 中间列为对象名称或 IP。
- 最右列为数量或风险值。
- 小型 Top 表格可不展示分页。
- 长报告中多个 Top 表格应保持列结构一致。

### 详情页信息分组规范

详情页内容应按业务意义进行分组，不应简单堆叠字段。

推荐分组：

- 基础信息
- 风险信息
- 评分信息
- 模型评估
- 流量信息
- 证书信息
- 资产信息
- 时间线
- 对比分析
- 关联记录
- 操作建议
- 日志记录

分组标题规则：

- 标题简短，建议 4–10 字。
- 可使用主色竖线。
- 分组之间留白明显。
- 同一页面分组标题样式保持一致。

### 详情页图表使用规范

详情页图表服务于解释单个对象，不应像 Dashboard 一样堆叠大量概览图。

推荐图表：

- 仪表盘：用于评分。
- 热力图：用于特征分布。
- 雷达图：用于多维能力或特征对比。
- 条形图：用于 Top 排名。
- 折线图：用于趋势。
- 进度条：用于资源占比。
- 时间线：用于事件过程。

图表数量规则：

- 抽屉式详情中建议 2–4 个图表。
- 独立详情页中建议 3–6 个图表。
- 长报告详情可使用多个小型表格和指标卡替代复杂图表。
- 单个模块内图表类型不宜超过 2 种。

### 详情页表格规范

详情页中的表格分为描述表格、数据表格和对比表格。

#### 描述表格

用于展示对象属性：

- 字段名 + 字段值成对出现。
- 字段名可使用浅灰背景。
- 字段值保持左对齐。
- 每行建议 2–4 组字段。
- 空值使用“-”。

#### 数据表格

用于展示关联记录：

- 结构可参考列表页表格。
- 行数不宜过多，必要时跳转完整列表。
- 可展示 Top5 / Top10。
- 可保留简化分页。

#### 对比表格

用于多模型、多对象、多版本比较：

- 第一列为维度或指标名。
- 后续列为对比对象。
- 高亮最高值、最低值或异常值。
- 各列对齐严格一致。
- 单元格内避免堆叠过多标签。

### 详情页操作规范

详情页操作分为页面级操作、模块级操作和行级操作。

#### 页面级操作

常见操作：

- 返回
- 关闭
- 导出详情
- 处置
- 编辑
- 刷新
- 复制
- 查看原始数据

规则：

- 抽屉详情使用关闭按钮。
- 独立详情页使用返回或面包屑。
- 主操作放在标题区右侧。
- 次操作放在主操作左侧或收纳到更多菜单。
- 导出类操作使用次级按钮。

#### 模块级操作

常见操作：

- 展开详情
- 收起详情
- 查看更多
- 跳转列表
- 查看 Top 明细

规则：

- 模块操作应贴近模块标题。
- 展开/收起状态需有明确图标变化。
- 不要让模块操作与页面级操作混淆。

### 详情页状态与风险表达

详情页应统一使用状态标签表达对象风险。

#### 风险状态

- 严重 / 高危：红色
- 风险：红色或橙红色
- 拒识 / 异常：橙色
- 安全 / 正常：绿色
- 未知 / 无数据：灰色

#### 评分表达

- 评分应使用大号数字。
- 分值需带单位，如“75 分”。
- 最高分可使用蓝色或绿色标记。
- 最低分可使用红色浅底标记。
- 评分图表应配合文字说明，不单独依赖颜色。

### 详情页文案与假数据规范

- 对象标题：8–24 字。
- 分组标题：4–10 字。
- 标签文案：2–6 字。
- 按钮文案：2–6 字。
- 描述字段名：2–8 字。
- 时间格式：`2023-08-10 14:53:26`。
- IP 格式：`192.168.02.1`。
- 评分格式：`75 分`。
- 百分比格式：`24.23%`。
- 数量格式：`73,847`。
- 风险标签示例：`严重 / 高危 / 风险 / 安全 / 拒识`。
- 模型名称示例：`模型1 / 模型2 / 360safetyEval-test-s2裁判模型`。
- 业务描述文本不宜超过 3 行，长文本应折叠或放入详情区域。

### 详情页 AI 生图约束

- 必须明确详情页类型：抽屉式详情或独立页面详情。
- 抽屉式详情必须保留背景遮罩和关闭按钮。
- 独立详情页必须保留顶部导航、侧边导航、面包屑或返回入口。
- 详情页必须有明确对象标题和状态标签。
- 复杂内容必须分组，不可把所有字段平铺。
- 图表必须服务于单个对象解释，不要做成 Dashboard。
- 描述表格、对比表格、时间线、图表之间应形成清晰层级。
- 长详情页应通过标签页、锚点导航或分组标题降低阅读压力。
- 不要让主操作按钮超过 2 个。
- 不要在详情页中使用过多彩色标签。
- 不要把抽屉详情做成全屏列表页。
- 不要生成无意义的大段 lorem ipsum，说明文字应简短可信。

### 详情页 Prompt 模板

#### 分析评估抽屉详情 Prompt

```text
生成一张企业级安全产品分析评估抽屉详情高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景为被半透明遮罩弱化的列表页，右侧打开大抽屉。抽屉顶部包含对象名称、严重标签和关闭按钮。内容区依次包含综合决策评估、基本信息、应用层模型评估、传输层模型评估和流信息评估。综合评估区左侧为评分仪表盘，显示 75 分，中间展示关键特征标签，右侧为通信次数、上行包数、下行包数等指标卡。基本信息使用描述表格。应用层评估包含绿色热力图和横向排名条形图。传输层评估包含雷达图和指标表。流信息评估包含证书信息表格和统计指标卡片。整体为白色抽屉、浅灰背景、科技绿主色，信息清晰、分组明确。
```

#### 对比分析抽屉详情 Prompt

```text
生成一张企业级 AI 模型对比分析抽屉详情高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面背景为灰色遮罩，中央或右侧显示大尺寸详情抽屉。顶部为标题和关闭按钮。上方基础信息区使用浅蓝渐变背景，展示评估时间、数据集、模型名称、任务数量，并在右上角放置导出详情按钮。下方为对比分析表，左侧是指标维度，右侧横向排列模型1、模型2、模型3、模型4，每列包含模型图标、模型名称、得分、时间和多项指标。最高分用蓝色高亮，最低分用红色浅底标记。整体风格简洁、企业级、对齐严格。
```

#### 时间轴型独立详情页 Prompt

```text
生成一张企业级风险事件时间轴详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧导航、面包屑、标题区和右上角主次操作按钮。标题区展示事件名称和风险标签。主体内容为风险行为时间轴，每个时间节点包含时间、摘要卡片、标签、字段信息和展开详情入口。展开后显示描述表格和关联数据表格。页面使用白色卡片、浅灰背景、科技绿分组竖线和低饱和风险色标签，信息层级清晰。
```

#### 图文概览独立详情页 Prompt

```text
生成一张企业级图文概览详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧导航和内容区。顶部摘要卡片左侧展示风险对象插图和高危状态，右侧展示主标题、标签、次要信息、关键字段和主次操作按钮。摘要卡片下方是标签页，标签页内容包含指标卡片、Top5 横向条形图、筛选区、数据表格和分页。整体为浅灰背景、白色卡片、科技绿主色，插图轻量，表格和图表比例协调。
```

#### 资产画像独立详情页 Prompt

```text
生成一张企业级资产画像详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧导航、返回入口、标签页对象切换栏和内容卡片。顶部横向展示多个资产指标卡，当前选中项使用浅绿色背景。主体包含资产基础信息卡、资源占比进度条、图标宫格、描述表格、关联消息表格和右侧锚点导航。底部消息表格为空时展示居中的暂无数据空状态。整体简洁、留白适中、适合设备和资产详情场景。
```

#### 报告型长详情页 Prompt

```text
生成一张企业级安全分析报告型详情页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧导航、标题区、顶部标签路径、浅蓝科技背景区域、多个纵向分组模块和长页面滚动内容。每个分组包含标题、说明文字、浅黄色提示条、指标卡片、Top10 表格和风险建议。页面展示安全风险检测、历史检测、实时防护日志、漏洞情报、应急响应、安全建议等模块。整体采用白色卡片、浅灰背景、科技绿主色、低饱和警示色，长页面结构清晰，适合报告阅读。
```

### 详情页 Negative Prompt

```text
不要把详情页做成纯列表页，不要把抽屉式详情做成无背景上下文的孤立页面，不要缺少标题和关闭按钮，不要把所有字段堆在一屏里，不要无分组、不加标题，不要让图表与表格混乱重叠，不要使用过多高饱和彩色标签，不要使用大面积红色背景，不要生成无意义乱码或长段占位文字，不要让操作按钮分散在页面各处，不要让时间线节点间距不一致，不要让对比表各列错位。
```

### 详情页生成验收标准

| 检查项  | 标准                    |
| ---- | --------------------- |
| 类型明确 | 能区分抽屉式详情或独立页面详情       |
| 标题完整 | 对象名称、状态标签、关闭或返回入口完整   |
| 信息分组 | 基础信息、分析信息、关联信息分组清晰    |
| 视觉层级 | 摘要、图表、表格、操作层级明确       |
| 图表合理 | 仪表盘、热力图、雷达图、条形图使用场景正确 |
| 表格可读 | 描述表格、对比表格、数据表格边界明确    |
| 操作清晰 | 主操作、次操作、模块操作不混淆       |
| 风险表达 | 风险色、状态标签、评分表达一致       |
| 长页组织 | 标签页、锚点或分组标题降低阅读压力     |
| 生图质量 | 文字基本可读，无明显乱码、变形、错位    |

***

***

# 表单页模板

## 表单页面模板规范

表单页面用于完成数据录入、参数配置、规则编辑、策略设置、对象新增与对象修改。表单页面的核心目标是降低填写成本、减少配置错误、明确保存范围，并在复杂场景中通过分组、步骤、标签页、抽屉和条件构建器保持清晰的操作路径。

表单页面分为两大类：

```text
独立页面表单
  ↓
适合字段较多、配置层级较深、需要完整页面承载的场景

抽屉式表单
  ↓
适合新增、编辑、添加规则、轻量参数配置等局部操作场景
```

### 表单页面通用结构

表单页应遵循以下基本结构：

```text
页面路径 / 页面标题 / 返回入口
  ↓
标签页 / 步骤条 / 左侧对象树
  ↓
表单内容区
  ↓
分组标题 / 字段区 / 条件配置区 / 卡片配置区
  ↓
底部操作栏
```

通用规则：

- 表单内容应以白色容器承载，页面背景使用浅灰色。
- 字段标签左对齐，输入控件宽度统一。
- 必填项使用红色星号。
- 复杂表单必须分组，不应把所有字段无序堆叠。
- 保存、取消、上一步、下一步等操作应固定或靠近表单底部。
- 影响范围较大的配置应提供提示说明。
- 表单控件状态需包含默认、聚焦、禁用、错误、已选、只读。
- 表单页不应被设计成 Dashboard，不应加入大量图表。

***

### 独立页面表单模板

独立页面表单用于完整承载配置流程，适合较长、较复杂或需要多区域协作的配置任务。

#### 标准单栏表单页

适用于常规信息录入、基础配置、终端管理配置等。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题
  ↓
白色表单卡片
  ↓
顶部标签页
  ↓
单栏字段纵向排列
  ↓
底部保存按钮
```

#### 字段类型

可包含：

- 单行输入框
- 下拉选择器
- 日期 / 时间选择器
- 单选框
- 复选框
- IPv4 输入框
- 数字输入框
- 文件上传
- 开关
- 多行文本域

#### 布局规则

- 表单标签位于左侧，输入控件位于右侧。
- 标签宽度应统一，建议 120–160px。
- 控件宽度应统一，普通输入框建议 360–480px。
- 每行字段垂直间距保持一致。
- 提示说明放在字段下方，使用浅灰文字。
- 文本域右下角可显示字数计数，如 `0/100`。
- 保存按钮位于底部左侧或底部操作区左侧。

#### 视觉规则

- 当前标签页使用科技绿高亮。
- 必填星号使用红色。
- 输入框边框使用浅灰色。
- 聚焦状态使用主色描边。
- 开关开启状态使用主色，关闭状态使用灰色。
- 上传按钮为描边按钮，上传格式说明位于按钮下方。

#### 多层级导航表单页

适用于页面层级较深、左侧存在多级菜单或打开多个检测报告页签的场景。

#### 页面结构

```text
顶部导航
  ↓
左侧多级导航
  ↓
顶部页签 / 报告标签
  ↓
主表单区域
  ↓
底部保存按钮
```

#### 规则

- 左侧导航最多展示到 4–5 级，当前项高亮。
- 顶部可使用打开页签表示当前任务，如检测报告标签。
- 表单区仍采用单栏纵向布局。
- 当层级较深时，应通过面包屑或顶部页签帮助用户定位。
- 底部保存按钮应固定在内容区底部，避免长页面中找不到保存入口。

#### 左树右表单配置页

适用于按终端分组、组织、设备、策略对象进行配置的页面。

#### 页面结构

```text
页面标题 / 副标题
  ↓
左侧对象树 / 分组树
  ↓
右侧标签页
  ↓
右侧配置表单 / 卡片式配置区
```

#### 左侧树结构

可包含：

- 分组标题
- 搜索框
- 新增按钮
- 展开 / 收起箭头
- 节点名称
- 节点数量
- 当前选中项高亮

规则：

- 左侧树宽度应稳定。
- 当前选中节点使用浅绿色背景。
- 节点过长时使用省略号。
- 节点数量右对齐。
- 左右分栏之间使用竖向分隔线。
- 左树用于定位对象，右侧用于配置内容。

#### 右侧配置区

可包含：

- 标签页
- 分组标题
- logo / 图标预览
- 输入框
- 按钮
- 版本卡片
- 功能菜单卡片
- 场景菜单卡片

规则：

- 右侧内容跟随左侧选中对象变化。
- 当前标签页使用主色高亮。
- 配置说明应放在当前模块附近。
- 卡片式配置项应统一尺寸、间距和圆角。

#### 卡片式配置选择表单页

适用于版本选择、功能套餐、菜单模板、策略模板等。

#### 页面结构

```text
左侧对象树
  ↓
右侧配置卡片容器
  ↓
版本选项卡片
  ↓
功能菜单 / 场景菜单卡片
  ↓
底部操作按钮
```

#### 卡片内容

可包含：

- 单选圆点
- 图标
- 标题
- 适用说明
- 菜单模块卡片
- 状态标签，如“荐”“未购买授权”
- 恢复默认入口

#### 规则

- 当前选中卡片使用主色边框或浅绿色背景。
- 未选中卡片使用白底浅边框。
- 卡片内文字层级为：标题 > 描述 > 标签。
- 多组卡片之间保持明显分区。
- 卡片不宜过度装饰，保持后台配置属性。

#### 条件组合查询表单页

适用于高级查询、规则条件、筛选表达式、字段组合判断等场景。

#### 页面结构

```text
页面标题
  ↓
基础类型单选区
  ↓
条件组合容器
  ↓
多选条件区
  ↓
查询按钮
```

#### 条件组合区

可包含：

- 分组标题，如“操作系统”
- 关系选择器，如“且 / 或”
- 字段选择器
- 运算符选择器，如“包含 / 模糊 / 相等”
- 值选择器
- 新增条件按钮

#### 规则

- 条件容器使用浅灰边框卡片。
- 每组条件内部控件横向排列。
- 逻辑关系与字段选择应有清晰顺序。
- 新增按钮使用主色方形按钮。
- 多个条件组之间保持一致宽度和间距。
- 条件组支持展开 / 收起。
- 查询按钮应位于条件区下方，靠左或靠右保持统一。

#### 分步式策略配置页

适用于策略配置、规则启用、分阶段配置、配置向导等流程型表单。

#### 页面结构

```text
返回标题
  ↓
顶部步骤条
  ↓
左侧策略列表
  ↓
右侧当前步骤内容
  ↓
底部固定操作栏
```

#### 顶部步骤条

步骤状态包括：

- 已完成
- 当前步骤
- 未开始

规则：

- 已完成步骤使用对勾。
- 当前步骤使用主色圆点数字。
- 未完成步骤使用灰色。
- 步骤之间使用连接线。
- 步骤数量建议 3–5 个。

#### 左侧策略列表

可包含：

- 策略名称
- 完成数 / 总数
- 状态标签，如已配置、未配置、配置中
- 当前选中状态

规则：

- 当前选中策略使用主色边框或浅绿色背景。
- 策略列表可滚动。
- 数量格式如 `2/6`、`13/13`。
- 状态标签保持低饱和。

#### 右侧步骤内容

可包含：

- 折叠分组
- 开关
- 复选框
- 输入框
- 警告提示条
- 说明文本
- 多个可开关配置项

规则：

- 当前配置分组展开，其余分组可折叠。
- 分组右侧可放置开关。
- 警告提示使用浅黄色背景。
- 内容区应保持宽松，避免配置项过密。

#### 底部操作栏

可包含：

- 存为草稿
- 信息提示
- 上一步
- 下一步
- 取消

规则：

- 底部栏固定在页面底部。
- 主按钮为下一步。
- 上一步为次按钮。
- 存为草稿位于左侧。
- 中间可展示当前流程完成提示。

#### 模块能力选择表单页

适用于配置终端能力、软件模块、终端软件库、界面定制等。

#### 页面结构

```text
左侧终端分组树
  ↓
右侧配置页签
  ↓
顶部说明卡片
  ↓
配置分组
  ↓
可选模块 / 上传图标 / 开关 / 单选
  ↓
右侧锚点目录
```

#### 规则

- 顶部说明卡片使用浅灰或浅蓝底。
- 分组标题使用主色竖线。
- 锁定子分组状态使用标签提示。
- 模块开关或选项应位于分组内容中。
- 图片上传/图标预览区域应使用虚线边框或轻边框。
- 右侧锚点目录可浮动显示当前分组位置。
- 该类页面信息层级较深，必须通过分组和锚点降低阅读压力。

***

### 抽屉式表单模板

抽屉式表单用于在不离开当前页面的情况下完成新增、编辑、规则配置等任务。

#### 抽屉式表单通用结构

```text
背景页面遮罩
  ↓
右侧抽屉容器
  ↓
抽屉标题 + 关闭按钮
  ↓
表单内容区
  ↓
底部固定按钮区
```

通用规则：

- 抽屉从右侧滑出。
- 背景页面添加灰色遮罩。
- 抽屉标题固定在顶部。
- 右上角必须有关闭按钮。
- 内容区可纵向滚动。
- 底部按钮区固定，避免长表单中丢失确认入口。
- 默认按钮顺序为：取消 / 确认或取消 / 保存。
- 抽屉宽度根据复杂度使用中宽或宽抽屉。
- 中等复杂规则配置可使用大宽度抽屉，接近 60%–70% 页面宽度。

#### 标准新增 / 编辑抽屉表单

适用于新增节点、编辑节点、基础对象信息维护。

#### 表单字段

可包含：

- 节点名称
- 节点地址
- 访问令牌
- 描述
- 节点联系人
- 联系人电话

#### 布局规则

- 字段采用单列纵向排列。
- 必填项使用红色星号。
- 访问令牌字段可为只读输入框。
- 访问令牌右侧可放置复制按钮。
- 描述字段使用多行文本域。
- 编辑态预填数据，新增态为空白输入。
- 保存按钮使用主按钮，取消按钮使用次按钮。

#### 视觉规则

- 弹层遮罩为中性灰，不能过黑。
- 抽屉或弹窗主体为白底。
- 表单标签宽度统一。
- 输入框高度、宽度统一。
- 底部按钮右对齐。
- 编辑态禁用字段使用浅灰底。

#### 添加规则抽屉表单

适用于规则创建、监控规则、采集周期、阈值设置、白名单配置等。

#### 页面结构

```text
抽屉标题：添加规则
  ↓
规则名称
  ↓
规则描述
  ↓
配置方式
  ↓
组件选择
  ↓
规则内容配置卡片
  ↓
底部按钮区
```

#### 表单字段

可包含：

- 规则名称
- 规则描述
- 配置方式：JSON / 参数配置
- 组件：IVI / T-BOX
- 探针类型
- 采集周期
- 告警阈值
- IP 白名单
- 端口白名单
- 域名解析白名单
- CPU 过载运行监控
- 内存过载运行监控
- 磁盘空间过载运行监控

#### 规则内容区

规则内容区应使用浅灰底卡片承载复杂参数。

布局规则：

- 分组标题位于卡片顶部。
- 相关参数按 2–3 列排列。
- 单位如“秒”“%”放在输入框右侧。
- 白名单类字段支持动态新增与删除。
- 新增入口使用主色文字或按钮。
- 删除入口可使用减号圆形图标。
- 不同组件选项对应不同规则内容。

#### 参数配置型抽屉

适用于“选择不同组件后展示不同参数组”的场景。

#### IVI 组件配置

可包含：

- 探针：系统资源监控
- CPU 过载运行监控
- 内存过载运行监控
- 磁盘空间过载运行监控
- 采集周期
- 告警阈值

布局规则：

- CPU、内存、磁盘三个监控块可并列或分区显示。
- 每个监控块包含采集周期与告警阈值。
- 数字输入框旁显示单位。
- 必填字段带红色星号。

#### T-BOX 组件配置

可包含：

- 探针：网络监控
- 采集周期
- IP 白名单
- 端口白名单
- 域名解析白名单

布局规则：

- 白名单按列分组展示。
- 每组支持新增多条。
- 删除图标靠近对应输入项。
- 输入框应保持固定宽度。
- 域名输入框宽度可大于 IP / 端口输入框。

#### 动态增删字段抽屉

适用于 IP 列表、端口列表、白名单、黑名单、域名列表等可重复录入项。

规则：

- 每个动态组有明确标题。
- 新增按钮放在当前组下方。
- 删除按钮或减号图标放在字段右侧。
- 多行字段之间间距一致。
- 动态字段为空时至少保留一行输入。
- 需要校验重复值、格式和值域。

***

### 表单控件使用规范

#### 基础输入

- 普通文本输入用于名称、账号、标题、地址等。
- 数字输入用于端口、天数、阈值、采集周期等。
- IPv4 输入用于 IP 地址输入。
- Textarea 用于备注、规则描述、长文本说明。
- 密码或令牌输入默认脱敏。
- 只读字段使用禁用态或只读浅灰底。

#### 选择控件

- Select 用于状态、类型、策略、组件、字段、运算符选择。
- Radio 用于互斥选择，如开启/关闭、是/否、JSON/参数配置。
- Checkbox 用于多选配置项。
- Switch 用于功能启停。
- Date/Time Picker 用于时间选择。
- Tabs 用于平级配置模块切换。
- Steps 用于流程型配置。

#### 文件上传

- 上传按钮使用描边按钮。
- 上传说明放在按钮下方。
- 支持扩展名示例应明确，如 `.rar .zip .doc .docx .pdf .jpg...`。
- 文件上传字段不应与普通按钮混淆。
- 上传失败需要错误提示。

#### 提示与说明

表单提示可包含：

- Tooltip 问号图标
- 字段下方帮助文案
- 浅黄色警告提示条
- 浅蓝色信息说明块
- 字数计数器
- 默认值说明
- 锁定说明

规则：

- Tooltip 解释短概念。
- 较长说明使用提示块。
- 警告类提示使用浅黄色，不使用大面积红色。
- 字数计数器应位于文本域右下角。

***

### 表单布局样式规范

#### 字段对齐

- 左侧标签宽度统一。
- 控件左边缘对齐。
- 必填星号贴近标签文字。
- 帮助图标放在标签右侧。
- 同组字段垂直间距一致。

#### 表单宽度

- 单栏表单控件建议宽度 360–480px。
- 描述文本域可扩展到 480–640px。
- 抽屉表单控件可根据抽屉宽度扩展。
- 条件构建器中的控件宽度应按字段类型区分。
- 多列参数配置中，输入框不宜过宽。

#### 分组样式

- 分组标题可使用主色竖线。
- 折叠分组使用箭头。
- 分组之间用浅灰分割线或留白区隔。
- 当前分组展开时内容白底显示。
- 禁用分组使用灰色开关或浅灰状态。

#### 底部操作栏

- 独立表单可使用页面底部操作栏。
- 抽屉表单必须使用固定底部操作栏。
- 步骤流程表单应保留上一步、下一步、取消。
- 支持草稿的页面可在左侧放“存为草稿”。
- 保存按钮优先使用主色。

***

### 表单交互规范

#### 校验规则

需校验：

- 必填项
- 字符长度
- IP 格式
- 端口范围
- URL 格式
- 手机号
- 邮箱
- 数值范围
- 重复值
- 文件格式
- 文件大小

错误反馈：

- 字段边框变为错误色。
- 字段下方显示错误原因。
- 保存时定位到第一个错误字段。
- 抽屉表单保存失败时不关闭抽屉。

#### 保存与离开

- 保存成功后展示成功提示。
- 保存失败后展示失败原因。
- 未保存离开页面时应提示。
- 点击取消时，如内容已修改，应确认是否放弃。
- 分步表单下一步前应校验当前步骤。

#### 动态表单

- 选项切换后可动态展示不同字段。
- 不可用字段应禁用而非直接消失，除非业务明确要求。
- 动态新增字段需支持删除。
- 动态字段为空时应保留输入入口。
- 已填写内容在切换前应提示是否保留或清空。

***

### 表单页面 AI 生图约束

- 必须先明确表单类型：独立页面表单或抽屉式表单。
- 独立页面表单需包含导航、页面标题、表单容器和底部操作。
- 抽屉式表单需包含背景遮罩、右侧抽屉、标题、关闭按钮和底部按钮。
- 表单必须体现标签、输入框、选择器、单选、多选、开关等控件的真实结构。
- 复杂表单必须有分组、tabs、步骤条或左树辅助组织。
- 分步表单必须有步骤条和底部上一步/下一步操作。
- 条件组合表单必须有关系、字段、运算符和值选择。
- 规则配置抽屉必须有规则名称、规则描述、配置方式、组件和规则内容区。
- 不要把表单页生成成纯表格列表页。
- 不要把抽屉表单生成成小弹窗，除非明确要求弹窗。
- 不要省略保存/确认/取消按钮。
- 不要让必填星号、帮助提示和错误提示混乱。
- 不要在表单页加入大量无关图表。
- 不要使用大面积装饰背景影响填写效率。

***

### 表单页面 Prompt 模板

#### 标准独立页面表单 Prompt

```text
生成一张企业级 B 端后台独立表单页面高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、页面标题和白色表单容器。表单顶部有标签页，当前标签使用科技绿高亮。表单采用单栏左标签右控件布局，包含必填输入框、下拉选择器、时间选择器、单选组、多选组、IPv4输入框、数字输入框、文件上传按钮、开关和备注文本域，字段下方有帮助说明和上传格式提示。底部有绿色保存按钮。整体为浅灰背景、白色内容区、控件对齐整齐、企业级后台风格。
```

#### 左树右表单配置页 Prompt

```text
生成一张企业级 B 端后台左树右表单配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航、页面标题和主体双栏布局。主体左侧为终端分组树，包含搜索框、新增按钮、展开收起箭头、节点名称和数量，当前节点使用浅绿色背景高亮。右侧为配置表单区，顶部有标签页，内容包含标题、说明文字、logo预览、输入框、按钮、版本选择卡片和功能菜单卡片。整体布局清晰，主色为科技绿，适合复杂配置管理。
```

#### 条件组合查询表单 Prompt

```text
生成一张企业级 B 端后台高级条件组合查询表单页面高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧导航、面包屑和白色表单容器。表单中包含基础类型单选组、多个条件组合卡片，每个卡片标题为操作系统，内部包含关系选择器、字段选择器、运算符选择器和值选择器，并提供绿色加号按钮新增条件。下方包含多选字段区和查询按钮。整体简洁、低饱和、控件排列整齐，体现复杂筛选表达式构建。
```

#### 分步式策略配置页 Prompt

```text
生成一张企业级 B 端后台分步式策略配置表单页面高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、左侧导航、返回标题和顶部三步步骤条，第一步已完成，第二步为当前步骤，第三步未完成。主体左侧为策略列表卡片，展示策略名称、完成数/总数和已配置/未配置状态。右侧为当前步骤配置内容，包含多个折叠分组、开关、复选框、输入框、浅黄色警告提示条和说明文本。底部固定操作栏包含存为草稿、信息提示、上一步、下一步和取消按钮。整体白底浅灰背景，科技绿主色。
```

#### 抽屉式标准表单 Prompt

```text
生成一张企业级 B 端后台抽屉式编辑表单高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景页面被灰色遮罩弱化，右侧打开白色抽屉。抽屉顶部显示编辑节点标题和关闭按钮。表单采用单栏布局，字段包括节点名称、节点地址、访问令牌、描述、节点联系人、联系人电话，节点名称和节点地址带红色必填星号，访问令牌为只读输入框并在右侧提供复制按钮，描述为多行文本域。抽屉底部固定操作栏包含取消和保存按钮。整体简洁、对齐整齐、企业级配置风格。
```

#### 添加规则抽屉表单 Prompt

```text
生成一张企业级 B 端后台添加规则抽屉表单高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景页面为灰色遮罩，右侧打开宽抽屉。抽屉标题为添加规则，右上角有关闭按钮。表单包含规则名称、规则描述、配置方式单选项 JSON/参数配置、组件单选项 IVI/T-BOX。下方规则内容区使用浅灰底卡片，包含探针下拉选择器、采集周期、告警阈值等必填输入项。若组件为 T-BOX，则展示 IP 白名单、端口白名单、域名解析白名单三组动态输入，并提供新增和删除图标。底部固定确认和取消按钮。整体结构清晰、参数分组明确。
```

***

### 表单页面 Negative Prompt

```text
不要把表单页面做成 Dashboard，不要加入无关图表，不要使用营销式大插图，不要缺少保存按钮，不要缺少取消或确认按钮，不要让标签和输入框不对齐，不要把必填星号放错位置，不要让字段间距混乱，不要把抽屉表单生成成普通页面，不要省略背景遮罩和关闭按钮，不要让条件组合控件没有关系/字段/运算符/值，不要把分步表单缺少步骤条，不要让复杂表单没有分组。
```

### 表单页面生成验收标准

| 检查项  | 标准                       |
| ---- | ------------------------ |
| 类型明确 | 能区分独立页面表单和抽屉式表单          |
| 页面骨架 | 导航、标题、表单容器、操作区完整         |
| 字段结构 | 标签、控件、必填、帮助文案对齐清晰        |
| 控件完整 | 输入框、选择器、单选、多选、开关、上传等使用合理 |
| 分组清晰 | 复杂表单有分组、tabs、步骤条或左树      |
| 抽屉规范 | 遮罩、标题、关闭按钮、底部按钮完整        |
| 步骤流程 | 步骤条、当前步骤、底部上一步/下一步完整     |
| 条件配置 | 关系、字段、运算符、值、新增条件完整       |
| 校验表达 | 必填、错误、禁用、只读等状态可识别        |
| 生图质量 | 文字基本可读，控件不变形，布局不混乱       |

***

***

# 用户管理模板

## 用户管理页面模板规范

用户管理用于支撑用户注册登录、信息增删改查、权限与角色分配、状态管控、组织归属维护等功能。该类页面应通过合理的数据结构、清晰的组织关系表达和严格的安全操作控制，保障用户数据准确完整、访问权限安全有序，并与权限、授权、组织架构、角色管理等模块协同，为各业务模块提供稳定支撑。

### 用户管理页面类型

用户管理模块通常包含以下页面类型：

```text
用户列表页
  ↓
用于查询、查看、创建、编辑、启用、禁用、重置密码、转移组织等操作

组织架构联动用户列表页
  ↓
用于根据组织树筛选用户，支持组织策略和用户数据联动管理

用户详情抽屉
  ↓
用于查看单个用户的基础信息、登录日志、设备绑定、策略管理、应用导航等信息

编辑用户抽屉
  ↓
用于修改用户基础资料、所属组织、账号、手机号、邮箱等信息

用户组管理页
  ↓
用于维护用户组树结构、子用户组、新建用户组、二维码、删除等操作
```

### 用户管理通用页面骨架

用户管理页面应保持以下基本结构：

```text
深色顶部导航
  ↓
左侧分级导航
  ↓
页面标题 / 当前模块
  ↓
查询区 / 组织树 / 工具栏
  ↓
用户表格 / 用户组表格
  ↓
分页 / 操作反馈
```

页面背景应采用浅灰色，核心内容区采用白色卡片。查询区、工具栏、表格区之间应有清晰分隔，不应将查询控件和批量操作混在同一视觉层级中。

### 用户列表页模板

用户列表页用于展示用户基础信息，并支持用户数据的查询、批量处理和单行操作。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
查询区：关键词输入 + 用户状态选择 + 搜索 + 重置
  ↓
表格工具栏：批量操作 + 创建 + 导出 / 设置
  ↓
用户表格
  ↓
分页区
```

#### 查询区规范

查询区应位于表格上方的独立白色卡片内。

常用查询字段：

- 姓名
- 账号
- 手机号
- 电子邮箱
- 用户状态
- 所属组织
- 角色
- 创建时间

查询控件规则：

- 关键词输入框 placeholder 可写为 `姓名/账号/手机号/电子邮箱`。
- 用户状态使用下拉选择器。
- 搜索按钮使用主按钮。
- 重置按钮使用次按钮。
- 查询区字段数量较少时采用横向排列。
- 查询区高度应控制在一行或两行内，避免占据过多首屏空间。

#### 工具栏规范

工具栏用于批量操作和页面级操作。

左侧批量操作：

- 启用
- 禁用
- 重置密码
- 禁用并删除
- 转移组织

右侧页面操作：

- 创建
- 导出
- 字段设置 / 表格设置

规则：

- 批量操作按钮在未选中用户时应为禁用态。
- 创建按钮应置于工具栏右侧。
- 导出与设置可使用图标按钮。
- 删除类操作必须二次确认。
- 重置密码属于高风险操作，应有确认提示或结果反馈。

#### 用户表格规范

用户表格字段建议包括：

- 勾选框
- 姓名
- 账号
- 电子邮箱
- 用户状态
- 手机号
- 所属组织
- 操作

表格规则：

- 用户状态使用状态点 + 文案。
- 启用状态使用绿色圆点。
- 禁用状态使用灰色或红色弱提示。
- 操作列固定在最右侧。
- 单行操作建议为 `查看 / 编辑`。
- 用户列表默认展示 8–12 行。
- 分页区应展示总条数、每页条数、页码、跳页。

### 组织架构联动用户列表页模板

组织架构联动用户列表用于根据组织树筛选用户，并在右侧展示对应组织下的用户列表。

#### 页面结构

```text
顶部查询区
  ↓
左侧组织架构栏 + 右侧用户表格
  ↓
右侧工具栏
  ↓
右侧用户列表
  ↓
分页区
```

#### 组织架构栏规范

左侧组织架构栏应包含：

- 顶部标签页，如 `组织架构 / 组策略`
- 组织搜索框
- 多级组织树
- 当前选中组织高亮
- 展开 / 收起图标
- 可选滚动条

组织树规则：

- 一级组织使用较强字重。
- 子级组织缩进展示。
- 当前选中项使用主色或浅绿色背景。
- 组织名称过长时使用省略号。
- 组织树区域应与右侧表格有清晰分隔。
- 可使用竖向拖拽分隔控件调整左右宽度。

#### 右侧用户表格规则

右侧用户表格应继承用户列表页字段规范，并根据当前组织筛选数据。

- 右侧工具栏应保留启用、禁用、重置密码、转移组织、创建等操作。
- 表格内容随组织树选中项刷新。
- 当前组织名称可在页面标题或组织树中高亮，不必重复占用表格标题区。
- 当组织无用户时，应展示空状态。

### 用户详情抽屉模板

用户详情抽屉用于快速查看单个用户信息，不打断用户列表上下文。

#### 页面结构

```text
背景列表页 + 半透明遮罩
  ↓
右侧大抽屉
  ↓
抽屉标题：用户详情 + 关闭按钮
  ↓
标签页：基本信息 / 登录日志 / 设备绑定 / 策略管理 / 应用导航
  ↓
详情内容区
```

#### 抽屉布局规则

- 抽屉从右侧滑出。
- 背景用户列表页应添加遮罩。
- 抽屉顶部固定显示标题和关闭按钮。
- 标签页位于标题下方。
- 标签内容区左对齐展示。
- 信息量较少时不应强行填满页面。

#### 基本信息内容

基本信息字段建议包括：

- 所属组织
- 账号
- 姓名
- 手机号
- 电子邮箱
- 职位
- 身份证号

字段展示规则：

- 字段名和字段值左右排列。
- 字段名使用弱灰色。
- 字段值使用正文色。
- 空值使用 `-`。
- 字段组应保持统一行距。
- 用户详情中不应出现可编辑输入框，除非处于编辑态。

#### 详情标签页规则

标签页可包含：

- 基本信息
- 登录日志
- 设备绑定
- 策略管理
- 应用导航

规则：

- 当前标签使用主色高亮和底部指示线。
- 标签数量建议 3–6 个。
- 标签页切换不应关闭抽屉。
- 登录日志和设备绑定可使用简化表格展示。
- 策略管理可展示策略列表、状态和操作。
- 应用导航可展示用户可访问应用列表。

### 编辑用户抽屉模板

编辑用户抽屉用于修改用户基础资料，适合从用户列表中的“编辑”操作触发。

#### 页面结构

```text
背景列表页 + 半透明遮罩
  ↓
右侧编辑抽屉
  ↓
抽屉标题：编辑用户 + 关闭按钮
  ↓
表单内容区
  ↓
底部操作区：取消 + 确认
```

#### 表单字段规范

编辑用户表单字段建议包括：

- 所属组织
- 账号
- 姓名
- 手机号
- 电子邮箱
- 职位
- 身份证号

规则：

- 必填项使用红色星号。
- 所属组织使用选择器。
- 账号、姓名、手机号、电子邮箱为常见必填项。
- 职位、身份证号可为选填。
- 输入框宽度应统一。
- 标签与输入框左右排列。
- 表单字段垂直间距保持一致。
- 输入框 placeholder 使用 `请输入` 或 `选项-选择完成`。

#### 表单说明文案

可在表单下方提供弱提示说明，如：

- 用户默认密码为账号 + 手机号。
- 新增用户后可通过重置密码重新设置。
- 手机号或邮箱用于登录验证或通知。

说明文案规则：

- 使用浅灰色。
- 不超过 2 行。
- 不应使用强警告色，除非涉及安全风险。

#### 底部操作区

- 抽屉底部固定显示操作按钮。
- 取消按钮为次按钮。
- 确认按钮为主按钮。
- 确认前应校验必填项、手机号、邮箱格式。
- 点击取消时，如内容已修改，应提示是否放弃修改。

### 用户组管理页模板

用户组管理用于维护用户组树结构和组内操作。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
页面标题：用户组管理
  ↓
白色表格卡片
  ↓
树形用户组表格
  ↓
行操作区
  ↓
分页
```

#### 用户组表格字段

字段建议包括：

- 用户组
- 操作

用户组列：

- 支持展开 / 收起。
- 父级用户组左侧显示箭头。
- 子级用户组缩进显示。
- 用户名称或叶子节点不显示展开箭头。
- 当前层级通过缩进体现。

操作列：

- 编辑
- 新建用户组
- 查看二维码
- 删除

规则：

- 操作列应置于右侧。
- 高频操作使用主色链接。
- 删除操作可使用弱化色或危险色，并必须二次确认。
- 不可删除项应展示禁用态。
- 新建用户组操作只出现在允许创建子级的行。

#### 用户组层级规则

- 一级组、二级组、用户节点应通过缩进区分。
- 展开状态使用向下箭头。
- 收起状态使用向右箭头。
- 用户组树不宜超过 4–5 层，过深层级应考虑搜索或组织树组件。
- 行高保持与普通表格一致。
- 层级过多时应支持滚动。

### 用户状态与权限表达

用户管理页面中的状态表达必须清晰且安全。

#### 用户状态

- 启用：绿色圆点 + `启用`
- 禁用：灰色圆点或红色弱标签 + `禁用`
- 未激活：橙色或灰色标签 + `未激活`
- 锁定：红色或橙红标签 + `锁定`
- 待审核：黄色或蓝色弱标签 + `待审核`

#### 权限与角色

权限和角色不应在用户列表中展示过多细节，可在详情抽屉或专门的权限配置页展示。

适合展示在列表中的信息：

- 所属组织
- 用户状态
- 角色名称
- 最近登录时间

适合展示在详情中的信息：

- 已绑定设备
- 已分配策略
- 应用访问范围
- 登录日志
- 角色权限详情

### 用户管理安全操作规范

用户管理涉及账号安全，以下操作必须具备明确反馈或确认机制：

- 禁用用户
- 删除用户
- 禁用并删除
- 重置密码
- 转移组织
- 修改手机号
- 修改邮箱
- 修改所属组织
- 修改角色或权限
- 新增用户组
- 删除用户组

安全规则：

- 删除、禁用、重置密码必须二次确认。
- 批量操作应提示影响人数。
- 涉及账号归属变化时应提示可能影响权限。
- 成功后应给出全局提示。
- 失败后应说明原因。
- 对高权限用户操作应增加额外确认。

### 用户管理文案与假数据规范

常用字段：

- 姓名
- 账号
- 电子邮箱
- 用户状态
- 手机号
- 所属组织
- 操作
- 所属组织
- 职位
- 身份证号

示例数据：

- 姓名：高林武 / tom / Kevin
- 账号：gaolinwu / tom01
- 邮箱：<tom01@360.cn> / <ncew@35.com>
- 手机号：18908083876 / 12334567890
- 所属组织：终端安全团队 / ZZW组织
- 用户状态：启用 / 禁用 / 未激活 / 锁定
- 用户组：总用户组名称 / 分用户组名称 / 用户名称

文案规则：

- 表头字段控制在 2–6 字。
- 操作文案控制在 2–6 字。
- 说明文案不超过 2 行。
- 用户名、账号、邮箱应尽量使用真实格式，避免乱码。
- 组织名称过长时使用省略号。

### 用户管理 AI 生图约束

- 用户管理页面必须突出“查询 + 表格 + 操作”的管理属性。
- 用户列表页不要加入图表。
- 组织架构联动页必须展示左侧组织树和右侧用户表格。
- 用户详情和编辑用户应使用抽屉，不要做成弹窗小卡片。
- 编辑用户表单必须展示必填星号、输入框和底部确认区。
- 用户组管理必须体现树形层级和行操作。
- 表格中必须包含用户状态和操作列。
- 批量操作按钮应在表格上方，不应放在每一行。
- 禁用、删除、重置密码等安全操作不要使用过强主按钮。
- 不要把用户管理页面做成营销页或 Dashboard。
- 不要出现大量无意义头像、插画或图表。
- 不要在用户列表中展示过多权限细节，详细权限应放入详情或专门页面。

### 用户管理 Prompt 模板

#### 用户列表页 Prompt

```text
生成一张企业级 B 端用户管理列表页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和用户管理主内容区。顶部查询区包含关键词输入框，placeholder 为姓名/账号/手机号/电子邮箱，右侧有用户状态下拉选择器、搜索按钮和重置按钮。下方表格工具栏左侧包含启用、禁用、重置密码、禁用并删除、转移组织等批量操作按钮，右侧包含创建、导出和设置按钮。表格包含勾选框、姓名、账号、电子邮箱、用户状态、手机号、所属组织和操作列，用户状态使用绿色圆点加启用文字，操作列包含查看和编辑。底部包含总条数、每页条数、页码和跳页。整体为浅灰背景、白色卡片、科技绿主色、信息清晰的企业级后台风格。
```

#### 组织架构联动用户列表页 Prompt

```text
生成一张企业级 B 端组织架构联动用户管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和主内容区。上方是查询区，包含关键词输入、用户状态选择、搜索和重置。下方内容区为左右双栏布局，左侧是组织架构栏，顶部有组织架构和组策略标签、组织名称搜索框、多级组织树和当前选中组织高亮；右侧是用户表格区域，顶部有启用、禁用、重置密码、禁用并删除、转移组织、创建、导出和设置按钮。表格包含姓名、账号、电子邮箱、用户状态、手机号、所属组织和查看编辑操作。整体布局规整、数据密度适中、主色为科技绿。
```

#### 用户详情抽屉 Prompt

```text
生成一张企业级 B 端用户详情抽屉高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景是被灰色遮罩弱化的用户列表页，右侧打开大抽屉。抽屉顶部标题为用户详情，右上角有关闭按钮。标题下方是标签页，包含基本信息、登录日志、设备绑定、策略管理、应用导航，当前选中基本信息并使用科技绿高亮。内容区以字段列表形式展示所属组织、账号、姓名、手机号、电子邮箱、职位、身份证号等信息，字段名为灰色，字段值为正文色，空值显示短横线。整体简洁、留白充足、适合快速查看用户资料。
```

#### 编辑用户抽屉 Prompt

```text
生成一张企业级 B 端编辑用户抽屉高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。背景为被灰色遮罩弱化的用户列表页，右侧打开编辑用户抽屉。抽屉顶部显示编辑用户标题和关闭按钮。表单区包含所属组织、账号、姓名、手机号、电子邮箱、职位、身份证号等字段，所属组织为下拉选择器，账号、姓名、手机号、电子邮箱带红色必填星号。输入框宽度统一，标签左对齐。表单下方有浅灰说明文案，底部固定操作栏包含取消按钮和绿色确认按钮。整体清晰、克制、企业级。
```

#### 用户组管理页 Prompt

```text
生成一张企业级 B 端用户组管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和用户组管理内容区。主内容区为白色表格卡片，表格字段包含用户组和操作。用户组列使用树形结构展示总用户组名称、分用户组名称和用户名称，支持展开收起箭头和层级缩进。操作列位于右侧，包含编辑、新建用户组、查看二维码、删除等链接操作，删除项可展示禁用或弱化状态。底部包含简单分页。整体为浅灰背景、白色卡片、科技绿操作链接，结构清晰。
```

### 用户管理 Negative Prompt

```text
不要把用户管理页做成 Dashboard，不要加入大面积图表，不要使用大量头像装饰，不要把批量操作放到每一行，不要缺少用户状态列，不要缺少操作列，不要让组织树和用户表格混在一起，不要用过多高饱和颜色，不要在编辑用户表单中遗漏必填星号和底部确认按钮，不要把用户详情做成复杂报告页，不要在列表中展示过多权限明细，不要让删除和重置密码看起来像普通安全操作。
```

### 用户管理生成验收标准

| 检查项   | 标准                               |
| ----- | -------------------------------- |
| 页面类型  | 能区分用户列表、组织联动列表、用户详情抽屉、编辑抽屉、用户组管理 |
| 查询能力  | 关键词、用户状态、搜索、重置完整                 |
| 表格字段  | 姓名、账号、邮箱、状态、手机号、组织、操作完整          |
| 批量操作  | 启用、禁用、重置密码、转移组织等操作位置合理           |
| 用户状态  | 启用、禁用、锁定等状态表达清晰                  |
| 组织联动  | 左侧组织树与右侧用户表格关系明确                 |
| 抽屉详情  | 标题、关闭按钮、标签页、字段信息完整               |
| 编辑表单  | 必填项、输入框、说明文案、取消确认按钮完整            |
| 用户组层级 | 展开收起、缩进、行操作清晰                    |
| 安全控制  | 删除、禁用、重置密码等高风险操作有弱化或确认逻辑         |

***

***

# 系统设置模板

## 系统设置页面模板规范

系统设置用于配置平台级全局参数，包括密码复杂度规则、账号安全策略、数据导出方式、日志存储策略、CDN 节点配置、界面显示设置、远程托管服务、虚拟节点管理、系统工具下载等。该类页面的核心目标是保证系统具备足够的灵活性、安全性、可维护性和可扩展性。

系统设置页面应强调“清晰分组、低干扰配置、强安全提示、明确保存反馈”。配置项通常影响全局系统行为，因此应避免过度装饰，优先保证可读性、准确性和操作安全。

### 系统设置页面类型

系统设置模块通常包含以下页面类型：

```text
基础设置页
  ↓
用于配置服务参数、任务管理、密码规则、SSO、SMTP、时间同步、外部访问、AI 服务、界面定制等全局参数

数据导出设置页
  ↓
用于配置数据导出开关、外发方式、IP / 端口、转发配置和业务日志范围

数据清理设置页
  ↓
用于展示日志存储分布，并设置不同日志类型的保留时间

CDN 设置页
  ↓
用于配置 CDN 下载节点、存储节点、流控开关、IP 与端口

系统工具页
  ↓
用于展示外联靶机、离线升级工具、升级服务器等工具下载入口
```

### 系统设置通用页面骨架

系统设置页面应保持以下基础结构：

```text
深色顶部导航
  ↓
左侧系统导航
  ↓
系统设置二级标签页
  ↓
白色配置内容区
  ↓
配置分组 / 表单项 / 表格 / 工具卡
  ↓
应用 / 保存按钮
```

通用规则：

- 顶部导航保持全局一致。
- 左侧导航用于一级功能模块切换。
- 系统设置内部使用横向标签页区分子模块。
- 内容区采用白色背景，外层为浅灰页面背景。
- 配置项应按业务含义分组，不应无序堆叠。
- 每个配置分组应有明确标题，可使用主色竖线强调。
- 操作按钮应靠近对应分组，避免用户不清楚保存范围。
- 全局保存类按钮可固定在页面底部或内容区底部。
- 高风险配置应配合说明、tooltip 或二次确认。

### 系统设置横向标签页规范

系统设置页顶部应使用横向标签页组织不同配置模块。

常见标签：

- 基本设置
- 数据导出
- 数据清理
- CDN 设置
- 系统工具

标签规则：

- 激活标签使用主色文字与底部指示线。
- 标签数量建议 3–7 个。
- 标签区应位于顶部导航下方、内容区上方。
- 标签页切换时仅替换主内容区，不应改变左侧导航结构。
- 标签命名应短而明确，建议 4–6 字。

### 基础设置页模板

基础设置页用于承载大量全局配置项，适合采用长页面分组结构，并提供右侧锚点目录辅助跳转。

#### 页面结构

```text
顶部导航 + 左侧系统导航
  ↓
系统设置横向标签页
  ↓
基础设置内容区
  ↓
右侧锚点目录
  ↓
多个配置分组
```

#### 配置分组

基础设置页可包含以下配置分组：

- 服务管理
- 任务管理设置
- 分组管理设置
- 升级文件上传账号
- 安全大脑行为分析
- 密码加密设置
- SSO 配置
- SMTP 服务器设置
- 客户端通信设置
- 企业微信机器人告警配置
- 时间同步设置
- 外部访问中心配置
- 远程托管服务
- 360GPT
- 管控中心界面定制

分组规则：

- 每个分组顶部使用标题 + 主色竖线。
- 分组之间使用浅灰分割线。
- 每个分组内只放与当前主题相关的配置。
- 配置项较少时，按钮可放在该分组底部。
- 配置项较多时，按钮可放在分组右侧或底部统一区域。
- 每个分组的保存按钮文案建议使用 `应用`。
- 不同分组的保存范围应明确。

#### 右侧锚点目录

当配置项较多时，应提供右侧锚点目录。

锚点目录规则：

- 固定在内容区右侧。
- 展示所有主要配置分组名称。
- 当前分组使用主色圆点或主色文字高亮。
- 点击锚点可滚动到对应分组。
- 锚点目录应悬浮于内容区上方，但不遮挡主表单。
- 长页面滚动时锚点目录可保持固定。

#### 配置项控件

基础设置页可使用以下控件：

- 单选框：开启 / 关闭、统一设置 / 分别设置
- 开关：SSO 开关、流控开关、自定义界面开关
- 滑块：下载速率、阈值、限速
- 数字输入框：任务有效期、端口、天数
- 输入框：用户名、IP、地址、URL、appid、appkey
- 密码输入框：密钥、密码、token
- 下拉选择器：认证方式、安全连接、回调方式
- 复选框：勾选启用某配置项
- 上传或图片展示：界面定制 logo、图标、系统名称

控件规则：

- 表单标签左对齐。
- 必填项使用红色星号。
- 输入框宽度统一。
- 控件说明可放在控件右侧或下方。
- 长说明建议使用浅蓝信息提示块。
- 高风险说明可用 tooltip，不应大段堆叠在表单中。
- 禁用态输入框应使用浅灰底并降低文字对比度。
- 密码和密钥字段默认脱敏显示。

#### 界面定制配置

界面定制区域适合展示品牌相关内容。

可配置项：

- 登录界面图标
- 管控界面图标
- 浏览器页面图标
- 管控产品名称
- 是否开启自定义界面

规则：

- Logo 图片应以预览形式展示。
- 图片旁应说明适用位置、支持格式和尺寸限制。
- 系统名称输入框应限制长度。
- 品牌配置保存后应提示可能需要刷新页面生效。
- 不同 logo 类型应保持独立配置，不应混用。

### 数据导出设置页模板

数据导出设置页用于配置数据导出开关、外发方式、外发地址和业务日志范围。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：数据导出
  ↓
数据导出方式
  ↓
IP / 端口配置
  ↓
可导出的 IP 列表表格
  ↓
转发配置
  ↓
外发业务配置
  ↓
底部应用按钮
```

#### 数据导出方式区

配置项建议包括：

- 数据导出：开启 / 关闭
- 数据外发方式：Syslog 外发等
- IP 地址
- 端口
- 添加 IP 和端口

规则：

- 开启 / 关闭使用单选框。
- 外发方式使用下拉选择器。
- IP 和端口输入框横向排列。
- 添加按钮使用描边按钮。
- 配置列表为空时展示空状态。

#### 可导出列表表格

表格字段建议包括：

- 可导出的 IP 列表
- 端口列表
- 操作

规则：

- 表格表头使用浅灰底。
- 表格无数据时展示居中空状态。
- 空状态使用轻量图标 + `暂无数据`。
- 表格区不应过高，避免空白过大。
- 添加 IP 后应显示在表格中。

#### 转发配置

转发配置可包含：

- 转发配置模式
- 默认模式
- Unicode 字符模式
- JSON 标准说明

规则：

- 使用单选框表达互斥配置。
- 每个选项后可附带灰色解释文案。
- 默认模式应优先放在第一项。

#### 外发业务配置

外发业务配置可使用“业务类型 + 业务日志标签”结构。

业务类型示例：

- 资产中心
- 风险管理
- 终端防御
- 勒索防护
- APT 防护
- 上网管理
- 数据安全
- 能耗管理
- 运维日志

业务日志展示规则：

- 每个业务类型占一行。
- 左侧为业务类型与图标。
- 右侧为可导出日志标签组。
- 日志标签使用浅灰描边小标签。
- 标签数量较多时可换行展示。
- 行之间使用浅灰分割线。
- 业务配置按钮应位于配置区上方或左上角。

### 数据清理设置页模板

数据清理设置页用于展示日志存储分布，并配置不同业务日志的保留时间。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：数据清理
  ↓
日志存储分布图表
  ↓
设置日志保留时间
  ↓
业务分类配置表单
  ↓
终端连接策略
  ↓
底部应用按钮
```

#### 日志存储分布图表

日志存储分布适合使用柱状图或堆叠柱状图。

图表规则：

- 横轴展示业务分类，如实时对抗、病毒查杀、漏洞管理、数据保护、外设管理、上网管理等。
- 纵轴展示空间或容量。
- 鼠标悬停时展示 tooltip，包含总量和各日志类型占比。
- 柱状图颜色可按业务分类使用低饱和彩色，但不应过度鲜艳。
- 图表下方或右侧不应堆叠过多说明。

#### 日志保留时间设置

设置区应按业务大类纵向排列。

每个业务大类可包含：

- 统一设置
- 分别设置
- 保留时间下拉选择器
- 各日志子项保留时间

规则：

- 统一设置与分别设置使用单选框。
- 统一设置选中时，只显示一个保留时间下拉框。
- 分别设置选中时，展示该业务下多个日志项。
- 日志项按 2–3 列栅格排列。
- 未启用的下拉框应显示禁用态。
- 保留时间选项示例：半年、一年、永久、自定义。
- 大量日志配置应保持充分行距，避免拥挤。

#### 终端连接策略

终端连接策略可配置：

- 不清理离线终端
- 自动清除离线若干天的终端
- 天数输入框

规则：

- 单选框表达互斥策略。
- 天数使用数字输入框。
- 单位“天”放在输入框后。
- 该配置影响范围较大，建议提供说明 tooltip。

### CDN 设置页模板

CDN 设置用于配置下载节点、存储节点和流控策略。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：CDN 设置
  ↓
日志存储分布 / CDN 配置区
  ↓
CDN 下载节点配置
  ↓
下载节点列表
  ↓
存储节点配置
  ↓
存储节点列表
  ↓
底部保存按钮
```

#### CDN 配置区

配置项建议包括：

- CDN 配置：开启 / 关闭
- CDN 下载节点：IP 地址、端口、备注信息、流控开关
- 添加下载节点
- 存储节点：IP 地址、端口
- 添加存储节点

规则：

- 开启 / 关闭使用单选框。
- 节点输入项横向排列。
- 添加节点按钮使用描边按钮。
- 流控开关使用复选框或开关。
- 节点列表为空时展示空状态。
- 下载节点与存储节点之间使用分割线区分。
- 底部保存按钮使用主按钮。

#### 节点表格

下载节点表格字段：

- IP 地址
- 端口
- 备注
- 流控开关
- 操作

存储节点表格字段：

- IP 地址
- 端口
- 操作

规则：

- 表头使用浅灰背景。
- 空状态居中显示。
- 操作列位于最右侧。
- 节点删除应二次确认。
- IP 和端口需校验格式。

### 系统工具页模板

系统工具页用于展示可下载工具、升级工具和服务器工具入口。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：系统工具
  ↓
工具卡片网格
```

#### 工具卡片

工具卡片可包含：

- 工具图标
- 工具名称
- 简短说明
- 下载按钮
- 下载下拉菜单

工具示例：

- 外联靶机
- 离线升级工具
- 升级服务器

卡片规则：

- 卡片采用 3 列布局。
- 图标居中放置。
- 标题居中，字重略强。
- 描述文字 1–2 行。
- 下载按钮位于卡片底部居中。
- 存在多个安装包时，按钮可带下拉菜单。
- 下拉菜单示例：Windows 安装包、Linux 安装包。
- 卡片之间保持等宽和等高。
- 工具页不应加入复杂图表和长表单。

### 系统设置表单分组规范

系统设置页表单复杂度较高，必须通过分组降低阅读压力。

分组规则：

- 每组只处理一个配置主题。
- 每组标题应简短明确。
- 多组配置之间使用细分割线。
- 每组内部表单项左对齐。
- 同类控件宽度保持一致。
- 每组保存按钮与该组配置保持视觉关联。
- 涉及全局影响的配置应有说明文案。
- 表单项数量较多时应使用右侧锚点目录或分屏布局。

### 系统设置安全与校验规范

系统设置通常影响全局服务，应明确安全校验。

需要校验的字段：

- IP 地址
- 端口
- URL
- Webhook 地址
- SMTP 地址
- 邮箱
- appid
- appkey
- 密码
- token
- NTP 地址
- 天数
- 速率阈值

安全规则：

- 密钥类字段默认脱敏。
- 高风险配置保存前应确认。
- 禁用关键服务前应提示影响范围。
- 外发配置应校验连通性或提供测试连接按钮。
- SMTP 配置应提供发送测试邮件按钮。
- 时间同步设置应展示当前系统时间。
- 保存成功后展示全局成功提示。
- 保存失败时展示失败原因。
- 配置未保存离开页面时应提示。

### 系统设置文案与假数据规范

常用文案：

- 开启 / 关闭
- 应用 / 保存
- 添加 IP 和端口
- 添加下载节点
- 添加存储节点
- 暂无数据
- 业务配置
- 发送测试邮件
- 下载
- 立即下载

示例数据：

- IP 地址：`10.209.213.159`
- 端口：`8022`
- URL：`http://10.217.104.2`
- NTP 地址：`time1.cloud.tencent.com`
- Webhook：`请输入 webhook URL`
- 速率：`512 KB/s`
- 保留时间：`半年`
- 时间：`2024-01-04 09:47:52`

文案规则：

- 配置项名称建议 4–10 字。
- 按钮文案建议 2–6 字。
- 说明文案尽量不超过 2 行。
- Tooltip 只解释关键风险或专业概念，不应承载长篇说明。
- 空状态文案统一使用 `暂无数据`。

### 系统设置 AI 生图约束

- 系统设置页面应以表单配置和管理效率为主，不要做成 Dashboard。
- 基础设置页必须体现长页面分组和右侧锚点目录。
- 数据导出页必须体现开启/关闭、外发方式、IP/端口、导出列表和业务日志配置。
- 数据清理页必须包含日志存储分布图表和大量保留时间配置表单。
- CDN 设置页必须体现下载节点、存储节点、空状态和保存按钮。
- 系统工具页必须体现工具卡片和下载按钮。
- 配置项较多时必须分组，不要平铺成无层级长表单。
- 高风险配置应配合说明、tooltip 或确认逻辑。
- 保存按钮应贴近对应配置范围，不要让用户不清楚保存哪一部分。
- 不要使用大面积插画、营销背景或复杂装饰。
- 不要把系统设置页面生成成用户列表或数据看板。
- 不要遗漏开启/关闭、应用/保存、空状态等关键配置状态。
- 不要出现乱码 URL、错误 IP 格式或无意义端口。

### 系统设置 Prompt 模板

#### 基础设置页 Prompt

```text
生成一张企业级 B 端系统设置基础设置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置横向标签页，当前选中基本设置。主内容区为长页面表单，按服务管理、任务管理设置、分组管理设置、升级文件上传账号、安全大脑行为分析、密码加密设置、SSO配置、SMTP服务器设置、客户端通信设置、企业微信机器人告警配置、时间同步设置、外部访问中心配置、远程托管服务、360GPT、管控中心界面定制等分组排列。每个分组使用科技绿竖线标题、输入框、单选框、开关、数字输入框、下拉框、说明 tooltip 和应用按钮。右侧显示锚点目录，当前分组高亮。整体白底浅灰背景、结构清晰、适合全局配置管理。
```

#### 数据导出设置页 Prompt

```text
生成一张企业级 B 端系统设置数据导出页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置横向标签页，当前选中数据导出。内容区包含数据导出方式分组，展示开启/关闭单选框、数据外发方式下拉框、IP地址输入框、端口输入框和添加IP和端口按钮。下方展示可导出的IP列表表格，表头包含可导出的IP列表、端口列表和操作，表格为空时显示居中的暂无数据空状态。页面下半部分包含转发配置单选项和外发业务配置表，按资产中心、风险管理、终端防御、APT防护、上网管理、数据安全等业务类型展示大量浅灰日志标签。底部有应用按钮，整体规整、清爽、主色科技绿。
```

#### 数据清理设置页 Prompt

```text
生成一张企业级 B 端系统设置数据清理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧系统导航和系统设置标签页，当前选中数据清理。上方展示日志存储分布柱状图，横轴为实时对抗、病毒查杀、漏洞管理、数据保护、外设管理、上网管理等业务类型，鼠标悬停 tooltip 展示实时对抗 1.01GB 及各日志占比。下方为设置日志保留时间区域，按资产中心、风险管理、终端防御、APT防护、上网管理、数据安全、能耗管理、运维日志等业务大类分组，每组包含统一设置/分别设置单选框和多个保留时间下拉框，默认显示半年。底部包含终端连接策略和应用按钮。页面信息密度较高但排列整齐，白底浅灰背景，科技绿主色。
```

#### CDN 设置页 Prompt

```text
生成一张企业级 B 端系统设置 CDN 设置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置标签页，当前选中 CDN 设置。内容区包含 CDN 配置，使用开启/关闭单选框。下载节点配置区域包含 IP 地址输入框、端口输入框、备注信息输入框、流控开关和添加下载节点按钮，下方是下载节点表格，表头包含 IP 地址、端口、备注、流控开关、操作，空数据时显示居中暂无数据。下方存储节点区域包含 IP 地址、端口输入框、添加存储节点按钮和存储节点表格，同样展示暂无数据空状态。底部有绿色保存按钮。整体简洁、留白充足、表单和表格层级清晰。
```

#### 系统工具页 Prompt

```text
生成一张企业级 B 端系统设置系统工具页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置标签页，当前选中系统工具。主内容区为三列工具卡片网格，分别为外联靶机、离线升级工具、升级服务器。每张卡片包含居中的绿色科技风图标、工具名称、1到2行说明文字和立即下载按钮。升级服务器卡片的下载按钮展开下拉菜单，包含 Windows 安装包和 Linux 安装包。整体白色卡片、浅灰背景、低饱和科技绿主色，布局简洁。
```

### 系统设置 Negative Prompt

```text
不要把系统设置页做成 Dashboard，不要加入无关图表，不要使用大面积插画，不要让配置项无分组堆叠，不要省略保存或应用按钮，不要让用户不清楚保存范围，不要把基础设置生成成列表页，不要把数据导出页缺少 IP/端口配置，不要让数据清理页缺少保留时间下拉框，不要让 CDN 设置页缺少空状态和节点表格，不要让系统工具页出现复杂表格，不要使用错误格式的 IP、URL、端口和邮箱。
```

### 系统设置生成验收标准

| 检查项    | 标准                            |
| ------ | ----------------------------- |
| 页面类型   | 能区分基础设置、数据导出、数据清理、CDN 设置、系统工具 |
| 标签页    | 系统设置横向标签清晰，当前项高亮              |
| 配置分组   | 分组标题明确，配置项不混乱堆叠               |
| 表单控件   | 输入框、选择器、开关、单选框、数字输入框使用合理      |
| 保存范围   | 应用或保存按钮位置明确                   |
| 安全提示   | 高风险配置有说明、tooltip 或确认逻辑        |
| 空状态    | 表格无数据时展示统一暂无数据                |
| 数据导出   | 开关、外发方式、IP/端口、业务日志配置完整        |
| 数据清理   | 日志分布图与保留时间设置完整                |
| CDN 配置 | 下载节点、存储节点、节点表格和保存按钮完整         |
| 系统工具   | 工具卡片、说明、下载按钮和下拉菜单清晰           |
| 生图质量   | 文字基本可读，无明显乱码、错位和控件变形          |

***

***

# 系统通用配置模板

## 系统通用配置页面模板规范

这一类页面属于平台后台中的“通用管理配置模板”，主要覆盖组织管理、任务管理、告警中心、告警配置、系统日志、邀请管理、备份与恢复、应用管理、网络配置、检测配置、多级配置以及相关弹窗表单等页面。\
它们的共同特点是：**结构稳定、信息密度中高、操作明确、规则导向强、偏管理后台而非数据分析看板**。

系统通用配置页的设计重点不在视觉表现，而在于：

- 让用户快速完成配置、检索、批量操作、查看状态；
- 保证复杂系统参数的层级清晰与操作可控；
- 通过列表、状态、表单、分页、弹窗等组件形成统一的管理体验。

### 系统通用配置页面分类

```text
系统通用配置模板
├── 树形组织与层级管理
│   └── 组织管理
├── 列表型任务与状态管理
│   └── 任务管理
├── 告警与事件管理
│   ├── 告警中心
│   └── 告警配置
├── 日志与记录管理
│   └── 系统日志
├── 邀请与码类管理
│   └── 邀请管理
├── 资产/数据恢复类管理
│   └── 备份与恢复
├── 应用接入与应用列表管理
│   └── 应用管理
├── 网络与设备基础配置
│   └── 网络配置
├── 检测能力配置
│   └── 文件检测配置
├── 多级节点与上下级接入配置
│   └── 多级配置
└── 弹窗表单模板
    ├── 新增节点弹窗
    └── 编辑节点弹窗
```

### 系统通用配置通用页面骨架

这类页面建议遵循统一的后台骨架：

```text
深色顶部导航
  ↓
浅色左侧一级导航
  ↓
页面标题 / 页签 / 二级切换
  ↓
筛选区 / 检索区
  ↓
工具栏 / 批量操作区
  ↓
主内容区（表格 / 树表 / 长表单 / 配置分组）
  ↓
分页 / 底部操作 / 保存按钮
```

基础规则：

- 顶部导航承载平台全局信息、一级站点导航、消息、用户信息。
- 左侧导航承载系统模块入口，当前项高亮。
- 内容区采用白底卡片或白底容器，置于浅灰页面背景中。
- 筛选区与表格区分开，避免混在一层。
- 列表页默认采用“筛选区 + 工具栏 + 表格 + 分页”的成熟模式。
- 配置页默认采用“局部 tabs + 分组表单 + 保存按钮”的模式。
- 所有“删除、禁用、还原、同步、取消”等具有风险的操作应保留明确语义。

***

### 组织管理页模板

组织管理页用于维护树形组织结构、规则入组信息及组织的增删改查。

#### 页面结构

```text
筛选区（组织名称 + 搜索）
  ↓
工具栏（启用、禁用、全选、已选数量）
  ↓
右上操作（创建组织、导入/导出）
  ↓
树形表格
```

#### 表格字段建议

- 复选框
- 展开/收起箭头
- 组织名称
- 规则入组
- 操作

操作项可包含：

- 新增
- 编辑
- 删除
- 更多

#### 树形表格规则

- 支持多层级缩进展示。
- 父节点需带展开/收起箭头。
- 子级通过缩进表达层级关系。
- 勾选支持批量操作。
- “已选 N 个”应在工具栏中明确展示。
- 右侧操作文案需统一、间距稳定。
- 当层级较深时，应优先保证名称列可读性。

#### 适用场景

- 组织架构管理
- 分组策略管理
- 层级目录管理
- 分类树管理

***

### 任务管理页模板

任务管理页用于展示系统任务状态、执行进度、时间与操作。

#### 页面结构

```text
筛选区（功能模块、发布人、状态）
  ↓
搜索按钮
  ↓
任务列表表格
```

#### 表格字段建议

- 任务
- 发布人
- 下发时间
- 终端完成比例
- 状态
- 截止时间
- 操作

#### 任务状态展示规范

状态表现形式建议：

- 进度条 + 百分比
- 状态圆点 + 文案
- 颜色区分任务状态

示例状态：

- 执行中
- 执行完成
- 已取消
- 已失败

规则：

- 进度条颜色应稳定使用系统语义色。
- 百分比应跟随进度条同列展示。
- 状态文案不要只依赖颜色，必须带文字。
- 进行中状态支持“查看详情 / 取消”。
- 已完成状态通常仅保留“查看详情”。

***

### 告警中心页模板

告警中心页用于查看系统告警事件，并支持按级别、状态、类型进行识别与处理。

#### 页面结构

```text
告警概览统计卡
  ↓
搜索区（告警名称）
  ↓
右上操作（告警配置、快捷入口）
  ↓
告警列表表格
  ↓
分页
```

#### 顶部概览区

建议展示以下指标：

- 全部告警事件
- 紧急待处理
- 待处理
- 已处理
- 已忽略

规则：

- 数值是核心视觉焦点。
- 状态类指标可配合语义色。
- 概览卡应支持点击筛选或联动列表。
- 不宜使用复杂图表，保持轻量。

#### 表格字段建议

- 告警名称
- 告警级别
- 状态
- 告警类型
- 告警方式
- 告警时间
- 告警内容
- 操作

#### 状态与级别规范

告警级别：

- 高危
- 中危
- 低危
- 安全 / 普通

告警状态：

- 待处理
- 已处理
- 已忽略

规则：

- 告警级别建议采用标签样式。
- 状态建议采用小圆点 + 文案组合。
- 告警内容过长时可截断显示。
- 表格中应优先突出级别与状态。

***

### 告警配置页模板

告警配置页用于维护告警策略与启停状态。

#### 页面结构

```text
返回标题区（如：告警配置）
  ↓
搜索区（告警名称 / 创建人）
  ↓
右上操作（新增策略、快捷入口）
  ↓
配置表格
```

#### 表格字段建议

- 告警名称
- 告警级别
- 状态
- 告警类型
- 告警方式
- 创建时间
- 操作

操作项可包含：

- 复制
- 删除
- 编辑
- 启用 / 停用

#### 规则

- 告警策略页面应更偏配置管理，不强调统计信息。
- “新增策略”应放在表格上方右侧。
- 状态需清楚区分已启用 / 已停用。
- 配置类表格的操作文案通常比事件表格更多。

***

### 系统日志页模板

系统日志页用于查看管理员操作日志与系统运行日志。

#### 页面结构

```text
顶部局部 Tabs（管理员操作日志 / 系统运行日志）
  ↓
筛选区（时间、操作人、操作内容）
  ↓
搜索按钮
  ↓
日志表格
```

#### 表格字段建议

- 操作人
- IP 地址
- 操作类型
- 操作内容
- 危险程度
- 操作时间
- 操作

#### 日志页面规则

- 日志页面必须支持时间筛选。
- 适合通过 tabs 切换不同日志类型，而不是多个页面跳转。
- 危险程度应使用标签或状态色展示。
- 操作项一般为“详情”。
- 日志表格应强调可追溯性和清晰检索。

***

### 邀请管理页模板

邀请管理页用于管理邀请码、邀请状态和申请列表。

#### 页面结构

```text
顶部局部 Tabs（邀请加入 / 申请列表）
  ↓
筛选区（组织名称/邀请码、状态）
  ↓
工具栏（删除、刷新邀请码、下载邀请码）
  ↓
右上操作（创建邀请码、导出全部）
  ↓
邀请列表表格
  ↓
分页
```

#### 表格字段建议

- 复选框
- 状态
- 组织名称
- 有效期
- 邀请码
- 审批类型
- 操作

操作项可包含：

- 刷新
- 删除
- 复制邀请码

#### 规则

- 邀请码类页面要突出“复制”和“导出”。
- 状态文案应明确，如已过期、有效中、待审批。
- 批量操作按钮应置于表格上方左侧。
- 分页信息需包含总数、每页数量、页码跳转。

***

### 备份与恢复页模板

备份与恢复页用于查看备份记录并执行备份、还原、下载等操作。

#### 页面结构

```text
右上功能按钮（手动备份 / 自动备份 / 导入备份 / 刷新）
  ↓
备份记录表格
```

#### 表格字段建议

- 备份时间
- 备份内容
- 文件大小
- 备份形式
- 备份状态
- 创建者
- 完成时间
- 备注
- 操作

操作项可包含：

- 删除
- 还原
- 下载

#### 状态展示规范

备份状态建议：

- 备份成功
- 备份中
- 备份失败

规则：

- 备份成功可用绿色状态点。
- 还原与删除属于高风险操作，应有确认提示。
- 表格应突出“备份时间”“状态”“操作”。

***

### 应用管理页模板

应用管理页用于维护应用接入信息与应用识别图标。

#### 页面结构

```text
筛选区（应用名称/URL、所属应用组、创建人）
  ↓
搜索 / 重置 / 展开
  ↓
工具栏（删除、导出、复制）
  ↓
右上操作（新建应用、管理默认图标、快捷入口）
  ↓
应用列表表格
  ↓
分页
```

#### 表格字段建议

- 复选框
- 应用名称
- URL
- 图标
- 开放共享
- 操作

操作项可包含：

- 编辑
- 查看

#### 规则

- 图标列适合用固定尺寸小图标展示。
- URL 需清晰可读，可允许省略过长路径。
- “开放共享”适合使用文本值或状态值。
- 筛选区支持基础筛选 + 展开高级筛选。

***

### 网络配置页模板

网络配置页属于复杂系统基础配置页，特点是：一个页面中包含多个配置分区，每个分区结构不同。

#### 页面结构

```text
页面标题（网络配置）
  ↓
局部 Tabs（接口信息 / 路由管理 / 网络检测 / 可信主机配置 / SSH服务）
  ↓
分区 1：接口信息
  ↓
分区 2：路由管理
  ↓
分区 3：网络检测
  ↓
分区 4：可信主机配置
  ↓
分区 5：SSH 服务
```

#### 接口信息区

字段建议：

- 接口
- 接口开关
- 接口类型
- 接口模式
- 速率
- 连接状态
- 接口信息-IPV4
- 接口信息
- 操作

规则：

- 开关应直接置于表格中。
- 连接状态需用颜色点 + 文案区分运行 / 关闭。
- 支持“编辑 / 详情 / 删除”等操作。

#### 路由管理区

字段建议：

- 出接口
- 网关
- 目标网络 / 掩码
- 状态
- 操作

右上需有：

- 添加按钮

#### 网络检测区

配置项建议：

- 诊断工具类型：PING / 路由跟踪 / TELNET
- 目的 IP / 域名
- 连接测试按钮

规则：

- 使用单选按钮切换检测方式。
- 输入框与操作按钮同一行。
- 检测结果可作为后续扩展区域，不强制默认展示。

#### 可信主机配置区

字段建议：

- 复选框
- IP
- 描述
- 操作

附加能力：

- web 访问控制开关
- 添加按钮
- 批量删除

#### SSH 服务区

配置项建议：

- SSH 服务开关
- 服务端口
- 保存按钮

规则：

- 端口输入应有合法性限制。
- 关闭 SSH 服务时可给予风险提醒。

***

### 检测配置页模板（以文件检测配置为例）

检测配置页面通常包含左侧局部导航、顶部局部 tabs 和多个纵向配置分组。

#### 页面结构

```text
左侧配置导航
  ↓
顶部局部 Tabs（基础配置 / 检测类型设置 / 检测特征设备 / 加密压缩包破解）
  ↓
纵向配置分组
  ├── 文件基础设置
  ├── 检测设置
  ├── 静态检测引擎
  ├── 下载样本查看口令
  ├── 虚拟机网络配置
  ├── 沙箱网关网卡配置
  └── 虚拟机环境
```

#### 文件基础设置

配置项建议：

- 文件检测大小（滑块 + 数字框）
- 虚拟执行策略（单选）
- 复用检测结果（开关）
- 合并机器学习结果（开关）
- 保存 / 重置

规则：

- 滑块适合与数值输入联动。
- 配置说明可用强调色文字提示。

#### 检测设置

配置项建议：

- 沙箱分析时长
- 压缩包解压层数
- 保存按钮

#### 引擎设置

配置项建议：

- 启发式检测引擎开关
- 360AV 检测引擎开关
- 保存按钮

#### 下载样本查看口令

配置项建议：

- 启发式检测引擎口令
- 随机口令
- 自定义口令
- 口令输入框
- 保存按钮

#### 虚拟机网络配置

配置项建议：

- 虚拟机不可联网
- 虚拟机可联网
- 虚拟机使用虚拟网络
- https 解密模式
- 保存按钮

#### 虚拟机环境

建议使用左侧环境列表 + 右侧环境详情。
左侧可包含：

- Windows XP
- Windows 7 32bit
- Windows 7 64bit
- Windows server 2003
- Windows 10
- CentOS 32bit

右侧可展示：

- 系统版本
- 软件版本
- 已安装软件列表
- 虚拟机个数

***

### 多级配置页模板

多级配置页用于配置上下级节点接入与节点管理。

#### 页面结构

```text
左侧局部导航
  ↓
右侧主内容
  ├── 接入上级节点设置
  ├── 接入下级节点设置
  └── 节点表格
```

#### 接入上级节点设置

配置项建议：

- 接入上级节点：是 / 否
- 数据分权同步：是 / 否

#### 接入下级节点设置

配置项建议：

- 允许下级节点接入：是 / 否
- 新增下级节点按钮
- 节点管理表格

表格字段建议：

- 节点名称
- 节点地址
- 描述
- 联系人
- 电话
- 节点状态
- 操作

操作项可包含：

- 编辑
- 配置同步
- 删除

节点状态可包含：

- 节点已接入
- 下级已注销
- 未接入
- 已禁用

#### 规则

- 上级、下级配置应拆成两个分组，避免混在一起。
- 节点表格要配合分页。
- 节点状态需配颜色点和文字说明。
- “配置同步”应区分于 सामान्य编辑操作。

***

### 弹窗表单模板：新增节点 / 编辑节点

这类弹窗适合沉淀为通用“系统配置表单弹窗模板”。

#### 弹窗结构

```text
弹窗标题
  ↓
表单区
  ↓
底部按钮区（取消 / 保存）
```

#### 表单字段建议

- 节点名称（必填）
- 节点地址（必填）
- 访问令牌
- 描述（多行）
- 节点联系人
- 联系人电话

附加能力：

- 访问令牌右侧“复制”按钮

#### 弹窗设计规范

- 弹窗需居中显示。
- 背景遮罩应弱化但可感知。
- 标题栏右上角应有关闭按钮。
- 必填项使用红色星号。
- 多行文本域用于描述。
- 底部按钮右对齐。
- 默认按钮顺序：取消 / 保存。
- 编辑态可预填数据，新增态为空白。

***

### 系统通用配置组件规范

#### 筛选区规范

常见筛选项：

- 输入框
- 下拉框
- 状态选择
- 时间范围
- 搜索按钮
- 重置按钮
- 展开高级筛选

规则：

- 基础筛选项一行展示。
- 搜索按钮使用主按钮。
- 重置按钮使用次按钮。
- 高级筛选可折叠展开。
- 输入框宽度保持统一。

#### 工具栏规范

常见工具按钮：

- 启用
- 禁用
- 删除
- 导出
- 复制
- 刷新
- 下载邀请码
- 新建应用
- 创建组织
- 新增策略

规则：

- 左侧以批量操作为主，右侧以新增/创建为主。
- 高风险按钮不应与主按钮视觉同权。
- 工具栏与表格头之间要有清晰分隔。

#### 表格规范

- 表头使用浅灰底。
- 表格行高统一。
- 文字左对齐优先。
- 操作列固定在右侧。
- 列表为空时需保留空状态。
- 表格分页统一置于底部右侧或底部居右。
- 分页区应包含总数、每页数量、页码、前进后退、页码跳转（可选）。

#### 状态规范

建议统一语义：

- 绿色：成功、启用、已处理、运行中、有效
- 橙色：高危、待处理、警告
- 红色：失败、关闭、异常、注销
- 灰色：禁用、已忽略、未启用

状态呈现方式：

- 标签
- 圆点 + 文案
- 开关
- 进度条

***

### 系统通用配置 AI 生图约束

- 这类页面本质是 B 端后台配置/管理界面，不要做成营销官网。
- 页面风格要克制，强调表格、表单、状态与操作逻辑。
- 不能过度使用卡片堆叠或花哨插画。
- 列表型页面必须体现“筛选区 + 工具栏 + 表格 + 分页”的逻辑。
- 复杂配置页必须体现“分组 + tabs + 保存按钮”的逻辑。
- 多级配置、网络配置、检测配置这类页面必须体现配置分区和表单层级。
- 告警类页面要突出概览状态、级别标签与处理状态。
- 日志类页面要突出可检索性和可追溯字段。
- 弹窗表单必须居中、带遮罩、带取消和保存按钮。
- 所有页面必须保持简体中文。
- 风格统一为浅色极简、安全产品后台风、科技绿色主色。

***

### 系统通用配置 Prompt 模板

#### 组织管理页 Prompt

```text
生成一张企业级安全管理平台的组织管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和白色主内容区。主内容上方为组织名称搜索框和搜索按钮，下方为工具栏，包含启用、禁用、全选、已选数量提示，右上有创建组织和导入导出按钮。主体为树形表格，字段包含组织名称、规则入组、操作，支持多级展开缩进，行尾操作包括新增、编辑、删除、更多。整体风格克制、规整、科技绿色主色、后台管理感强。
```

#### 任务管理页 Prompt

```text
生成一张企业级安全管理平台的任务管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含筛选区，支持功能模块、发布人、全部状态筛选，并有搜索按钮。下方为任务表格，字段包括任务、发布人、下发时间、终端完成比例、状态、截止时间、操作。完成比例使用进度条展示，状态包含执行中和执行完成，操作含查看详情和取消。整体浅色背景、白色内容区、表格清晰、绿色主色。
```

#### 告警中心页 Prompt

```text
生成一张企业级安全管理平台的告警中心页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部展示全部告警事件、紧急待处理、待处理、已处理、已忽略等概览统计卡。中间为告警名称搜索框和搜索按钮，右上包含告警配置按钮。主体为告警列表表格，字段包含告警名称、告警级别、状态、告警类型、告警方式、告警时间、告警内容、操作。告警级别使用标签样式，状态使用彩色圆点加文字，底部有分页。整体风格专业克制。
```

#### 告警配置页 Prompt

```text
生成一张企业级安全管理平台的告警配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部有返回标题“告警配置”，下方是搜索区，支持按告警名称或创建人搜索，右上有新增策略按钮。下方表格字段包含告警名称、告警级别、状态、告警类型、告警方式、创建时间、操作，操作含复制、删除、编辑、启用。页面布局简洁，表格信息清晰，绿色主色突出关键操作。
```

#### 系统日志页 Prompt

```text
生成一张企业级安全管理平台的系统日志页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部包含局部 tabs：管理员操作日志、系统运行日志。下方为筛选区，支持时间范围、操作人、操作内容检索，并有搜索按钮。主体为日志表格，字段包含操作人、IP地址、操作类型、操作内容、危险程度、操作时间、操作。危险程度用标签展示，操作为详情。页面整体清晰规整。
```

#### 邀请管理页 Prompt

```text
生成一张企业级安全管理平台的邀请码管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部包含 tabs：邀请加入、申请列表。下方是搜索区，支持组织名称/邀请码和状态筛选。工具栏包含删除、刷新邀请码、下载邀请码，右上包含创建邀请码和导出全部。主体为邀请码表格，字段包含状态、组织名称、有效期、邀请码、审批类型、操作，操作支持刷新、删除、复制邀请码。底部包含总条数、每页条数和分页器。
```

#### 备份与恢复页 Prompt

```text
生成一张企业级安全管理平台的备份与恢复页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面右上包含手动备份、自动备份、导入备份和刷新按钮。主体是备份记录表格，字段包含备份时间、备份内容、文件大小、备份形式、备份状态、创建者、完成时间、备注、操作。操作包括删除、还原、下载，状态以绿色成功状态显示。整体简洁、专业、后台感强。
```

#### 应用管理页 Prompt

```text
生成一张企业级安全管理平台的应用管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面上方为筛选区，包含应用名称/URL、所属应用组、创建人，以及搜索、重置、展开按钮。下方工具栏包含删除、导出、复制，右上角包含新建应用、管理默认图标和快捷操作按钮。主体为应用列表表格，字段包括应用名称、URL、图标、开放共享、操作。图标列为统一尺寸的小图标，操作包括编辑和查看，底部含分页。
```

#### 网络配置页 Prompt

```text
生成一张企业级安全管理平台的网络配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和白色主内容区。主内容顶部有局部 tabs：接口信息、路由管理、网络检测、可信主机配置、SSH服务。页面中依次展示多个分区：接口信息表格、路由管理表格、网络检测配置、可信主机配置表格、SSH服务开关与端口设置。表格包含开关、状态圆点、网络信息字段和分页。整体为复杂系统配置页风格，信息清晰、分组明确。
```

#### 文件检测配置页 Prompt

```text
生成一张企业级安全管理平台的文件检测配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面左侧为配置导航，顶部包含局部 tabs：基础配置、检测类型设置、检测特征设备、加密压缩包破解。主内容为纵向多个白色配置分组，包含文件基础设置、检测设置、静态检测引擎、下载样本查看口令、虚拟机网络配置、沙箱网关网卡配置、虚拟机环境。表单中包含滑块、单选框、开关、下拉框、输入框、说明文字和保存按钮。底部虚拟机环境区采用左侧系统列表、右侧环境详情布局。
```

#### 多级配置页 Prompt

```text
生成一张企业级安全管理平台的多级配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面左侧为局部导航，当前选中多级配置。主内容包含两个配置分组：接入上级节点设置、接入下级节点设置。上级节点设置中展示是否接入上级节点、是否数据分权同步。下级节点设置中展示是否允许下级节点接入、新增下级节点按钮和节点管理表格。表格字段包括节点名称、节点地址、描述、联系人、电话、节点状态、操作，状态用彩色圆点和文字表示，操作包括编辑、配置同步、删除，底部含分页。
```

#### 新增/编辑节点弹窗 Prompt

```text
生成一张企业级安全管理平台的新增下级节点弹窗高保真界面效果图，简体中文，4:3，正视角平面 UI。画面为灰色遮罩背景，中间为白色居中弹窗。弹窗标题为“新增下级节点”或“编辑节点”，右上角有关闭按钮。表单字段包括节点名称、节点地址、访问令牌、描述、节点联系人、联系人电话，其中节点名称和节点地址为必填项，访问令牌右侧有复制按钮。底部右对齐放置取消和保存按钮。整体表单整齐、留白合理、后台配置风格明显。
```

***

### 系统通用配置 Negative Prompt

```text
不要做成数据看板，不要出现夸张渐变背景，不要使用营销官网式大插画，不要把复杂配置页做成单一表格，不要省略筛选区和工具栏，不要让分页消失，不要让状态只有颜色没有文字，不要让弹窗没有遮罩或没有关闭按钮，不要生成乱码字段，不要让表格列顺序混乱，不要把网络配置、检测配置、多级配置做成普通列表页。
```

### 系统通用配置生成验收标准

| 检查项    | 标准                                    |
| ------ | ------------------------------------- |
| 页面类型识别 | 能区分组织、任务、告警、日志、邀请、备份、应用、网络、检测、多级配置等模板 |
| 页面骨架   | 具备后台系统统一骨架，导航、筛选、内容区逻辑明确              |
| 筛选区    | 输入框、下拉框、搜索等组件布局规范                     |
| 工具栏    | 批量操作与新增操作分区明确                         |
| 表格质量   | 表头、行高、列结构、操作列、分页完整                    |
| 状态表达   | 状态点、标签、进度条等表达清楚，含文字                   |
| 配置分组   | 网络/检测/多级配置等复杂页面分组清楚                   |
| 弹窗质量   | 居中、遮罩、必填项、底部按钮齐全                      |
| 中文可读性  | 主要文字基本可读，无大面积乱码                       |
| 风格统一   | 浅色、极简、安全后台风格统一                        |

***

***

# AI生图强约束与NegativePrompt

本文件用于在具体页面生成时提供短上下文约束。不要整套规范一次性投喂给 AI；每次只组合“全局基础规范 + 对应页面模板 + 本文件 + 当前页面内容”。

## 调用原则

每次生成单个页面时，只调用：

```text
01_全局基础规范.md
+ 当前页面模板文件
+ 10_AI生图强约束与NegativePrompt.md
+ 当前页面业务内容
```

不要同时在一个 Prompt 中混入 Dashboard、列表页、详情页、表单页、系统设置页等多个页面模板。

## 通用强 Prompt 前缀

```text
生成一张企业级 B 端安全管理平台高保真 UI 界面效果图，简体中文，16:9，4K，正视角平面 UI，无透视、无设备外框、无浏览器外壳。整体为深色顶部导航、浅色左侧导航、浅灰页面背景、白色内容卡片、科技绿色主色。顶部导航高度克制，左侧导航宽度不超过页面六分之一，主内容区左右边距一致，模块间距统一，所有组件对齐整齐。
```

## 页面类型调用矩阵

| 要生成的页面          | 必须调用文件                | 禁止混入               |
| --------------- | --------------------- | ------------------ |
| Dashboard / 工作台 | 03\_Dashboard工作台模板.md | 高级搜索列表、长表单         |
| 标准列表页           | 04\_列表页模板.md          | 大面积图表、Dashboard 风格 |
| 左筛选列表页          | 04\_列表页模板.md          | 把左筛选栏做成系统导航        |
| 高级搜索列表页         | 04\_列表页模板.md          | 指标卡、图表看板           |
| 抽屉详情页           | 05\_详情页模板.md          | 输入表单、必填星号          |
| 独立详情页           | 05\_详情页模板.md          | 查询区、批量操作           |
| 独立表单页           | 06\_表单页模板.md          | 表格分页、Dashboard 图表  |
| 抽屉表单页           | 06\_表单页模板.md          | 详情图表、分析评估模块        |
| 用户管理页           | 07\_用户管理模板.md         | 图表看板               |
| 系统设置页           | 08\_系统设置模板.md         | Dashboard 图表堆叠     |
| 系统通用配置页         | 09\_系统通用配置模板.md       | 营销插画、大屏风格          |

## 页面类型短约束

### Dashboard / 工作台

- 以指标卡和图表为主，表格只是辅助。
- 首屏建议 4–6 个指标卡、3–4 个图表，最多 1 个表格。
- 图表卡片采用规则栅格布局，不做大屏可视化。
- 顶部可有风险告警横幅或摘要区。

Prompt 片段：

```text
这是 Dashboard 工作台页面，以指标和图表为主。首屏上方是一条风险告警横幅和 4 个核心指标卡，中部使用 2 列图表布局，下方放一个简洁表格或资源状态卡。图表数量控制在 4 个以内，所有卡片等间距排列。
```

### 列表页

- 以表格为主体，不要生成图表。
- 查询区只占上方小面积，表格卡片占页面主要空间。
- 表格展示约 10 行、6–10 列，右侧有操作列，底部有分页。
- 分页紧贴表格底部。

Prompt 片段：

```text
这是列表页，以表格为主体，不要生成图表。查询区只占上方一小块，表格卡片占据页面主要空间。表格展示约 10 行数据、8 列字段、浅灰表头、右侧操作列和底部分页。
```

### 详情页

- 以单个对象的信息分组展示为主。
- 抽屉详情必须右侧贴边，有遮罩、标题、关闭按钮。
- 独立详情必须有面包屑/返回、对象摘要、分组内容。
- 图表数量控制在 3 个以内。

Prompt 片段：

```text
这是详情页，以单个对象的信息分组展示为主。页面必须有明确标题、状态标签和分组结构。抽屉详情从右侧滑出，背景页面加灰色遮罩，顶部固定标题和关闭按钮。
```

### 表单页

- 以字段录入和保存为主。
- 标签列统一宽度，输入框左边缘对齐。
- 独立表单不要让输入框横跨全屏。
- 抽屉表单必须右侧贴边，有遮罩、标题、关闭按钮和底部操作栏。
- 复杂表单必须使用分组、tabs、步骤条或左树。

Prompt 片段：

```text
这是表单页面，以字段录入和保存为主。标签列统一宽度，所有输入框左边缘对齐，控件宽度一致。抽屉表单从右侧滑出，背景灰色遮罩，顶部标题和关闭按钮固定，底部确认取消按钮固定。
```

### 系统设置页

- 以分组配置项为主，不是数据看板。
- 顶部有系统设置二级标签页。
- 内容区是白底长表单，每个配置分组用绿色竖线标题区分。
- 右侧可有锚点目录。
- 每个配置分组有独立应用/保存按钮。

Prompt 片段：

```text
这是系统设置页，以分组配置项为主，不是数据看板。内容区使用白底长表单，每个配置分组用绿色竖线标题和浅灰分割线区分，输入框、单选、开关、下拉框左对齐，右侧有窄锚点目录。
```

## 通用 Negative Prompt

```text
不要生成营销海报，不要生成概念大屏，不要使用三维透视，不要使用手机电脑设备壳，不要使用复杂背景纹理，不要大面积渐变，不要过度玻璃拟态，不要厚重阴影，不要粗黑边框，不要文字乱码，不要表格错位，不要控件大小不一致，不要页面元素漂浮，不要混淆 Dashboard、列表页、详情页和表单页。
```

## 常见失败结果与修正 Prompt

| 失败现象    | 追加修正 Prompt                         |
| ------- | ----------------------------------- |
| 页面像营销海报 | 请按企业后台系统截图生成，不要海报化，不要大标题视觉，不要装饰性背景。 |
| 页面像数据大屏 | 请使用浅色后台管理界面，不要深色大屏，不要发光网格，不要全屏可视化。  |
| 导航过宽    | 左侧导航宽度不超过页面六分之一，顶部导航高度不超过页面十分之一。    |
| 内容太松散   | 提高信息密度，表格展示约 10 行数据，卡片间距统一但不要过大。    |
| 内容太拥挤   | 减少模块数量，保留 3–4 个核心模块，卡片内边距充足。        |
| 表格不可读   | 表格表头浅灰底，8 列以内，10 行数据，列宽均衡，分页紧贴底部。   |
| 表单错位    | 标签列统一宽度，所有输入框左边缘对齐，字段垂直间距一致。        |
| 抽屉像弹窗   | 右侧贴边抽屉，占页面宽度约 60%，背景页面有灰色遮罩。        |
| 列表页出现图表 | 这是列表页，以表格为主体，不要生成任何图表。              |
| 文字乱码严重  | 减少正文文字，只保留短中文标题、表头、按钮和少量数字。         |

## 生图硬性验收清单

| 检查项   | 合格标准                                |
| ----- | ----------------------------------- |
| 页面类型  | 一眼能看出是 Dashboard、列表、详情、表单、系统设置中的哪一类 |
| 导航结构  | 顶部导航、左侧导航比例正确                       |
| 主内容比例 | 主体模块占比合理，不拥挤、不松散                    |
| 模块顺序  | 与模板结构一致，没有随机堆叠                      |
| 表格质量  | 表头、数据行、操作列、分页完整                     |
| 表单质量  | 标签、控件、必填、保存按钮对齐清楚                   |
| 图表数量  | Dashboard 图表不过量，列表页不出现无关图表          |
| 抽屉质量  | 遮罩、标题、关闭、底部操作完整                     |
| 色彩控制  | 主色不滥用，状态色只用于状态                      |
| 文字密度  | 中文文字数量适中，没有大片乱码                     |

