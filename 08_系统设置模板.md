# 系统设置模板

## 系统设置页面模板规范

系统设置用于配置平台级全局参数，包括密码复杂度规则、账号安全策略、数据导出方式、日志存储策略、CDN 节点配置、界面显示设置、远程托管服务、虚拟节点管理、系统工具下载等。该类页面的核心目标是保证系统具备足够的灵活性、安全性、可维护性和可扩展性。

系统设置页面应强调“清晰分组、低干扰配置、强安全提示、明确保存反馈”。配置项通常影响全局系统行为，因此应避免过度装饰，优先保证可读性、准确性和操作安全。

### 系统设置页面类型

系统设置模块通常包含以下页面类型：

```text
基础设置页
  ↓
用于配置服务参数、任务管理、密码规则、SSO、SMTP、时间同步、外部访问、AI 服务、界面定制等全局参数

数据导出设置页
  ↓
用于配置数据导出开关、外发方式、IP / 端口、转发配置和业务日志范围

数据清理设置页
  ↓
用于展示日志存储分布，并设置不同日志类型的保留时间

CDN 设置页
  ↓
用于配置 CDN 下载节点、存储节点、流控开关、IP 与端口

系统工具页
  ↓
用于展示外联靶机、离线升级工具、升级服务器等工具下载入口
```

### 系统设置通用页面骨架

系统设置页面应保持以下基础结构：

```text
深色顶部导航
  ↓
左侧系统导航
  ↓
系统设置二级标签页
  ↓
白色配置内容区
  ↓
配置分组 / 表单项 / 表格 / 工具卡
  ↓
应用 / 保存按钮
```

通用规则：
- 顶部导航保持全局一致。
- 左侧导航用于一级功能模块切换。
- 系统设置内部使用横向标签页区分子模块。
- 内容区采用白色背景，外层为浅灰页面背景。
- 配置项应按业务含义分组，不应无序堆叠。
- 每个配置分组应有明确标题，可使用主色竖线强调。
- 操作按钮应靠近对应分组，避免用户不清楚保存范围。
- 全局保存类按钮可固定在页面底部或内容区底部。
- 高风险配置应配合说明、tooltip 或二次确认。

### 系统设置横向标签页规范

系统设置页顶部应使用横向标签页组织不同配置模块。

常见标签：
- 基本设置
- 数据导出
- 数据清理
- CDN 设置
- 系统工具

标签规则：
- 激活标签使用主色文字与底部指示线。
- 标签数量建议 3–7 个。
- 标签区应位于顶部导航下方、内容区上方。
- 标签页切换时仅替换主内容区，不应改变左侧导航结构。
- 标签命名应短而明确，建议 4–6 字。

### 基础设置页模板

基础设置页用于承载大量全局配置项，适合采用长页面分组结构，并提供右侧锚点目录辅助跳转。

#### 页面结构

```text
顶部导航 + 左侧系统导航
  ↓
系统设置横向标签页
  ↓
基础设置内容区
  ↓
右侧锚点目录
  ↓
多个配置分组
```

#### 配置分组

基础设置页可包含以下配置分组：

- 服务管理
- 任务管理设置
- 分组管理设置
- 升级文件上传账号
- 安全大脑行为分析
- 密码加密设置
- SSO 配置
- SMTP 服务器设置
- 客户端通信设置
- 企业微信机器人告警配置
- 时间同步设置
- 外部访问中心配置
- 远程托管服务
- 360GPT
- 管控中心界面定制

分组规则：
- 每个分组顶部使用标题 + 主色竖线。
- 分组之间使用浅灰分割线。
- 每个分组内只放与当前主题相关的配置。
- 配置项较少时，按钮可放在该分组底部。
- 配置项较多时，按钮可放在分组右侧或底部统一区域。
- 每个分组的保存按钮文案建议使用 `应用`。
- 不同分组的保存范围应明确。

#### 右侧锚点目录

当配置项较多时，应提供右侧锚点目录。

锚点目录规则：
- 固定在内容区右侧。
- 展示所有主要配置分组名称。
- 当前分组使用主色圆点或主色文字高亮。
- 点击锚点可滚动到对应分组。
- 锚点目录应悬浮于内容区上方，但不遮挡主表单。
- 长页面滚动时锚点目录可保持固定。

#### 配置项控件

基础设置页可使用以下控件：

- 单选框：开启 / 关闭、统一设置 / 分别设置
- 开关：SSO 开关、流控开关、自定义界面开关
- 滑块：下载速率、阈值、限速
- 数字输入框：任务有效期、端口、天数
- 输入框：用户名、IP、地址、URL、appid、appkey
- 密码输入框：密钥、密码、token
- 下拉选择器：认证方式、安全连接、回调方式
- 复选框：勾选启用某配置项
- 上传或图片展示：界面定制 logo、图标、系统名称

控件规则：
- 表单标签左对齐。
- 必填项使用红色星号。
- 输入框宽度统一。
- 控件说明可放在控件右侧或下方。
- 长说明建议使用浅蓝信息提示块。
- 高风险说明可用 tooltip，不应大段堆叠在表单中。
- 禁用态输入框应使用浅灰底并降低文字对比度。
- 密码和密钥字段默认脱敏显示。

#### 界面定制配置

界面定制区域适合展示品牌相关内容。

可配置项：
- 登录界面图标
- 管控界面图标
- 浏览器页面图标
- 管控产品名称
- 是否开启自定义界面

规则：
- Logo 图片应以预览形式展示。
- 图片旁应说明适用位置、支持格式和尺寸限制。
- 系统名称输入框应限制长度。
- 品牌配置保存后应提示可能需要刷新页面生效。
- 不同 logo 类型应保持独立配置，不应混用。

### 数据导出设置页模板

数据导出设置页用于配置数据导出开关、外发方式、外发地址和业务日志范围。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：数据导出
  ↓
数据导出方式
  ↓
IP / 端口配置
  ↓
可导出的 IP 列表表格
  ↓
转发配置
  ↓
外发业务配置
  ↓
底部应用按钮
```

#### 数据导出方式区

配置项建议包括：
- 数据导出：开启 / 关闭
- 数据外发方式：Syslog 外发等
- IP 地址
- 端口
- 添加 IP 和端口

规则：
- 开启 / 关闭使用单选框。
- 外发方式使用下拉选择器。
- IP 和端口输入框横向排列。
- 添加按钮使用描边按钮。
- 配置列表为空时展示空状态。

#### 可导出列表表格

表格字段建议包括：
- 可导出的 IP 列表
- 端口列表
- 操作

规则：
- 表格表头使用浅灰底。
- 表格无数据时展示居中空状态。
- 空状态使用轻量图标 + `暂无数据`。
- 表格区不应过高，避免空白过大。
- 添加 IP 后应显示在表格中。

#### 转发配置

转发配置可包含：
- 转发配置模式
- 默认模式
- Unicode 字符模式
- JSON 标准说明

规则：
- 使用单选框表达互斥配置。
- 每个选项后可附带灰色解释文案。
- 默认模式应优先放在第一项。

#### 外发业务配置

外发业务配置可使用“业务类型 + 业务日志标签”结构。

业务类型示例：
- 资产中心
- 风险管理
- 终端防御
- 勒索防护
- APT 防护
- 上网管理
- 数据安全
- 能耗管理
- 运维日志

业务日志展示规则：
- 每个业务类型占一行。
- 左侧为业务类型与图标。
- 右侧为可导出日志标签组。
- 日志标签使用浅灰描边小标签。
- 标签数量较多时可换行展示。
- 行之间使用浅灰分割线。
- 业务配置按钮应位于配置区上方或左上角。

### 数据清理设置页模板

数据清理设置页用于展示日志存储分布，并配置不同业务日志的保留时间。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：数据清理
  ↓
日志存储分布图表
  ↓
设置日志保留时间
  ↓
业务分类配置表单
  ↓
终端连接策略
  ↓
底部应用按钮
```

#### 日志存储分布图表

日志存储分布适合使用柱状图或堆叠柱状图。

图表规则：
- 横轴展示业务分类，如实时对抗、病毒查杀、漏洞管理、数据保护、外设管理、上网管理等。
- 纵轴展示空间或容量。
- 鼠标悬停时展示 tooltip，包含总量和各日志类型占比。
- 柱状图颜色可按业务分类使用低饱和彩色，但不应过度鲜艳。
- 图表下方或右侧不应堆叠过多说明。

#### 日志保留时间设置

设置区应按业务大类纵向排列。

每个业务大类可包含：
- 统一设置
- 分别设置
- 保留时间下拉选择器
- 各日志子项保留时间

规则：
- 统一设置与分别设置使用单选框。
- 统一设置选中时，只显示一个保留时间下拉框。
- 分别设置选中时，展示该业务下多个日志项。
- 日志项按 2–3 列栅格排列。
- 未启用的下拉框应显示禁用态。
- 保留时间选项示例：半年、一年、永久、自定义。
- 大量日志配置应保持充分行距，避免拥挤。

#### 终端连接策略

终端连接策略可配置：
- 不清理离线终端
- 自动清除离线若干天的终端
- 天数输入框

规则：
- 单选框表达互斥策略。
- 天数使用数字输入框。
- 单位“天”放在输入框后。
- 该配置影响范围较大，建议提供说明 tooltip。

### CDN 设置页模板

CDN 设置用于配置下载节点、存储节点和流控策略。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：CDN 设置
  ↓
日志存储分布 / CDN 配置区
  ↓
CDN 下载节点配置
  ↓
下载节点列表
  ↓
存储节点配置
  ↓
存储节点列表
  ↓
底部保存按钮
```

#### CDN 配置区

配置项建议包括：
- CDN 配置：开启 / 关闭
- CDN 下载节点：IP 地址、端口、备注信息、流控开关
- 添加下载节点
- 存储节点：IP 地址、端口
- 添加存储节点

规则：
- 开启 / 关闭使用单选框。
- 节点输入项横向排列。
- 添加节点按钮使用描边按钮。
- 流控开关使用复选框或开关。
- 节点列表为空时展示空状态。
- 下载节点与存储节点之间使用分割线区分。
- 底部保存按钮使用主按钮。

#### 节点表格

下载节点表格字段：
- IP 地址
- 端口
- 备注
- 流控开关
- 操作

存储节点表格字段：
- IP 地址
- 端口
- 操作

规则：
- 表头使用浅灰背景。
- 空状态居中显示。
- 操作列位于最右侧。
- 节点删除应二次确认。
- IP 和端口需校验格式。

### 系统工具页模板

系统工具页用于展示可下载工具、升级工具和服务器工具入口。

#### 页面结构

```text
顶部导航 + 左侧导航
  ↓
系统设置标签页：系统工具
  ↓
工具卡片网格
```

#### 工具卡片

工具卡片可包含：
- 工具图标
- 工具名称
- 简短说明
- 下载按钮
- 下载下拉菜单

工具示例：
- 外联靶机
- 离线升级工具
- 升级服务器

卡片规则：
- 卡片采用 3 列布局。
- 图标居中放置。
- 标题居中，字重略强。
- 描述文字 1–2 行。
- 下载按钮位于卡片底部居中。
- 存在多个安装包时，按钮可带下拉菜单。
- 下拉菜单示例：Windows 安装包、Linux 安装包。
- 卡片之间保持等宽和等高。
- 工具页不应加入复杂图表和长表单。

### 系统设置表单分组规范

系统设置页表单复杂度较高，必须通过分组降低阅读压力。

分组规则：
- 每组只处理一个配置主题。
- 每组标题应简短明确。
- 多组配置之间使用细分割线。
- 每组内部表单项左对齐。
- 同类控件宽度保持一致。
- 每组保存按钮与该组配置保持视觉关联。
- 涉及全局影响的配置应有说明文案。
- 表单项数量较多时应使用右侧锚点目录或分屏布局。

### 系统设置安全与校验规范

系统设置通常影响全局服务，应明确安全校验。

需要校验的字段：
- IP 地址
- 端口
- URL
- Webhook 地址
- SMTP 地址
- 邮箱
- appid
- appkey
- 密码
- token
- NTP 地址
- 天数
- 速率阈值

安全规则：
- 密钥类字段默认脱敏。
- 高风险配置保存前应确认。
- 禁用关键服务前应提示影响范围。
- 外发配置应校验连通性或提供测试连接按钮。
- SMTP 配置应提供发送测试邮件按钮。
- 时间同步设置应展示当前系统时间。
- 保存成功后展示全局成功提示。
- 保存失败时展示失败原因。
- 配置未保存离开页面时应提示。

### 系统设置文案与假数据规范

常用文案：
- 开启 / 关闭
- 应用 / 保存
- 添加 IP 和端口
- 添加下载节点
- 添加存储节点
- 暂无数据
- 业务配置
- 发送测试邮件
- 下载
- 立即下载

示例数据：
- IP 地址：`10.209.213.159`
- 端口：`8022`
- URL：`http://10.217.104.2`
- NTP 地址：`time1.cloud.tencent.com`
- Webhook：`请输入 webhook URL`
- 速率：`512 KB/s`
- 保留时间：`半年`
- 时间：`2024-01-04 09:47:52`

文案规则：
- 配置项名称建议 4–10 字。
- 按钮文案建议 2–6 字。
- 说明文案尽量不超过 2 行。
- Tooltip 只解释关键风险或专业概念，不应承载长篇说明。
- 空状态文案统一使用 `暂无数据`。

### 系统设置 AI 生图约束

- 系统设置页面应以表单配置和管理效率为主，不要做成 Dashboard。
- 基础设置页必须体现长页面分组和右侧锚点目录。
- 数据导出页必须体现开启/关闭、外发方式、IP/端口、导出列表和业务日志配置。
- 数据清理页必须包含日志存储分布图表和大量保留时间配置表单。
- CDN 设置页必须体现下载节点、存储节点、空状态和保存按钮。
- 系统工具页必须体现工具卡片和下载按钮。
- 配置项较多时必须分组，不要平铺成无层级长表单。
- 高风险配置应配合说明、tooltip 或确认逻辑。
- 保存按钮应贴近对应配置范围，不要让用户不清楚保存哪一部分。
- 不要使用大面积插画、营销背景或复杂装饰。
- 不要把系统设置页面生成成用户列表或数据看板。
- 不要遗漏开启/关闭、应用/保存、空状态等关键配置状态。
- 不要出现乱码 URL、错误 IP 格式或无意义端口。

### 系统设置 Prompt 模板

#### 基础设置页 Prompt

```text
生成一张企业级 B 端系统设置基础设置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置横向标签页，当前选中基本设置。主内容区为长页面表单，按服务管理、任务管理设置、分组管理设置、升级文件上传账号、安全大脑行为分析、密码加密设置、SSO配置、SMTP服务器设置、客户端通信设置、企业微信机器人告警配置、时间同步设置、外部访问中心配置、远程托管服务、360GPT、管控中心界面定制等分组排列。每个分组使用科技绿竖线标题、输入框、单选框、开关、数字输入框、下拉框、说明 tooltip 和应用按钮。右侧显示锚点目录，当前分组高亮。整体白底浅灰背景、结构清晰、适合全局配置管理。
```

#### 数据导出设置页 Prompt

```text
生成一张企业级 B 端系统设置数据导出页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置横向标签页，当前选中数据导出。内容区包含数据导出方式分组，展示开启/关闭单选框、数据外发方式下拉框、IP地址输入框、端口输入框和添加IP和端口按钮。下方展示可导出的IP列表表格，表头包含可导出的IP列表、端口列表和操作，表格为空时显示居中的暂无数据空状态。页面下半部分包含转发配置单选项和外发业务配置表，按资产中心、风险管理、终端防御、APT防护、上网管理、数据安全等业务类型展示大量浅灰日志标签。底部有应用按钮，整体规整、清爽、主色科技绿。
```

#### 数据清理设置页 Prompt

```text
生成一张企业级 B 端系统设置数据清理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含顶部导航、左侧系统导航和系统设置标签页，当前选中数据清理。上方展示日志存储分布柱状图，横轴为实时对抗、病毒查杀、漏洞管理、数据保护、外设管理、上网管理等业务类型，鼠标悬停 tooltip 展示实时对抗 1.01GB 及各日志占比。下方为设置日志保留时间区域，按资产中心、风险管理、终端防御、APT防护、上网管理、数据安全、能耗管理、运维日志等业务大类分组，每组包含统一设置/分别设置单选框和多个保留时间下拉框，默认显示半年。底部包含终端连接策略和应用按钮。页面信息密度较高但排列整齐，白底浅灰背景，科技绿主色。
```

#### CDN 设置页 Prompt

```text
生成一张企业级 B 端系统设置 CDN 设置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置标签页，当前选中 CDN 设置。内容区包含 CDN 配置，使用开启/关闭单选框。下载节点配置区域包含 IP 地址输入框、端口输入框、备注信息输入框、流控开关和添加下载节点按钮，下方是下载节点表格，表头包含 IP 地址、端口、备注、流控开关、操作，空数据时显示居中暂无数据。下方存储节点区域包含 IP 地址、端口输入框、添加存储节点按钮和存储节点表格，同样展示暂无数据空状态。底部有绿色保存按钮。整体简洁、留白充足、表单和表格层级清晰。
```

#### 系统工具页 Prompt

```text
生成一张企业级 B 端系统设置系统工具页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧系统导航和系统设置标签页，当前选中系统工具。主内容区为三列工具卡片网格，分别为外联靶机、离线升级工具、升级服务器。每张卡片包含居中的绿色科技风图标、工具名称、1到2行说明文字和立即下载按钮。升级服务器卡片的下载按钮展开下拉菜单，包含 Windows 安装包和 Linux 安装包。整体白色卡片、浅灰背景、低饱和科技绿主色，布局简洁。
```

### 系统设置 Negative Prompt

```text
不要把系统设置页做成 Dashboard，不要加入无关图表，不要使用大面积插画，不要让配置项无分组堆叠，不要省略保存或应用按钮，不要让用户不清楚保存范围，不要把基础设置生成成列表页，不要把数据导出页缺少 IP/端口配置，不要让数据清理页缺少保留时间下拉框，不要让 CDN 设置页缺少空状态和节点表格，不要让系统工具页出现复杂表格，不要使用错误格式的 IP、URL、端口和邮箱。
```

### 系统设置生成验收标准

| 检查项 | 标准 |
|---|---|
| 页面类型 | 能区分基础设置、数据导出、数据清理、CDN 设置、系统工具 |
| 标签页 | 系统设置横向标签清晰，当前项高亮 |
| 配置分组 | 分组标题明确，配置项不混乱堆叠 |
| 表单控件 | 输入框、选择器、开关、单选框、数字输入框使用合理 |
| 保存范围 | 应用或保存按钮位置明确 |
| 安全提示 | 高风险配置有说明、tooltip 或确认逻辑 |
| 空状态 | 表格无数据时展示统一暂无数据 |
| 数据导出 | 开关、外发方式、IP/端口、业务日志配置完整 |
| 数据清理 | 日志分布图与保留时间设置完整 |
| CDN 配置 | 下载节点、存储节点、节点表格和保存按钮完整 |
| 系统工具 | 工具卡片、说明、下载按钮和下拉菜单清晰 |
| 生图质量 | 文字基本可读，无明显乱码、错位和控件变形 |

---
