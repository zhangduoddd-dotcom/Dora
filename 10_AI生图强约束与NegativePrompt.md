# AI生图强约束与NegativePrompt

本文件用于在具体页面生成时提供短上下文约束。不要整套规范一次性投喂给 AI；每次只组合“全局基础规范 + 对应页面模板 + 本文件 + 当前页面内容”。

## 调用原则

每次生成单个页面时，只调用：

```text
01_全局基础规范.md
+ 当前页面模板文件
+ 10_AI生图强约束与NegativePrompt.md
+ 当前页面业务内容
```

不要同时在一个 Prompt 中混入 Dashboard、列表页、详情页、表单页、系统设置页等多个页面模板。

## 通用强 Prompt 前缀

```text
生成一张企业级 B 端安全管理平台高保真 UI 界面效果图，简体中文，16:9，4K，正视角平面 UI，无透视、无设备外框、无浏览器外壳。整体为深色顶部导航、浅色左侧导航、浅灰页面背景、白色内容卡片、科技绿色主色。顶部导航高度克制，左侧导航宽度不超过页面六分之一，主内容区左右边距一致，模块间距统一，所有组件对齐整齐。
```

## 页面类型调用矩阵

| 要生成的页面 | 必须调用文件 | 禁止混入 |
|---|---|---|
| Dashboard / 工作台 | 03_Dashboard工作台模板.md | 高级搜索列表、长表单 |
| 标准列表页 | 04_列表页模板.md | 大面积图表、Dashboard 风格 |
| 左筛选列表页 | 04_列表页模板.md | 把左筛选栏做成系统导航 |
| 高级搜索列表页 | 04_列表页模板.md | 指标卡、图表看板 |
| 抽屉详情页 | 05_详情页模板.md | 输入表单、必填星号 |
| 独立详情页 | 05_详情页模板.md | 查询区、批量操作 |
| 独立表单页 | 06_表单页模板.md | 表格分页、Dashboard 图表 |
| 抽屉表单页 | 06_表单页模板.md | 详情图表、分析评估模块 |
| 用户管理页 | 07_用户管理模板.md | 图表看板 |
| 系统设置页 | 08_系统设置模板.md | Dashboard 图表堆叠 |
| 系统通用配置页 | 09_系统通用配置模板.md | 营销插画、大屏风格 |

## 页面类型短约束

### Dashboard / 工作台

- 以指标卡和图表为主，表格只是辅助。
- 首屏建议 4–6 个指标卡、3–4 个图表，最多 1 个表格。
- 图表卡片采用规则栅格布局，不做大屏可视化。
- 顶部可有风险告警横幅或摘要区。

Prompt 片段：

```text
这是 Dashboard 工作台页面，以指标和图表为主。首屏上方是一条风险告警横幅和 4 个核心指标卡，中部使用 2 列图表布局，下方放一个简洁表格或资源状态卡。图表数量控制在 4 个以内，所有卡片等间距排列。
```

### 列表页

- 以表格为主体，不要生成图表。
- 查询区只占上方小面积，表格卡片占页面主要空间。
- 表格展示约 10 行、6–10 列，右侧有操作列，底部有分页。
- 分页紧贴表格底部。

Prompt 片段：

```text
这是列表页，以表格为主体，不要生成图表。查询区只占上方一小块，表格卡片占据页面主要空间。表格展示约 10 行数据、8 列字段、浅灰表头、右侧操作列和底部分页。
```

### 详情页

- 以单个对象的信息分组展示为主。
- 抽屉详情必须右侧贴边，有遮罩、标题、关闭按钮。
- 独立详情必须有面包屑/返回、对象摘要、分组内容。
- 图表数量控制在 3 个以内。

Prompt 片段：

```text
这是详情页，以单个对象的信息分组展示为主。页面必须有明确标题、状态标签和分组结构。抽屉详情从右侧滑出，背景页面加灰色遮罩，顶部固定标题和关闭按钮。
```

### 表单页

- 以字段录入和保存为主。
- 标签列统一宽度，输入框左边缘对齐。
- 独立表单不要让输入框横跨全屏。
- 抽屉表单必须右侧贴边，有遮罩、标题、关闭按钮和底部操作栏。
- 复杂表单必须使用分组、tabs、步骤条或左树。

Prompt 片段：

```text
这是表单页面，以字段录入和保存为主。标签列统一宽度，所有输入框左边缘对齐，控件宽度一致。抽屉表单从右侧滑出，背景灰色遮罩，顶部标题和关闭按钮固定，底部确认取消按钮固定。
```

### 系统设置页

- 以分组配置项为主，不是数据看板。
- 顶部有系统设置二级标签页。
- 内容区是白底长表单，每个配置分组用绿色竖线标题区分。
- 右侧可有锚点目录。
- 每个配置分组有独立应用/保存按钮。

Prompt 片段：

```text
这是系统设置页，以分组配置项为主，不是数据看板。内容区使用白底长表单，每个配置分组用绿色竖线标题和浅灰分割线区分，输入框、单选、开关、下拉框左对齐，右侧有窄锚点目录。
```

## 通用 Negative Prompt

```text
不要生成营销海报，不要生成概念大屏，不要使用三维透视，不要使用手机电脑设备壳，不要使用复杂背景纹理，不要大面积渐变，不要过度玻璃拟态，不要厚重阴影，不要粗黑边框，不要文字乱码，不要表格错位，不要控件大小不一致，不要页面元素漂浮，不要混淆 Dashboard、列表页、详情页和表单页。
```

## 常见失败结果与修正 Prompt

| 失败现象 | 追加修正 Prompt |
|---|---|
| 页面像营销海报 | 请按企业后台系统截图生成，不要海报化，不要大标题视觉，不要装饰性背景。 |
| 页面像数据大屏 | 请使用浅色后台管理界面，不要深色大屏，不要发光网格，不要全屏可视化。 |
| 导航过宽 | 左侧导航宽度不超过页面六分之一，顶部导航高度不超过页面十分之一。 |
| 内容太松散 | 提高信息密度，表格展示约 10 行数据，卡片间距统一但不要过大。 |
| 内容太拥挤 | 减少模块数量，保留 3–4 个核心模块，卡片内边距充足。 |
| 表格不可读 | 表格表头浅灰底，8 列以内，10 行数据，列宽均衡，分页紧贴底部。 |
| 表单错位 | 标签列统一宽度，所有输入框左边缘对齐，字段垂直间距一致。 |
| 抽屉像弹窗 | 右侧贴边抽屉，占页面宽度约 60%，背景页面有灰色遮罩。 |
| 列表页出现图表 | 这是列表页，以表格为主体，不要生成任何图表。 |
| 文字乱码严重 | 减少正文文字，只保留短中文标题、表头、按钮和少量数字。 |

## 生图硬性验收清单

| 检查项 | 合格标准 |
|---|---|
| 页面类型 | 一眼能看出是 Dashboard、列表、详情、表单、系统设置中的哪一类 |
| 导航结构 | 顶部导航、左侧导航比例正确 |
| 主内容比例 | 主体模块占比合理，不拥挤、不松散 |
| 模块顺序 | 与模板结构一致，没有随机堆叠 |
| 表格质量 | 表头、数据行、操作列、分页完整 |
| 表单质量 | 标签、控件、必填、保存按钮对齐清楚 |
| 图表数量 | Dashboard 图表不过量，列表页不出现无关图表 |
| 抽屉质量 | 遮罩、标题、关闭、底部操作完整 |
| 色彩控制 | 主色不滥用，状态色只用于状态 |
| 文字密度 | 中文文字数量适中，没有大片乱码 |
