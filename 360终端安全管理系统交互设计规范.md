# 360终端安全管理系统交互设计规范

## 一、页面容器搭建规则

该系统采用典型的**企业级后台管理系统布局**，整体为「顶部导航栏 + 左侧边栏 + 主内容区」的三层嵌套结构，各模块容器规则如下：

### 1.1 整体布局容器

采用经典的 **Flex 水平 + 垂直嵌套布局**，外层为全屏容器，内部分为三大模块：

| 模块      | 尺寸 / 定位                                | 容器规则                                   |
| ------- | -------------------------------------- | -------------------------------------- |
| 顶部单行导航栏 | 高度固定 `56px`，宽度 100%                    | 深色背景，内部元素水平均匀分布，左右对齐（左侧品牌区、右侧用户 / 功能区） |
| 左侧边栏    | 展开时宽度固定 `184px`，收起时宽度固定 `56px`，高度 100% | 浅色背景，内部为垂直导航菜单，底部固定版本号，菜单支持二级选中态高亮     |
| 主内容区    | 剩余宽高自适应                                | 白色背景，内部按功能模块分块，所有业务容器均在此区域内排列          |

### 1.2 各模块容器搭建细节

#### 1.2.1 顶部导航栏容器

- **容器规则**：
  - 高度固定 `56px`，背景为深灰 / 黑色，与下方内容区形成明确分隔。
  - 内部采用 `flex` 水平布局，分为左、中、右三部分：
    - 左侧：品牌 Logo + 系统名称 + 本级中心下拉菜单。
    - 中间：安全状态提示文本。
    - 右侧：可视化下拉、功能图标（通知 / 设置 / 安全）、用户头像 / 账号下拉。
  - 容器内元素垂直居中对齐，间距固定，不随内容变化挤压变形。

#### 1.2.2 左侧边栏容器

- **容器规则**：
  - 展开宽度固定 `184px`，收起时宽度固定 `56px`，高度 100%，容器颜色使用【Gray 中性 /gray-1】`\#FFFFFF`为底色。

#### 1.2.3 主内容区容器

主内容区为所有业务模块的父容器，内部按「**页面标题区 → 内容区**」垂直排列，各子容器规则如下：

##### （1）页面标题容器

- 高度固定 `48px`，宽度 100%，包含页面头部标题文本等。

##### （2）内容容器

1. **色彩规则**：**内容容器**使用 【Gray 中性 /gray-3 白背景 hover】`\#F2F3F5`为底色，内部至少有一个子容器容器颜色使用【Gray 中性 /gray-1】`\#FFFFFF`为底色；
2. 内容容器中子容器常见组合说明：列表页包含「搜索区域」「表格及操作区域」；详情页包含「详情区域」；表单页包含「表单区」「操作区」；
3. 间距圆角规则：
   - **内容容器**内部上下左右采用固定间距 16px；
   - **内容容器**内子容器间水平 / 垂直采用固定间距 16px；
   - **内容容器内子**容器边框圆角值为固定 8px。

### 1.3 通用容器设计规范

1. **响应式规则**：
   - 顶部导航栏、左侧边栏尺寸固定，不随窗口缩放变化。
   - 主内容区自适应窗口宽度，可根据内容调整，超出时显示水平滚动条。

## 二、基础版搜索栏规范

### 2.1 整体容器

- **默认状态**：容器默认纯白色，铺满外部容器，圆角统一 8px。
- **容器边距**：上下左右内边距统一为 24px。
- **响应式规则**：横向拉伸时，搜索项、功能按钮及元素间距保持相对固定；横向收缩空间不足时，搜索项自动换行排列。
- **容器内间距**：内部垂直间距统一为 20px，水平间距为 32px。

### 2.2 搜索项布局规范

#### 2.2.1 通用布局形式

所有搜索项均采用**标题 + 输入控件**横向组合形式，标题与输入控件垂直居中对齐。

#### 2.2.2 间距与尺寸规范

- 单个搜索项内部，标题与输入控件间距统一 8px。
- **输入控件**：最小宽度限制 200px，最大宽度限制 260px，全页面搜索项输入控件宽度保持一致。
- **标题字段**：最大字数 8 个汉字，14 号字体下最大宽度限制 112px，文字统一右对齐，纵向单列标题与输入控件宽度整体对齐。
- 多行搜索项布局时，所有标题宽度统一以字数最多的标题宽度为标准宽度进行约束；
- 单行搜索项布局时，标题宽度可自适应，无需额外对齐约束。

#### 2.2.3 占位文案规范

- 输入类控件：统一占位文案为 “请输入”；如有其他说明逗号后增加说明，如 “请输入，支持模糊搜索”。
- 下拉选择类控件：统一占位文案为 “请选择”；如有其他说明逗号后增加说明，如：“请输入，支持多选”。
- 文案溢出处理：占位文案过长时，单行省略展示，鼠标悬停通过文字气泡展示完整内容。

### 2.3 操作按钮规范

- **按钮样式与文案**：搜索按钮采用主按钮样式，固定文案 “搜索”；清空按钮采用辅助按钮样式，固定文案 “清空”。
- **按钮位置与间距**：按钮组整体右对齐，跟随最后一行搜索项布局，与搜索项遵循同一换行逻辑；按钮之间间距统一 8px。

### 2.4 换行规则

- 所有搜索项从左至右依次排列，单行空间无法容纳完整一组标题 + 输入控件时，自动换行排布。
- 操作按钮默认跟随最后一行搜索项，若单行剩余空间不足，按钮可单独换行，整体保持右对齐。
- 搜索项数量较少时，整体跟随容器宽度自适应排布。

### 2.5 示意图

不同搜索项数量对应布局规则示意图如下：

![Image](https://internal-api-drive-stream.larkoffice.com/space/api/box/stream/download/authcode/?code=OTczODg0ZTRjYTE5ZGI4ZTU0ZjIxY2MwMzYwYmUxMWRfMTI1NjljYzdlYWIzYjlkZjllMmVjNjE3MDQwNjhiNjRfSUQ6NzYzNjcwNzI1Mzg4NjU1MzAzNl8xNzc4MDU5NTU0OjE3NzgxNDU5NTRfVjM)

## 三、列表页表格区域 UI 与交互规范

### 3.1 整体设计原则

表格区域需遵循「功能关联明确、操作优先级清晰、视觉层次统一、交互反馈精准」原则，整体采用轻量化企业级 SaaS 风格，打造简洁高效的视觉与交互体验。

视觉层面，表格无外边框、无圆角，仅通过行底部分隔线区分数据层级（分隔线颜色【Gray 中性 /gray-4 - 分割线】#E8EAED），摒弃冗余装饰，突出核心数据内容；结构层面，操作栏、表头、内容区形成稳定统一的信息架构，全程遵循全站设计一致性，最大限度降低用户学习成本，大幅提升高频操作的执行效率，适配企业级后台系统的日常使用场景。

表格区域整体由五大核心部分组成，各模块分工明确、联动顺畅：

1. **表格顶部操作栏**：承载表格全量操作入口，是用户核心操作区域；
2. **表头区域**：定义数据列属性，支持排序、筛选等基础功能；
3. **数据内容区域**：展示核心业务数据，是表格核心展示区；
4. **操作列区域**：针对单条数据的操作入口，实现单行数据精细化操作；
5. **分页区域**：控制数据分页展示，优化大数据量下的加载与浏览体验。

#### 3.1.1 外部容器规范

表格区域需嵌入一个固定样式的外部容器内，容器规范如下：容器圆角为 8px，容器与表格区域的上下左右间距均固定为 24px，确保表格区域与页面其他元素边界清晰、布局规整，适配企业级后台整体视觉风格。

#### 3.1.2 容器内冻结规范

- 固定冻结规则：表格顶部操作栏、表头固定冻结于表格内容区域顶部，分页器固定冻结于表格内容区域底部，形成上下固定、中间可滚动的稳定布局。
- 内容较少适配：当表格内容较少、不足以充满内容区容器时，分页器紧贴最后一行表格内容排布，避免出现无效空白间距，保证布局紧凑规整。
- 多列数据适配：当表格内容列较多时，默认固定首列（数据标识列）和尾列（操作列），仅中间核心数据区域可进行横向 / 纵向滚动。
- 滚动交互逻辑：表格滚动时，操作栏、表头、分页器、首列、尾列均相对外部容器冻结，确保用户浏览任意位置数据时，可随时触发操作、查看数据标识与操作入口，无需回滚页面，提升操作便捷性与浏览体验。

### 3.2 内容规范

#### 3.2.1 表格顶部操作栏规范

##### 3.2.1.1 布局原则

操作栏统一布局在表格表头正上方，整体布局严格遵循四大原则：功能关联分区、操作优先级排序、高频操作显性化、全站一致性布局，杜绝随意排布，保证全平台操作逻辑统一。

操作栏横向划分为三大功能区域，各区域边界清晰、功能互不交叉，具体划分规则如下：

| 区域      | 功能属性               | 对齐方式 |
| ------- | ------------------ | ---- |
| 左侧核心操作区 | 数据新增类操作，属于表格核心入口操作 | 左对齐  |
| 中间批量操作区 | 勾选数据后的批量处理操作       | 左对齐  |
| 右侧全局配置区 | 表格整体维度的配置与操作       | 右对齐  |

区域内所有按钮水平间距统一为 **8px**，禁止随意调整间距，保证视觉整齐统一。

##### 3.2.1.2 左侧核心操作区

本区域仅承载与表格数据新增强关联的操作，是表格数据录入的核心入口，优先级最高。

###### 排序规则

1. **新建**：固定排布在区域最左侧，采用主按钮样式，作为表格核心新增入口，视觉辨识度最高，引导用户优先操作；
2. **导入**：紧邻 “新建” 按钮右侧排布，采用辅助按钮样式，专门用于批量数据导入，补充单条新建的效率短板。

###### 特殊规则

若页面不支持单条数据 “新建” 能力，仅开放批量导入权限时，“导入” 按钮自动升级为主按钮样式，作为唯一的数据新增入口，保证核心操作入口的突出性。

##### 3.2.1.3 中间批量操作区

本区域仅承载勾选多条数据后的批量处理操作，未勾选数据时无实际操作意义，需做好状态联动控制。

###### 排序规则

所有操作按钮严格按照用户使用频率从左至右排列，核心高频操作靠左，固定规则为：导出 → 批量编辑 → 其他批量操作 → 删除，其中**删除按钮固定在区域最右侧**，规避误操作风险。

###### 状态联动

默认无数据勾选状态下，所有批量操作按钮统一置为禁用状态，不可点击；用户勾选任意一条或多条数据后，按钮自动解除禁用，可正常操作，从根源避免误操作与空操作。

###### 特殊规则

当批量操作按钮数量超过 3 个时，执行折叠规则：保留导出、删除等高频核心操作直接展示，其余低频操作统一折叠至 “操作” 下拉菜单中，避免操作栏过于拥挤。同时，按钮命名仅保留核心动词，如删除、导出、编辑，不额外增加 “批量” 前缀，防止与全局操作混淆，保持命名简洁统一。

##### 3.2.1.4 右侧全局配置区

本区域承载整个表格维度的全局操作与配置，不针对单条或多条数据，服务于表格整体浏览与配置需求。

###### 排序规则

按钮从左至右固定排序为：刷新 → 打印 → 全屏 → 导出全部 → 配置表格字段，其中**配置表格字段按钮固定在区域最后一位**。

###### 样式规则

低频辅助操作可采用纯图标按钮，节省横向空间；高频操作保留文字 + 图标或纯文字样式，确保用户一眼识别功能，兼顾美观与实用性。

###### 响应式规则

当页面宽度不足、操作栏出现挤压时，非核心按钮自动折叠至 “更多” 下拉菜单，仅保留 “导出全部”“配置表格字段” 两个核心功能按钮可见，保证小屏设备下核心操作不受影响。

#### 3.2.2 表格基础视觉规范

##### 3.2.2.1 表格整体

- 无外边框、无圆角，整体视觉极简干练；
- 背景色统一为 【Gray 中性 /gray-1】#FFFFFF，适配企业后台浅色视觉体系；
- 仅通过行底部分隔线区分单行数据，分隔线纤细不突兀（分隔线颜色【Gray 中性 /gray-4 - 分割线】#E8EAED），不干扰数据阅读；
- 数据区域支持横向滚动，适配多列数据的展示场景。

###### 横向滚动规则

表格横向滚动时，执行固定规则：表头区域全程固定、首列数据固定、操作列固定，仅中间核心数据区域横向滚动，确保用户滚动时始终能看到数据标识列与操作列，不会丢失数据对应关系。

##### 3.2.2.2 分页器规范

分页器仅在表格非空、存在数据时显示，无数据时隐藏，避免无效组件展示。

###### 使用组件

统一采用【分页栏 - 大】组件，确保全站分页样式一致；默认选中分页器为 20 条 / 页，常规下拉可选数量为：10/20/50/100。

###### 布局规则

分页器固定位于表格底部正下方，整体右对齐排布，与表格底部间距统一为 8px，布局紧凑规整。

##### 3.2.2.3 表头规范

###### 3.2.2.3.1 基础样式

表头作为数据列的标识区，样式统一规范，具体参数如下：

| 属性   | 规范参数                       |
| ---- | -------------------------- |
| 行高   | 40px                       |
| 内边距  | 上下 10px；左右 16px            |
| 字号   | 12px                       |
| 字重   | Medium                     |
| 文本颜色 | #242933（【Gray 中性 /gray-9】） |
| 背景色  | #F8F9FA（【Gray 中性 /gray-2】） |

表头文本统一规则：强制单行显示，超出文本宽度时自动省略（…），鼠标 hover 表头时，通过 Tooltip 完整展示全部文本，兼顾布局整洁与信息完整性。

###### 3.2.2.3.2 表头 Hover 规则

可操作表头：
具备排序、筛选功能的表头为可操作表头，鼠标 hover 时，背景色切换为 【Gray 中性 /gray-3 白背景 hover】#F2F3F5，给出明确的可交互反馈；鼠标移开后，自动恢复默认背景色。

不可操作表头：
仅做数据标识、无排序筛选功能的表头，鼠标 hover 时无任何背景与样式变化，避免误导用户。

##### 3.2.2.4 排序图标规范

###### 3.2.2.4.1 基础规则

排序图标参数统一，确保视觉大小、颜色、间距完全一致，具体规范如下：

| 属性       | 规范参数                       |
| -------- | -------------------------- |
| 尺寸       | 14×14px                    |
| 与文字间距    | 4px                        |
| 默认颜色     | #8A9099（【Gray 中性 /gray-7】） |
| hover 颜色 | #1DB887（【Brand 品牌 /p5】）    |
| 激活颜色     | #00AB7A（【Brand 品牌 /p6】）    |

图标统一采用上下双向箭头样式，直观体现升序、降序两种排序模式。

###### 3.2.2.4.2 交互规则

点击排序图标，循环切换排序状态：升序 → 降序 → 取消排序，循环往复。精准点击上箭头时，直接触发升序；精准点击下箭头时，直接触发降序，当前生效的排序方向对应图标高亮显示，清晰反馈当前排序状态。

##### 3.2.2.5 筛选图标规范

###### 3.2.2.5.1 基础规则

筛选图标基础参数与排序图标保持一致，保证表头内图标视觉统一，具体规范如下：

| 属性       | 规范参数                       |
| -------- | -------------------------- |
| 尺寸       | 14×14px                    |
| 与文字间距    | 4px                        |
| 默认颜色     | #8A9099（【Gray 中性 /gray-7】） |
| hover 颜色 | #1DB887（【Brand 品牌 /p5】）    |
| 激活颜色     | #00AB7A（【Brand 品牌 /p6】）    |

若单个表头同时存在排序与筛选功能，筛选图标固定位于排序图标右侧，两者间距统一为 4px，排布紧凑有序。

###### 3.2.2.5.2 交互规则

点击筛选图标，立即弹出对应列的筛选菜单，菜单内包含当前列全部可筛选项、确定按钮、重置按钮，操作逻辑清晰。

按钮行为

- 确定：应用当前所选筛选条件，刷新表格数据；
- 重置：一键清除当前列所有筛选条件，恢复默认数据展示。

当表头存在生效筛选条件时，筛选图标自动高亮显示，直观提示用户当前列已筛选。

##### 3.2.2.6 内容单元格规范

###### 3.2.2.6.1 基础样式

数据内容单元格样式统一，保证数据阅读舒适，具体参数如下：

| 属性   | 规范参数                       |
| ---- | -------------------------- |
| 行高   | 40px                       |
| 内边距  | 上下 10px；左右 16px            |
| 字号   | 12px                       |
| 字重   | Regular                    |
| 文本颜色 | #525966（【Gray 中性 /gray-8】） |
| 对齐方式 | 左对齐                        |

###### 3.2.2.6.2 文本规则

普通文本：强制单行显示，超出单元格宽度时自动省略，鼠标 hover 单元格时，通过 Tooltip 完整展示全部文本，避免多行文本导致表格高度错乱。

时间字段：格式强制统一为 YYYY-MM-DD HH:MM:SS，全程完整展示，不做省略处理，保证时间信息精准无误。

###### 3.2.2.6.3 行背景与交互

默认状态
单元格背景色为 【Gray 中性 /gray-1】#FFFFFF，行底部分隔线颜色为 【Gray 中性 /gray-4 - 分割线】#E8EAED，线条纤细，清晰区分各行数据。

Hover 状态
鼠标 hover 任意数据单元格时，对应整行背景色统一切换为 【Gray 中性 /gray-3 白背景 hover】#F2F3F5，包含内容列与操作列，实现整行高亮，方便用户快速定位单行数据，避免误操作。

##### 3.2.2.7 操作列规范

###### 3.2.2.7.1 基础规则

操作列内所有按钮统一采用【文字按钮 - 常规】样式，禁止随意更换按钮样式，保证全站操作列视觉一致。

###### 3.2.2.7.2 展示规则

操作项 ≤ 3
所有操作项直接横向展示，无需折叠，方便用户一键点击，提升操作效率。

操作项 \&gt; 3
执行折叠规则：前 2 个高频核心操作项直接展示，第 3 个及以后的操作项统一折叠，显示为 “更多” 文字按钮 + 下箭头图标；鼠标 hover “更多” 按钮时，自动展开下拉菜单，展示剩余所有操作项。

###### 3.2.2.7.3 行联动规则

鼠标 hover 操作列任意位置时，对应数据整行同步高亮，效果与 hover 数据单元格一致（背景色切换为【Gray 中性 /gray-3 白背景 hover】#F2F3F5），强化行关联，让用户清晰感知操作对应的单行数据。

### 3.3 权限与状态规范

#### 3.3.1 权限控制

所有操作按钮需根据用户角色权限，动态做三种处理：正常显示、隐藏、禁用，严格遵循权限管控规则，杜绝越权操作。例如：普通用户默认隐藏 “删除” 按钮、隐藏 “配置表格字段” 按钮；仅管理员账号开放对应高权限操作。

#### 3.3.2 禁用状态

处于禁用状态的按钮，需明确置灰处理，完全不可点击；鼠标 hover 禁用按钮时，自动弹出 Tooltip 提示禁用原因，让用户清晰知晓无法操作的缘由，提升体验。

#### 3.3.3 操作反馈

表格内所有操作均需具备即时反馈，杜绝无响应操作：核心操作触发 Toast 提示、异步操作展示 Loading 状态、操作完成给出明确的成功 / 失败反馈；所有可点击按钮 hover 时，需具备明确的高亮、变色反馈，清晰提示可交互状态，保障交互闭环。针对删除、停用 / 启用等关键操作，需根据业务情景增加二次确认拦截功能，避免对系统造成不可逆影响，示例：点击删除按钮时，弹出确认对话框，反馈文案规范如下 ——「标题」确定删除所选内容吗？「内容」删除后将无法恢复，请谨慎操作。

## 四、标签使用规范

### 4.1 标签通用规则

1. **一致性原则**：当同一系统中有同一维度的标签在不同页面类型出现时，尽量保持样式一致。
2. **标准化原则**：使用系统统一定义的标签样式，优先使用默认标签样式。
3. **替代规则**：当结果类、状态标签在表格中出现时，可使用状态点替代 icon。

### 4.2 标签降噪规则

1. **类别控制**：为保证系统最重要状态清晰，整个系统的面性标签类别≤3 类。
2. **等级区分**：表格中有多个特定等级标签时，尽量只保留一个等级为面性标签，其他等级均使用线性加面性标签；操作状态较多时，非重要状态可用状态点代替 icon。

### 4.3 统一定义的标签样式

#### 4.3.1 等级标签

- 面性标签：用于功能模块中标识**最重要信息、需用户优先识别**的等级。
- 线性加面性标签：用于功能模块中标识**较重要信息、需用户快速识别**的等级。
- 以下为各场景等级标签示意（含图标与文案）：

| 场景分类        | 等级 / 状态             |
| ----------- | ------------------- |
| 告警 - 告警级别   | 紧急、严重、警告、提醒         |
| 安全事件 - 事件级别 | 紧急、严重、警告、提醒         |
| 攻击者 - 威胁等级  | 紧急、高危、中危、低危、无风险、未知  |
| 漏洞等级        | 超危、高危、中危、低危、信息      |
| 脆弱性 - 威胁等级  | 超危、高危、中危、低危、信息      |
| 风险资产 - 风险等级 | 已失陷、高风险、中风险、低风险、无风险 |

![Image](https://internal-api-drive-stream.larkoffice.com/space/api/box/stream/download/authcode/?code=NjExMDdmMGI2ZTI5Yzg0OWJhNzc3N2FkMjZiN2E5ZjVfMWZlNzM5M2M3NDg4ZDNlNmIyZmZiNjkzMTBiZjdjYmVfSUQ6NzYzNjcwNjQxNzEzOTY5ODY0Nl8xNzc4MDU5NTU0OjE3NzgxNDU5NTRfVjM)

#### 4.3.2 状态标签

- 适用场景：在功能模块中提供**解释性 / 描述性的状态 / 结果信息**时，使用「icon + 文字标签」；吸引用户注意、表达正在运行的状态，可使用动画的形式（即线性加面性标签，线性 icon 可设计循环动效）。

![Image](https://internal-api-drive-stream.larkoffice.com/space/api/box/stream/download/authcode/?code=YTkxYjkxZjE2MGRmYzY1Mzg3ZTE2MjQzNzI2NGM3MjNfMTM1MWFhNzFiZjA0NmVmMzk5NDcwY2IwM2FmZjM4MTFfSUQ6NzYzNjcwNjQxOTQ4OTMyODA3NV8xNzc4MDU5NTU0OjE3NzgxNDU5NTRfVjM)

- 以下为各场景状态标签示意（含图标与文案）：

| 场景分类 | 状态 / 结果              |
| ---- | -------------------- |
| 研判结果 | 攻击成功、误报、攻击失败、未研判     |
| 处置状态 | 待处置、处置中、处置完成、忽略、处置失败 |
| 置信度  | 高置信、中置信、低置信、未知       |

![Image](https://internal-api-drive-stream.larkoffice.com/space/api/box/stream/download/authcode/?code=ZTc0MmViNjQ4YWFlNzRmNWY4MzI3ZmZjOGUzY2VjMDlfMmQ3ZTMxOWM1Mjk3NjQ0NjMwNWExZGJiNDExYWZjMjRfSUQ6NzYzNjcwNjQxNTQ4NzA3NzMxNl8xNzc4MDU5NTU0OjE3NzgxNDU5NTRfVjM)

#### 4.3.3 默认标签

- 适用场景：无需区分状态、仅展示标签内容时，可使用默认标签。
- 特殊说明：如需在默认标签中区分信息优先级，可使用蓝色（信息）标签进行强调。
- 示例：`默认标签（如“一般内容”）`、`强调标签（如“强内容”，蓝色样式）`

#### 4.3.4 其他状态标签

- 适用场景：其他非特定的内容，可使用普通多彩标签。
- 样式类型：
  1. 面性标签；
  2. 线性加面性标签；
  3. icon + 文字标签。
- 用色规则：
  - **线性加面性标签**：底色使用对应语义色的 `\-1` 号，文字色使用对应语义色的 `\-7` 号；特殊灰色使用 `\#E8EAED`（gray-4）为底色，`\#525966`（gray-8）为文字色。
  - **面性标签**：底色使用对应语义色的 `\-6` 号，文字色使用 `\#FFFFFF`（gray-1）；
  - **icon 标签**：icon 使用对应语义色的 `\-6` 号为底色，使用 `\#525966`（gray-8）为文字色。

| 色系 | 颜色编号         | 色值         | 说明                  |
| -- | ------------ | ---------- | ------------------- |
| 深红 | Deep red-1   | `\#FCE7E3` | 线性标签底色              |
| 深红 | Deep red-6   | `\#BC0004` | 面性标签 / Icon 底色      |
| 深红 | Deep red-7   | `\#960008` | 线性标签文字色             |
| 红  | Red-1        | `\#FFE8E6` | 线性标签底色              |
| 红  | Red-6        | `\#F0131E` | 面性标签 / Icon 底色      |
| 红  | Red-7        | `\#C90616` | 线性标签文字色             |
| 深橙 | Orange red-1 | `\#FFF3E6` | 线性标签底色              |
| 深橙 | Orange red-6 | `\#F05A0A` | 面性标签 / Icon 底色      |
| 深橙 | Orange red-7 | `\#C94000` | 线性标签文字色             |
| 橙  | Orange-1     | `\#FFF8E6` | 线性标签底色              |
| 橙  | Orange-6     | `\#F58A02` | 面性标签 / Icon 底色      |
| 橙  | Orange-7     | `\#CF6E00` | 线性标签文字色             |
| 黄  | Yellow-1     | `\#ffffe6` | 线性标签底色              |
| 黄  | Yellow-6     | `\#DFBC00` | 面性标签 / Icon 底色      |
| 黄  | Yellow-7     | `\#B89600` | 线性标签文字色             |
| 绿  | Green-1      | `\#EFF8EE` | 线性标签底色              |
| 绿  | Green-6      | `\#52C41A` | 面性标签 / Icon 底色      |
| 绿  | Green-7      | `\#389E0D` | 线性标签文字色             |
| 蓝  | Blue-1       | `\#E6F7FF` | 背景色                 |
| 蓝  | Blue-6       | `\#1890FF` | 面性标签 / Icon 底色      |
| 蓝  | Blue-7       | `\#096DD9` | 线性标签文字色             |
| 灰  | gray-4       | `\#E8EAED` | 特殊灰色线性标签底色          |
| 灰  | gray-6       | `\#AEB2B8` | 面性标签 / Icon 底色      |
| 灰  | gray-8       | `\#525966` | 特殊灰色线性标签 / Icon 文字色 |

![Image](https://internal-api-drive-stream.larkoffice.com/space/api/box/stream/download/authcode/?code=YzRlYTVjNTIxYTc3NGY5M2M4ZWY0MzE1ZjA2OTBjMjVfOTA2NGRlOTQ0MmQzYjAyYTJmMWYzNzliMzIyOTNhMDNfSUQ6NzYzNjcwNjQxOTc0MzgwNDM3OF8xNzc4MDU5NTU0OjE3NzgxNDU5NTRfVjM)

### 4.4 状态点 + 文案规范

#### 4.4.1 使用规范

- 用途：用于表示状态的小圆点（6\*6px）与文字间距 4px，常用于表示**非重要的结果、严重等级、操作进程**等。
- 颜色规范：状态点使用对应语义色的 `\-6` 号颜色，无需搭配 icon 和文字色（贴合状态点 6\*6px 小圆点的视觉定义）。

#### 4.4.2 核心语义色值

| 颜色 | 颜色编号     | 色值         | 语义说明               |
| -- | -------- | ---------- | ------------------ |
| 红色 | Red-6    | `\#F0131E` | 失败、阻断、不可用、严重异常     |
| 橙色 | Orange-6 | `\#F58A02` | 异常、预警、轻微问题、不阻断流程   |
| 蓝色 | Blue-6   | `\#1890FF` | 进行中、待处理、待审批、正常流转   |
| 绿色 | Green-6  | `\#52C41A` | 启动、成功、完成、正常运行      |
| 灰色 | gray-6   | `\#AEB2B8` | 草稿、已停止、用户主动终止、禁用状态 |

> 注：深红色、深橙色为多级等级专属定义色，不使用在流程颜色中。

#### 4.4.3 三大场景用色规则

1. **流程节点状态**
   - 未进入流程 / 草稿 → **灰色**
   - 待处理 / 审批中 / 流转中 → **蓝色**
   - 异常 / 回退 / 不通过 → **橙色**
   - 正常结束流程：完成 / 结束 → **绿色**
   - 异常结束流程：失败 / 失效 / 阻断 → **红色**
   - 用户主动终止且无需关注 → **灰色**
2. **功能开关状态**
   - 启动 / 开启 → **绿色**
   - 停止 / 关闭（无风险） → **灰色**
3. **数据连接状态**
   - 正常传输 / 连接正常 → **绿色**
   - 轻微异常 / 有小问题（不影响传输） → **橙色**
   - 阻断性故障 / 无法传输 → **红色**

#### 4.4.4 约束规则

1. 流程 “未结束” 时，禁止使用红色、绿色。
2. 草稿状态，禁止使用蓝色、橙色。
3. 同一状态只允许使用一种语义色，不混用、不自定义。

> （注：文档部分内容可能由 AI 生成）

