# 系统通用配置模板

## 系统通用配置页面模板规范

这一类页面属于平台后台中的“通用管理配置模板”，主要覆盖组织管理、任务管理、告警中心、告警配置、系统日志、邀请管理、备份与恢复、应用管理、网络配置、检测配置、多级配置以及相关弹窗表单等页面。  
它们的共同特点是：**结构稳定、信息密度中高、操作明确、规则导向强、偏管理后台而非数据分析看板**。

系统通用配置页的设计重点不在视觉表现，而在于：
- 让用户快速完成配置、检索、批量操作、查看状态；
- 保证复杂系统参数的层级清晰与操作可控；
- 通过列表、状态、表单、分页、弹窗等组件形成统一的管理体验。

### 系统通用配置页面分类

```text
系统通用配置模板
├── 树形组织与层级管理
│   └── 组织管理
├── 列表型任务与状态管理
│   └── 任务管理
├── 告警与事件管理
│   ├── 告警中心
│   └── 告警配置
├── 日志与记录管理
│   └── 系统日志
├── 邀请与码类管理
│   └── 邀请管理
├── 资产/数据恢复类管理
│   └── 备份与恢复
├── 应用接入与应用列表管理
│   └── 应用管理
├── 网络与设备基础配置
│   └── 网络配置
├── 检测能力配置
│   └── 文件检测配置
├── 多级节点与上下级接入配置
│   └── 多级配置
└── 弹窗表单模板
    ├── 新增节点弹窗
    └── 编辑节点弹窗
```

### 系统通用配置通用页面骨架

这类页面建议遵循统一的后台骨架：

```text
深色顶部导航
  ↓
浅色左侧一级导航
  ↓
页面标题 / 页签 / 二级切换
  ↓
筛选区 / 检索区
  ↓
工具栏 / 批量操作区
  ↓
主内容区（表格 / 树表 / 长表单 / 配置分组）
  ↓
分页 / 底部操作 / 保存按钮
```

基础规则：
- 顶部导航承载平台全局信息、一级站点导航、消息、用户信息。
- 左侧导航承载系统模块入口，当前项高亮。
- 内容区采用白底卡片或白底容器，置于浅灰页面背景中。
- 筛选区与表格区分开，避免混在一层。
- 列表页默认采用“筛选区 + 工具栏 + 表格 + 分页”的成熟模式。
- 配置页默认采用“局部 tabs + 分组表单 + 保存按钮”的模式。
- 所有“删除、禁用、还原、同步、取消”等具有风险的操作应保留明确语义。

---

### 组织管理页模板

组织管理页用于维护树形组织结构、规则入组信息及组织的增删改查。

#### 页面结构

```text
筛选区（组织名称 + 搜索）
  ↓
工具栏（启用、禁用、全选、已选数量）
  ↓
右上操作（创建组织、导入/导出）
  ↓
树形表格
```

#### 表格字段建议

- 复选框
- 展开/收起箭头
- 组织名称
- 规则入组
- 操作

操作项可包含：
- 新增
- 编辑
- 删除
- 更多

#### 树形表格规则

- 支持多层级缩进展示。
- 父节点需带展开/收起箭头。
- 子级通过缩进表达层级关系。
- 勾选支持批量操作。
- “已选 N 个”应在工具栏中明确展示。
- 右侧操作文案需统一、间距稳定。
- 当层级较深时，应优先保证名称列可读性。

#### 适用场景

- 组织架构管理
- 分组策略管理
- 层级目录管理
- 分类树管理

---

### 任务管理页模板

任务管理页用于展示系统任务状态、执行进度、时间与操作。

#### 页面结构

```text
筛选区（功能模块、发布人、状态）
  ↓
搜索按钮
  ↓
任务列表表格
```

#### 表格字段建议

- 任务
- 发布人
- 下发时间
- 终端完成比例
- 状态
- 截止时间
- 操作

#### 任务状态展示规范

状态表现形式建议：
- 进度条 + 百分比
- 状态圆点 + 文案
- 颜色区分任务状态

示例状态：
- 执行中
- 执行完成
- 已取消
- 已失败

规则：
- 进度条颜色应稳定使用系统语义色。
- 百分比应跟随进度条同列展示。
- 状态文案不要只依赖颜色，必须带文字。
- 进行中状态支持“查看详情 / 取消”。
- 已完成状态通常仅保留“查看详情”。

---

### 告警中心页模板

告警中心页用于查看系统告警事件，并支持按级别、状态、类型进行识别与处理。

#### 页面结构

```text
告警概览统计卡
  ↓
搜索区（告警名称）
  ↓
右上操作（告警配置、快捷入口）
  ↓
告警列表表格
  ↓
分页
```

#### 顶部概览区

建议展示以下指标：
- 全部告警事件
- 紧急待处理
- 待处理
- 已处理
- 已忽略

规则：
- 数值是核心视觉焦点。
- 状态类指标可配合语义色。
- 概览卡应支持点击筛选或联动列表。
- 不宜使用复杂图表，保持轻量。

#### 表格字段建议

- 告警名称
- 告警级别
- 状态
- 告警类型
- 告警方式
- 告警时间
- 告警内容
- 操作

#### 状态与级别规范

告警级别：
- 高危
- 中危
- 低危
- 安全 / 普通

告警状态：
- 待处理
- 已处理
- 已忽略

规则：
- 告警级别建议采用标签样式。
- 状态建议采用小圆点 + 文案组合。
- 告警内容过长时可截断显示。
- 表格中应优先突出级别与状态。

---

### 告警配置页模板

告警配置页用于维护告警策略与启停状态。

#### 页面结构

```text
返回标题区（如：告警配置）
  ↓
搜索区（告警名称 / 创建人）
  ↓
右上操作（新增策略、快捷入口）
  ↓
配置表格
```

#### 表格字段建议

- 告警名称
- 告警级别
- 状态
- 告警类型
- 告警方式
- 创建时间
- 操作

操作项可包含：
- 复制
- 删除
- 编辑
- 启用 / 停用

#### 规则

- 告警策略页面应更偏配置管理，不强调统计信息。
- “新增策略”应放在表格上方右侧。
- 状态需清楚区分已启用 / 已停用。
- 配置类表格的操作文案通常比事件表格更多。

---

### 系统日志页模板

系统日志页用于查看管理员操作日志与系统运行日志。

#### 页面结构

```text
顶部局部 Tabs（管理员操作日志 / 系统运行日志）
  ↓
筛选区（时间、操作人、操作内容）
  ↓
搜索按钮
  ↓
日志表格
```

#### 表格字段建议

- 操作人
- IP 地址
- 操作类型
- 操作内容
- 危险程度
- 操作时间
- 操作

#### 日志页面规则

- 日志页面必须支持时间筛选。
- 适合通过 tabs 切换不同日志类型，而不是多个页面跳转。
- 危险程度应使用标签或状态色展示。
- 操作项一般为“详情”。
- 日志表格应强调可追溯性和清晰检索。

---

### 邀请管理页模板

邀请管理页用于管理邀请码、邀请状态和申请列表。

#### 页面结构

```text
顶部局部 Tabs（邀请加入 / 申请列表）
  ↓
筛选区（组织名称/邀请码、状态）
  ↓
工具栏（删除、刷新邀请码、下载邀请码）
  ↓
右上操作（创建邀请码、导出全部）
  ↓
邀请列表表格
  ↓
分页
```

#### 表格字段建议

- 复选框
- 状态
- 组织名称
- 有效期
- 邀请码
- 审批类型
- 操作

操作项可包含：
- 刷新
- 删除
- 复制邀请码

#### 规则

- 邀请码类页面要突出“复制”和“导出”。
- 状态文案应明确，如已过期、有效中、待审批。
- 批量操作按钮应置于表格上方左侧。
- 分页信息需包含总数、每页数量、页码跳转。

---

### 备份与恢复页模板

备份与恢复页用于查看备份记录并执行备份、还原、下载等操作。

#### 页面结构

```text
右上功能按钮（手动备份 / 自动备份 / 导入备份 / 刷新）
  ↓
备份记录表格
```

#### 表格字段建议

- 备份时间
- 备份内容
- 文件大小
- 备份形式
- 备份状态
- 创建者
- 完成时间
- 备注
- 操作

操作项可包含：
- 删除
- 还原
- 下载

#### 状态展示规范

备份状态建议：
- 备份成功
- 备份中
- 备份失败

规则：
- 备份成功可用绿色状态点。
- 还原与删除属于高风险操作，应有确认提示。
- 表格应突出“备份时间”“状态”“操作”。

---

### 应用管理页模板

应用管理页用于维护应用接入信息与应用识别图标。

#### 页面结构

```text
筛选区（应用名称/URL、所属应用组、创建人）
  ↓
搜索 / 重置 / 展开
  ↓
工具栏（删除、导出、复制）
  ↓
右上操作（新建应用、管理默认图标、快捷入口）
  ↓
应用列表表格
  ↓
分页
```

#### 表格字段建议

- 复选框
- 应用名称
- URL
- 图标
- 开放共享
- 操作

操作项可包含：
- 编辑
- 查看

#### 规则

- 图标列适合用固定尺寸小图标展示。
- URL 需清晰可读，可允许省略过长路径。
- “开放共享”适合使用文本值或状态值。
- 筛选区支持基础筛选 + 展开高级筛选。

---

### 网络配置页模板

网络配置页属于复杂系统基础配置页，特点是：一个页面中包含多个配置分区，每个分区结构不同。

#### 页面结构

```text
页面标题（网络配置）
  ↓
局部 Tabs（接口信息 / 路由管理 / 网络检测 / 可信主机配置 / SSH服务）
  ↓
分区 1：接口信息
  ↓
分区 2：路由管理
  ↓
分区 3：网络检测
  ↓
分区 4：可信主机配置
  ↓
分区 5：SSH 服务
```

#### 接口信息区

字段建议：
- 接口
- 接口开关
- 接口类型
- 接口模式
- 速率
- 连接状态
- 接口信息-IPV4
- 接口信息
- 操作

规则：
- 开关应直接置于表格中。
- 连接状态需用颜色点 + 文案区分运行 / 关闭。
- 支持“编辑 / 详情 / 删除”等操作。

#### 路由管理区

字段建议：
- 出接口
- 网关
- 目标网络 / 掩码
- 状态
- 操作

右上需有：
- 添加按钮

#### 网络检测区

配置项建议：
- 诊断工具类型：PING / 路由跟踪 / TELNET
- 目的 IP / 域名
- 连接测试按钮

规则：
- 使用单选按钮切换检测方式。
- 输入框与操作按钮同一行。
- 检测结果可作为后续扩展区域，不强制默认展示。

#### 可信主机配置区

字段建议：
- 复选框
- IP
- 描述
- 操作

附加能力：
- web 访问控制开关
- 添加按钮
- 批量删除

#### SSH 服务区

配置项建议：
- SSH 服务开关
- 服务端口
- 保存按钮

规则：
- 端口输入应有合法性限制。
- 关闭 SSH 服务时可给予风险提醒。

---

### 检测配置页模板（以文件检测配置为例）

检测配置页面通常包含左侧局部导航、顶部局部 tabs 和多个纵向配置分组。

#### 页面结构

```text
左侧配置导航
  ↓
顶部局部 Tabs（基础配置 / 检测类型设置 / 检测特征设备 / 加密压缩包破解）
  ↓
纵向配置分组
  ├── 文件基础设置
  ├── 检测设置
  ├── 静态检测引擎
  ├── 下载样本查看口令
  ├── 虚拟机网络配置
  ├── 沙箱网关网卡配置
  └── 虚拟机环境
```

#### 文件基础设置

配置项建议：
- 文件检测大小（滑块 + 数字框）
- 虚拟执行策略（单选）
- 复用检测结果（开关）
- 合并机器学习结果（开关）
- 保存 / 重置

规则：
- 滑块适合与数值输入联动。
- 配置说明可用强调色文字提示。

#### 检测设置

配置项建议：
- 沙箱分析时长
- 压缩包解压层数
- 保存按钮

#### 引擎设置

配置项建议：
- 启发式检测引擎开关
- 360AV 检测引擎开关
- 保存按钮

#### 下载样本查看口令

配置项建议：
- 启发式检测引擎口令
- 随机口令
- 自定义口令
- 口令输入框
- 保存按钮

#### 虚拟机网络配置

配置项建议：
- 虚拟机不可联网
- 虚拟机可联网
- 虚拟机使用虚拟网络
- https 解密模式
- 保存按钮

#### 虚拟机环境

建议使用左侧环境列表 + 右侧环境详情。
左侧可包含：
- Windows XP
- Windows 7 32bit
- Windows 7 64bit
- Windows server 2003
- Windows 10
- CentOS 32bit

右侧可展示：
- 系统版本
- 软件版本
- 已安装软件列表
- 虚拟机个数

---

### 多级配置页模板

多级配置页用于配置上下级节点接入与节点管理。

#### 页面结构

```text
左侧局部导航
  ↓
右侧主内容
  ├── 接入上级节点设置
  ├── 接入下级节点设置
  └── 节点表格
```

#### 接入上级节点设置

配置项建议：
- 接入上级节点：是 / 否
- 数据分权同步：是 / 否

#### 接入下级节点设置

配置项建议：
- 允许下级节点接入：是 / 否
- 新增下级节点按钮
- 节点管理表格

表格字段建议：
- 节点名称
- 节点地址
- 描述
- 联系人
- 电话
- 节点状态
- 操作

操作项可包含：
- 编辑
- 配置同步
- 删除

节点状态可包含：
- 节点已接入
- 下级已注销
- 未接入
- 已禁用

#### 规则

- 上级、下级配置应拆成两个分组，避免混在一起。
- 节点表格要配合分页。
- 节点状态需配颜色点和文字说明。
- “配置同步”应区分于 सामान्य编辑操作。

---

### 弹窗表单模板：新增节点 / 编辑节点

这类弹窗适合沉淀为通用“系统配置表单弹窗模板”。

#### 弹窗结构

```text
弹窗标题
  ↓
表单区
  ↓
底部按钮区（取消 / 保存）
```

#### 表单字段建议

- 节点名称（必填）
- 节点地址（必填）
- 访问令牌
- 描述（多行）
- 节点联系人
- 联系人电话

附加能力：
- 访问令牌右侧“复制”按钮

#### 弹窗设计规范

- 弹窗需居中显示。
- 背景遮罩应弱化但可感知。
- 标题栏右上角应有关闭按钮。
- 必填项使用红色星号。
- 多行文本域用于描述。
- 底部按钮右对齐。
- 默认按钮顺序：取消 / 保存。
- 编辑态可预填数据，新增态为空白。

---

### 系统通用配置组件规范

#### 筛选区规范

常见筛选项：
- 输入框
- 下拉框
- 状态选择
- 时间范围
- 搜索按钮
- 重置按钮
- 展开高级筛选

规则：
- 基础筛选项一行展示。
- 搜索按钮使用主按钮。
- 重置按钮使用次按钮。
- 高级筛选可折叠展开。
- 输入框宽度保持统一。

#### 工具栏规范

常见工具按钮：
- 启用
- 禁用
- 删除
- 导出
- 复制
- 刷新
- 下载邀请码
- 新建应用
- 创建组织
- 新增策略

规则：
- 左侧以批量操作为主，右侧以新增/创建为主。
- 高风险按钮不应与主按钮视觉同权。
- 工具栏与表格头之间要有清晰分隔。

#### 表格规范

- 表头使用浅灰底。
- 表格行高统一。
- 文字左对齐优先。
- 操作列固定在右侧。
- 列表为空时需保留空状态。
- 表格分页统一置于底部右侧或底部居右。
- 分页区应包含总数、每页数量、页码、前进后退、页码跳转（可选）。

#### 状态规范

建议统一语义：
- 绿色：成功、启用、已处理、运行中、有效
- 橙色：高危、待处理、警告
- 红色：失败、关闭、异常、注销
- 灰色：禁用、已忽略、未启用

状态呈现方式：
- 标签
- 圆点 + 文案
- 开关
- 进度条

---

### 系统通用配置 AI 生图约束

- 这类页面本质是 B 端后台配置/管理界面，不要做成营销官网。
- 页面风格要克制，强调表格、表单、状态与操作逻辑。
- 不能过度使用卡片堆叠或花哨插画。
- 列表型页面必须体现“筛选区 + 工具栏 + 表格 + 分页”的逻辑。
- 复杂配置页必须体现“分组 + tabs + 保存按钮”的逻辑。
- 多级配置、网络配置、检测配置这类页面必须体现配置分区和表单层级。
- 告警类页面要突出概览状态、级别标签与处理状态。
- 日志类页面要突出可检索性和可追溯字段。
- 弹窗表单必须居中、带遮罩、带取消和保存按钮。
- 所有页面必须保持简体中文。
- 风格统一为浅色极简、安全产品后台风、科技绿色主色。

---

### 系统通用配置 Prompt 模板

#### 组织管理页 Prompt

```text
生成一张企业级安全管理平台的组织管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和白色主内容区。主内容上方为组织名称搜索框和搜索按钮，下方为工具栏，包含启用、禁用、全选、已选数量提示，右上有创建组织和导入导出按钮。主体为树形表格，字段包含组织名称、规则入组、操作，支持多级展开缩进，行尾操作包括新增、编辑、删除、更多。整体风格克制、规整、科技绿色主色、后台管理感强。
```

#### 任务管理页 Prompt

```text
生成一张企业级安全管理平台的任务管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含筛选区，支持功能模块、发布人、全部状态筛选，并有搜索按钮。下方为任务表格，字段包括任务、发布人、下发时间、终端完成比例、状态、截止时间、操作。完成比例使用进度条展示，状态包含执行中和执行完成，操作含查看详情和取消。整体浅色背景、白色内容区、表格清晰、绿色主色。
```

#### 告警中心页 Prompt

```text
生成一张企业级安全管理平台的告警中心页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部展示全部告警事件、紧急待处理、待处理、已处理、已忽略等概览统计卡。中间为告警名称搜索框和搜索按钮，右上包含告警配置按钮。主体为告警列表表格，字段包含告警名称、告警级别、状态、告警类型、告警方式、告警时间、告警内容、操作。告警级别使用标签样式，状态使用彩色圆点加文字，底部有分页。整体风格专业克制。
```

#### 告警配置页 Prompt

```text
生成一张企业级安全管理平台的告警配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部有返回标题“告警配置”，下方是搜索区，支持按告警名称或创建人搜索，右上有新增策略按钮。下方表格字段包含告警名称、告警级别、状态、告警类型、告警方式、创建时间、操作，操作含复制、删除、编辑、启用。页面布局简洁，表格信息清晰，绿色主色突出关键操作。
```

#### 系统日志页 Prompt

```text
生成一张企业级安全管理平台的系统日志页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部包含局部 tabs：管理员操作日志、系统运行日志。下方为筛选区，支持时间范围、操作人、操作内容检索，并有搜索按钮。主体为日志表格，字段包含操作人、IP地址、操作类型、操作内容、危险程度、操作时间、操作。危险程度用标签展示，操作为详情。页面整体清晰规整。
```

#### 邀请管理页 Prompt

```text
生成一张企业级安全管理平台的邀请码管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面顶部包含 tabs：邀请加入、申请列表。下方是搜索区，支持组织名称/邀请码和状态筛选。工具栏包含删除、刷新邀请码、下载邀请码，右上包含创建邀请码和导出全部。主体为邀请码表格，字段包含状态、组织名称、有效期、邀请码、审批类型、操作，操作支持刷新、删除、复制邀请码。底部包含总条数、每页条数和分页器。
```

#### 备份与恢复页 Prompt

```text
生成一张企业级安全管理平台的备份与恢复页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面右上包含手动备份、自动备份、导入备份和刷新按钮。主体是备份记录表格，字段包含备份时间、备份内容、文件大小、备份形式、备份状态、创建者、完成时间、备注、操作。操作包括删除、还原、下载，状态以绿色成功状态显示。整体简洁、专业、后台感强。
```

#### 应用管理页 Prompt

```text
生成一张企业级安全管理平台的应用管理页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面上方为筛选区，包含应用名称/URL、所属应用组、创建人，以及搜索、重置、展开按钮。下方工具栏包含删除、导出、复制，右上角包含新建应用、管理默认图标和快捷操作按钮。主体为应用列表表格，字段包括应用名称、URL、图标、开放共享、操作。图标列为统一尺寸的小图标，操作包括编辑和查看，底部含分页。
```

#### 网络配置页 Prompt

```text
生成一张企业级安全管理平台的网络配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面包含深色顶部导航、浅色左侧导航和白色主内容区。主内容顶部有局部 tabs：接口信息、路由管理、网络检测、可信主机配置、SSH服务。页面中依次展示多个分区：接口信息表格、路由管理表格、网络检测配置、可信主机配置表格、SSH服务开关与端口设置。表格包含开关、状态圆点、网络信息字段和分页。整体为复杂系统配置页风格，信息清晰、分组明确。
```

#### 文件检测配置页 Prompt

```text
生成一张企业级安全管理平台的文件检测配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面左侧为配置导航，顶部包含局部 tabs：基础配置、检测类型设置、检测特征设备、加密压缩包破解。主内容为纵向多个白色配置分组，包含文件基础设置、检测设置、静态检测引擎、下载样本查看口令、虚拟机网络配置、沙箱网关网卡配置、虚拟机环境。表单中包含滑块、单选框、开关、下拉框、输入框、说明文字和保存按钮。底部虚拟机环境区采用左侧系统列表、右侧环境详情布局。
```

#### 多级配置页 Prompt

```text
生成一张企业级安全管理平台的多级配置页高保真界面效果图，简体中文，16:9，4K，正视角平面 UI。页面左侧为局部导航，当前选中多级配置。主内容包含两个配置分组：接入上级节点设置、接入下级节点设置。上级节点设置中展示是否接入上级节点、是否数据分权同步。下级节点设置中展示是否允许下级节点接入、新增下级节点按钮和节点管理表格。表格字段包括节点名称、节点地址、描述、联系人、电话、节点状态、操作，状态用彩色圆点和文字表示，操作包括编辑、配置同步、删除，底部含分页。
```

#### 新增/编辑节点弹窗 Prompt

```text
生成一张企业级安全管理平台的新增下级节点弹窗高保真界面效果图，简体中文，4:3，正视角平面 UI。画面为灰色遮罩背景，中间为白色居中弹窗。弹窗标题为“新增下级节点”或“编辑节点”，右上角有关闭按钮。表单字段包括节点名称、节点地址、访问令牌、描述、节点联系人、联系人电话，其中节点名称和节点地址为必填项，访问令牌右侧有复制按钮。底部右对齐放置取消和保存按钮。整体表单整齐、留白合理、后台配置风格明显。
```

---

### 系统通用配置 Negative Prompt

```text
不要做成数据看板，不要出现夸张渐变背景，不要使用营销官网式大插画，不要把复杂配置页做成单一表格，不要省略筛选区和工具栏，不要让分页消失，不要让状态只有颜色没有文字，不要让弹窗没有遮罩或没有关闭按钮，不要生成乱码字段，不要让表格列顺序混乱，不要把网络配置、检测配置、多级配置做成普通列表页。
```

### 系统通用配置生成验收标准

| 检查项 | 标准 |
|---|---|
| 页面类型识别 | 能区分组织、任务、告警、日志、邀请、备份、应用、网络、检测、多级配置等模板 |
| 页面骨架 | 具备后台系统统一骨架，导航、筛选、内容区逻辑明确 |
| 筛选区 | 输入框、下拉框、搜索等组件布局规范 |
| 工具栏 | 批量操作与新增操作分区明确 |
| 表格质量 | 表头、行高、列结构、操作列、分页完整 |
| 状态表达 | 状态点、标签、进度条等表达清楚，含文字 |
| 配置分组 | 网络/检测/多级配置等复杂页面分组清楚 |
| 弹窗质量 | 居中、遮罩、必填项、底部按钮齐全 |
| 中文可读性 | 主要文字基本可读，无大面积乱码 |
| 风格统一 | 浅色、极简、安全后台风格统一 |

---
